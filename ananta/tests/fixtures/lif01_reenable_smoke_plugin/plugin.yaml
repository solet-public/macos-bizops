name: lif01_reenable_smoke_plugin
description: >
  Test fixture for the LIF-01 set_plugin_enabled disable->enable smoke.
  LifecycleManaged; prepare_for_readiness raises RuntimeError unless
  orchestrator_ref was injected first -- reproduces the exact Dax S31.4
  failure mode when the legacy clear-and-rebuild path drops it.
optional: true
