{
  "process_key": "service_interface::memory_service::consolidate_cron",
  "display_name": "Consolidate Memories (Cron Sibling)",
  "description": "Scheduler-fired EDGE_SINK sibling that summarizes weak episodic memories into semantic memories. Terminal action; no result-processor dispatch and no inference scaffold. Cron-only wiring (`is_discoverable=False`); not surfaced via process_search. The model-callable equivalent is service_interface::memory_service::consolidate.",
  "embedding_description": "Scheduler cron sibling that fires weekly episodic-to-semantic consolidation as an EDGE_SINK terminal action. Operator wiring used only by the actr_memory consolidation schedule; returns candidates/clusters/actions without re-entering the inference scaffold. The model-callable equivalent is service_interface::memory_service::consolidate."
}
