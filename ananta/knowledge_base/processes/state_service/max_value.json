{
  "process_key": "service_interface::state_service::max_value",
  "display_name": "Max Column Value",
  "description": "Return the largest value of a single column over rows matching table + filter criteria. The SQL MAX runs inside the owner plugin and returns only the scalar at data.result.value (column-typed, or null over an empty set). No rows are materialized or shipped.",
  "embedding_description": "Find the maximum value of one column across a filtered set of rows -- the largest number, the latest timestamp, or the highest-sorting value -- returned as a single scalar at data.result.value.",
  "result_processor_customizations": {
    "action_label": "Max Column Value",
    "result_type": "max_value_result",
    "result_description": "Column maximum at data.result.value (column-typed scalar, or null over an empty set; a TIMESTAMP column yields a naive datetime the caller normalizes)",
    "presentation_guidance": "",
    "output_action_guidance": "Determine the appropriate next action based on the context and action result."
  },
  "error_processor_customizations": {
    "action_context": "Computing the maximum of a column in a state table using table, column, and filter criteria",
    "error_interpretation": "Error with 'table': missing or invalid table name. Error with 'column': max_value requires a column to aggregate. Error with 'filters': invalid filter format.",
    "recovery_guidance": "For table or column errors: provide a valid table name and a column to aggregate. For filter errors: verify the filter matches the query_state grammar (equality / list = ANY / is_null / is_not_null / range)."
  }
}
