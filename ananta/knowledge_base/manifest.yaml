name: ananta_service
description: >
  Ananta platform service interface process definitions and reference documentation.

category: platform-internal
tags:
  - platform_reference
  - service_interfaces

content:
  patterns:
    include:
      - "**/*.md"
      - "**/*.txt"
    exclude:
      - "**/.git/**"
      - "**/.archive/**"
      - "processes/**"
  chunking:
    strategy: headers
    max_chars: 3000
    overlap_chars: 200
