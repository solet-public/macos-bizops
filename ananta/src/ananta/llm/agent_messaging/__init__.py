"""Agent messaging — durable thread/message persistence for inter-agent calls.

Shared platform code consumed by ``agent_messaging_plugin``, which both
exposes the service through structural typing and adapts it to a
localhost HTTP/MCP surface (consolidated 2026-05-16 — see
``workbench/2026-05-16_codex_mcp_channel_and_inter_agent_outstanding_work.md``).
"""
