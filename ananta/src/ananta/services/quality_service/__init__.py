"""quality_service — decorated discovery surface for the GTE-02 verbs.

The implementation is bound via ``service_bindings`` to
``platform_dev_surface_plugin``; this package carries only the
``@service_interface_process``-decorated ABC scanned by the process registry.
"""
