"""Local self-deployment service public discovery surface.

The actual contract lives at
``ananta.interfaces.local_self_deployment_service_interface.LocalSelfDeploymentServiceInterface``.
This package carries the ``@service_interface_process`` registration
wrappers for macOS/local-only self-deployment operations so bound
``ServiceProvider`` plugins stay callable through ``service_interface::*``.
"""
