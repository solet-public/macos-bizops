"""repo_service — decorated discovery surface for the read-only repo verbs.

Implementation bound via ``service_bindings`` to ``platform_dev_surface_plugin``;
this package carries only the ``@service_interface_process``-decorated ABC.
"""
