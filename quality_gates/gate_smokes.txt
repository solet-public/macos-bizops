# Gate-eligible smoke register (run by quality_gates/run_smokes.py).
#
# One repo-relative *_smoke.py path per line. A smoke gates the commit only
# once it is listed here. This is a tracked, growing allowlist (same model as
# god_class_allowlist.txt / radon_*_allowlist.txt): adding a smoke is an
# explicit, reviewable act, and the gate never silently broadens to smokes that
# need a live platform. Every entry MUST pass offline (no live solet / LM Studio),
# with one ratified exception class (GTE-09, Coordinator-Day Q2): a smoke whose
# LOAD-BEARING proof genuinely requires a live external dependency (e.g.
# external_postgres_plugin's smoke_readonly.py/smoke_multistatement.py, a real
# Postgres) may register if an unreachable dependency is a VISIBLE, disclosed
# outcome — an unmissable banner plus the dedicated SKIP exit code (77, the
# automake/Meson/CTest convention; UPDATED 2026-08-08, undeclared-dependency
# audit: run_smokes.py now reports passed/skipped/failed distinctly, so the
# GTE-09-era fail-loud-instead-of-silent-skip workaround is no longer needed) —
# rather than a silent skip. The offline rule is about invisible scope creep,
# not about barring a documented, disclosed live-dependency smoke.
#
# Seed set = the blue->green TRANSITION-critical smokes (the cutover must not be
# confirmed with any of these red). Grow as new gate-relevant smokes land
# (e.g. the proposed S1 post-transition-invariant + S2 readiness-truth smokes).

plugins/macos_self_deployment_plugin/tests/swap_round_trip_smoke.py
plugins/macos_self_deployment_plugin/tests/cutover_failure_smoke.py
plugins/macos_self_deployment_plugin/tests/complete_swap_crash_consistency_smoke.py
plugins/macos_self_deployment_plugin/tests/rollback_release_smoke.py
plugins/macos_self_deployment_plugin/tests/schema_preflight_smoke.py
plugins/macos_self_deployment_plugin/tests/preflight_refusal_smoke.py
plugins/macos_self_deployment_plugin/tests/release_manager_crash_consistency_smoke.py
plugins/macos_self_deployment_plugin/tests/release_builder_dirty_tree_gate_smoke.py
plugins/macos_self_deployment_plugin/tests/supervisor_smoke.py
plugins/macos_self_deployment_plugin/tests/backstop_loop_integration_smoke.py
plugins/macos_self_deployment_plugin/tests/drain_sigterm_exit_smoke.py
plugins/macos_self_deployment_plugin/tests/router_socket_wait_smoke.py
plugins/macos_self_deployment_plugin/tests/router_socket_ownership_smoke.py
# LaunchAgent plist render contract (Option-B supervisor KeepAlive, out-of-tree
# CWD, and the §39.2 Homebrew-inclusive PATH the daemon needs to see tmux at
# all). Pure render assertions -- no launchctl, no filesystem mutation outside
# the smoke's own scratch dir. Registered 2026-08-10: it existed and was green
# but was never on this allowlist, so the gate had been blind to a silent
# regression of the boot-path plist.
plugins/macos_self_deployment_plugin/tests/autostart_plist_render_smoke.py

# Phase 0 "freeze current contracts" — substrate-contract guards that must stay
# green through the coding-agent substrate transition
# (workbench/2026-07-01_claude_coding_agent_substrate_architecture_and_planning_v2.md).
# Each fails if a working substrate invariant is regressed or bypassed.
ananta/tests/core/substrate_contracts/queue_completion_is_dispatch_smoke.py
ananta/tests/core/substrate_contracts/deterministic_continuation_single_step_smoke.py
ananta/tests/core/substrate_contracts/wbs_result_processor_kind_required_smoke.py
ananta/tests/core/substrate_contracts/pre_dispatch_validation_smoke.py
ananta/tests/core/actions/action_factory_edge_sink_skip_smoke.py

# D0.3/Option-A (sync-verb Phase 0 remediation, workbench/2026-08-09_sync_verb_d03_deferred_completion_doctrine_syncverb-doctrine.md):
# AsyncJobManager's completion-handler firing is a production mechanism (real
# evidence: comfyui_image_generation_plugin's completion_handlers fires on
# every generated image) — this is its hermetic regression guard, not the
# live proof.
ananta/tests/core/actions/async_job_manager_completion_handler_smoke.py

# Phase 2 "context assembly as a process" — the pure agent-context briefing
# boundary (group_blocks_into_briefing): named-bundle grouping + provenance +
# answer-contract-as-data + the budget contract. Offline (hand-built blocks).
ananta/tests/core/context_service/agent_context_briefing_smoke.py
# assemble_agent_context end-to-end via a fake factory (full envelope + budget +
# bundle filter + fail-fast on unknown bundle); and EMBEDDING_SERVICE as a
# declared REQUIRED service (POR §1.3 ◆R2).
ananta/tests/core/context_service/assemble_agent_context_smoke.py
ananta/tests/core/context_service/required_services_embedding_smoke.py

# Phase 3 "author-by-value registration" (Seam A) — the 4 thinking_service
# validate/register verbs for agent-authored WBS documents + joseki cards:
# Q4 error/warning tiers, hard-fail-before-storage, no-overwrite guards,
# lifecycle row at draft. Offline (recording stubs; real validators/parser).
# ABC↔wrapper↔plugin parity for thinking_service: every decorated verb on
# ThinkingServiceAPI must be callable on the concrete ThinkingService wrapper
# AND the bound plugin — kills the 4-layer-rule miss caught live 2026-07-02
# ('ThinkingService' object has no attribute 'validate_authored_joseki').
ananta/tests/core/thinking_service/wrapper_parity_smoke.py

# validate_all_embedding_descriptions logs ONE aggregate WARNING per finding
# category (missing / out-of-range) instead of one WARNING per process --
# ~50 pre-existing gaps were repeating the same lines on every single boot.
ananta/tests/core/process_registry/embedding_description_summary_smoke.py

# purge_old_logs (7-day log-directory retention sweep, cold-boot-wired in
# cli.py) deletes files past retention.days recursively, keeps fresh ones,
# and no-ops on a missing log dir. Offline (tempfile-only, no live platform).
ananta/tests/cli/log_retention_purge_smoke.py

# Phase 4 "pull-based step engine" (Seam C) — start/get_next/record/advance
# over durable KB annotations: invalid observations change nothing, Q15
# auto-safe evaluation, and the ◆R2 EXPLICIT disconnect/resume acceptance
# case (fresh session resumes from the next unexecuted step, no lost
# observations). Offline (in-memory durable doubles; real engine/parser).
plugins/default_thinking_plugin/tests/pull_execution_smoke.py

# Phase 5 "vertex resolver + session providers" (Seam B) — the InferenceService
# short-circuit that routes MCP-originating flows to their bound session vertex
# (or DEFERs when the session is absent) BEFORE the default local model. The
# ◆R2 three-way discriminator (role-resolvability, sharpened to
# explicit-binding-this-lifetime): PROVIDER / DEFER (role-holder absent OR
# tombstoned roleless instance — never silent-Qwen) / DEFAULT (unbound /
# untagged, incl. the v4 §8 streamable-falls-through case). Offline (fake
# plugin/state/provider for the resolver; RealShapeState for the real role
# accessors + tombstone lifecycle). The provider smoke also pins the
# D2-window ruling (2026-08-04): get_autonomic_provider answers None for a
# HELD slot too — a peer-session holder cannot serve raw vertex turns, so
# the vertex lane DEFERs to the durable queue (mutation-proven: the
# pre-ruling return-provider body reds exactly the held-slot leg).
ananta/src/ananta/services/inference_service/tests/inference_vertex_resolver_smoke.py
plugins/agent_messaging_plugin/tests/session_inference_provider_smoke.py

# INF-01 sub-slice-2 — sys:autonomic auto-assignment lifecycle (§D.9): Trigger-1
# vacancy-fill/crash-heal, Trigger-2 grace-delayed succession (reconnect NO-OP /
# superseded / newest-live-provider-capable successor / leave-vacant), manual-set
# with §5.4 both-party notices, first-claim drain honesty (retain-without-
# primitive / hard-delete-on-re-drive) + wiring. Offline (RealShapeState + fake
# bridge/registry/provider collaborators).
plugins/agent_messaging_plugin/tests/autonomic_assignment_smoke.py

# INF-01 ★ FLIP-ASSERTION (Day-ruled, sub-slice-2 review centerpiece): a vacant /
# gone-holder sys:autonomic slot DEFERs the organism's error/result turn into the
# durable NO-LOSS queue — it must NEVER fall to the local default model (the
# dead sub-slice-1 interim). Also pins the §D.3 safe floor (plugin-unreachable /
# lookup-raise stay LOCAL, counted). FAILS if vacancy ever falls LOCAL again.
ananta/src/ananta/services/inference_service/tests/autonomic_flip_smoke.py

# REL-09 — bridge-lifecycle full cleanup: the idle sweeper (sweep_idle finally
# DRIVEN) routes every expired bridge through the SAME cleanup as close (sidecar
# clear + ◆R2 tombstone + Trigger-2 hook + registry unregister — swept ≡ closed),
# a swept sys:autonomic holder fires grace succession with exactly-one notice,
# and the startup reconciliation purges pre-restart zombie peer_binding rows.
plugins/agent_messaging_plugin/tests/bridge_lifecycle_sweep_smoke.py
plugins/agent_messaging_plugin/tests/bridge_delivery_dead_origin_smoke.py

# Bridge /health readiness-probe gating (Task #11 cloud-boot fix): no probe →
# unconditional 200 (legacy contract); probe False → 503 starting; flip → 200
# without router rebuild. Gate-registered at REL-09 after the smoke was found
# silently rotted (stale kwarg) — registration is what prevents that rot class.
plugins/agent_messaging_plugin/tests/bridge_health_readiness_probe_smoke.py

# REL-01 Fork 4 — two-way role-addressed delivery: the consume-side lift of the
# caller's DURABLE role from the flow trigger_data
# (ActionProcessor._lift_inference_vertex_identity), the sender-stamp fallback
# ladder (role -> originating instance -> system:scheduler), the role-vs-instance
# envelope reply-to (_wake_reply_hint), and the persist-first return-leg
# reconnect survival (queued_for_replay). Role names treated as OPAQUE throughout
# (arbitrary-role assertions per the operator's binding constraint). Offline stubs.
plugins/agent_messaging_plugin/tests/role_two_way_reply_smoke.py

# WS-2c V4 (A1) — the reply ADDRESS a delivery hands its recipient. The bridge
# route for peer_send_by_name passed reply_to_role="" HARDCODED since e02beb860,
# so every role-addressed send from an MCP session handed back an INSTANCE
# reply-to on a healthy fleet (churn only decided when it hurt); the direct path
# had no such parameter at all. Drives the REAL route impls — a test over
# build_wake_reply_hint passes with the whole route wiring absent. Also pins the
# EXACTLY-ONE-ROLE rule: sorted(roles)[0] is fine for a flow TAG and misroutes a
# reply ADDRESS, so a multi-role sender keeps the instance reply-to (DEF-3).
# Fixture is RealShapeState, not a bare stub: the predecessor stub DEGRADED
# rather than raised, which made a role-hint assertion unfalsifiable.
plugins/agent_messaging_plugin/tests/reply_address_role_smoke.py

# WS-2c V4 (A2) — direct reply hints carry the sender's stable logical-session
# key beside the exact instance id. The reachable peer_send path resolves the
# instance FIRST and uses the session key only after peer_unreachable; unknown
# keys preserve the original error and duplicate keys fail loud with candidate
# ids. Real in-memory PeerRegistry filter; hermetic constructed rotation state,
# explicitly NOT a live restart-wave claim. Both MCP schemas + stdio wire pinned.
plugins/agent_messaging_plugin/tests/sender_session_reply_fallback_smoke.py

# §34.6 — CLI-originated sends carry the caller's identity instead of the
# "System (Scheduler)" sentinel. Drives the real chain: the bridge/open route
# parks the caller's opaque launcher session key, PlatformSurface derives the
# identity from the peer REGISTRY (never from the request) under a key family
# separate from inference_vertex_*, the real ActionProcessor lift carries it,
# and both send verbs stamp it — send_peer_message previously hardcoded the
# sentinel on EVERY transport, MCP included. Carries the negative control that
# keeps the rejected design out: attribution must never register a peer
# identity, because PeerRegistry.register sweeps by session_label and
# close_bridge unregisters by bridge_id, so a registering CLI call would evict
# its own session's row. Offline stubs; in-memory registry store.
plugins/agent_messaging_plugin/tests/cli_sender_attribution_smoke.py

# WS-2d census batch 3 — the wake hook's footer, identity guard, and pairing.
# D6: extracts the process key from the footer the model is ACTUALLY shown and
# asserts the platform can answer it (decorated verb + registered KB process
# JSON + declared arguments) — that string 500'd against the live registry for
# as long as peer_inbox was unregistered and nothing would have noticed, because
# nothing anywhere executes it as a check. D3: `wake` must stay silent for a
# non-fleet session (the Stop hook is user-scoped) but be LOUD for a
# half-configured one, matching `watch`, which dies loud on the same input.
# D4: the watcher publishes its actual spool choice so `--spool` / `--no-spool`
# can no longer decouple the pair invisibly. Hermetic; no bridge, no DB.
plugins/agent_messaging_plugin/tests/wake_footer_and_pairing_smoke.py

# REL-04 — role-claim handover notifications (peer_claim_role): a displaced prior
# holder bound to a DIFFERENT instance is notified (best-effort); the claim NEVER
# fails if that holder is unreachable (loud log); the new holder is confirmed so
# it drains the role backlog. Role names treated as OPAQUE (arbitrary-role
# assertions). Drives the real _notify_role_handover orchestration + the real
# best-effort _send_handover_notice (never-raises). Offline stubs.
plugins/agent_messaging_plugin/tests/role_claim_handover_smoke.py

# §4.3.3a takeover reachability — the advertised process, bridge, and watch
# surfaces expose an explicit default-false takeover; false refuses a live
# holder, true displaces + warns + notifies, vacant takeover is ordinary, the
# authorization is consumed before reconnect exceptions, and a displaced
# watcher exits loudly on its next refused plain re-claim rather than re-steal.
plugins/agent_messaging_plugin/tests/takeover_route_reachability_smoke.py

# S1-S3 agent_session_id splice — reconnect self-refresh for ROLE sends. Drives
# the REAL /peer/register route END-TO-END (FastAPI TestClient + in-memory state):
# a reconnect (rotated agent_instance_id, stable agent_session_id) re-points every
# held role in the agent_role_binding state table (rerouted:n), the reconnected
# holder's backlog re-delivers, a single session-id CAS re-points ALL held roles,
# a missing carrier fails LOUD (no_session_key, no mutation), and the response is
# a STATE token never an address-book one (S3). Plus PEER_BINDING agent_session_id
# data-capture (current_identity surfaces it; empty stays empty). Offline stubs.
plugins/agent_messaging_plugin/tests/role_reconnect_self_refresh_smoke.py

# Session-identity carrier resolution, PER-AGENT-KIND (2026-07-16) —
# _resolve_agent_session_id(agent_id): codex bridges prefer their OWN
# CODEX_THREAD_ID (authoritative, never stale for a Codex child) then
# AGENT_SESSION_ID; every other kind (incl. unknown) uses AGENT_SESSION_ID
# ONLY and never adopts the codex carrier (role-refresh CAS keys on
# agent_session_id alone — cross-agent adoption re-points the wrong session's
# roles, both nesting directions). Neither carrier → preserved loud-logged "".
# Pins: codex-both→thread-id-wins; codex-only-neutral falls back; claude_code with
# leaked CODEX_THREAD_ID→"" NOT adopted (reverse-nesting, load-bearing); unknown
# kind refuses codex carrier; neither→"" back-compat; structural chain shape.
# Offline (env-var unit scope).
plugins/agent_messaging_plugin/tests/session_id_carrier_chain_smoke.py

# R2 fix (2026-08-18): the managed-spawn session-id guard in
# mcp_bridge.__main__ — a managed MCP bridge spawn (injected AGENT_INSTANCE_ID)
# with no resolved AGENT_SESSION_ID now dies loud instead of silently
# persisting a peer_binding row that peer_holds_role/peer_claim_role can
# never confirm. Pins both discriminators: the managed+empty case raises, the
# documented interactive/un-managed degrade (self-minted instance id) is
# unaffected. Offline (pure-function unit scope, no live bridge).
plugins/agent_messaging_plugin/tests/managed_bridge_session_id_guard_smoke.py

# Role-model v4 SLICE A (additive foundation) — the two fresh tables (`role`
# entity + discriminated `role_binding`, holder_kind session|inference_provider,
# nullable identity cols + holder_identity/properties JSON + claim_epoch), the v4
# resolver with the §4.6 typed per-holder_kind fail-loud parse + the §4.3
# ResolvedRole projection (holder_kind/holder_identity/agent_session_id), and the
# §5.5/§7 entity upsert + best-effort memory ingest. Non-breaking: the live
# resolve stays on agent_role_binding until the §9 migration (slice-D). Role
# names OPAQUE throughout. Offline (RealShapeState + in-memory store).
plugins/agent_messaging_plugin/tests/role_model_v4_slice_a_smoke.py

# Role-model v4 slice-B — the RACE-SAFE claim path + act-time re-check + the
# REL-07 live heal. §5.1 claim/displace predicated-CAS (first-claim write_state
# INSERT → inspect the non-completed ActionResult → displace CAS on claim_epoch;
# a same-session re-claim is an idempotent 'refreshed', never a displace), the
# load-bearing NO-TOMBSTONE invariant (hard-delete release → re-claim works; a
# soft-delete tombstone DEADLOCKS the slot), §5.0 holds_role act-time ownership
# re-check (reference abort after displace), resolve_by_agent_session_id +
# fail-loud dup policy + agent_session_id_for_instance (claim-source), and the
# reroute acceptance (a binding WITH a session id → reconnect CAS matches; the
# pre-fix empty id → matches nothing). Role names OPAQUE. Offline (RealShapeState
# + a faithful write_state + in-memory store).
plugins/agent_messaging_plugin/tests/role_model_v4_slice_b_smoke.py

# Fleet session-management Phase B, D1 §3.1 — the role_class one-shot backfill.
# Stamps ROLE_CLASS_DEFAULT onto pre-Phase-B role rows (marker-gated, leaves
# already-set rows untouched, self-healing on an unset marker); separately
# detects (never silently fixes) a pre-existing agent_instance_id holding more
# than one named role, excluding sys:* slots from the count — re-checked every
# run, NOT itself marker-gated, since new violations can appear after the
# one-shot stamp completes. Offline (fake state over real ActionResult shapes).
plugins/agent_messaging_plugin/tests/role_class_backfill_smoke.py

# Fleet session-management Phase B, D1 §2/AMEND-4b — the session_role_claim
# cardinality gate, unit-level. Fresh claim wins; a same-session re-claim of
# the SAME role is a no-op refresh (branch i); a second NAMED role while the
# first binding still names this session is refused CardinalityConflictError
# (branch ii), never silently allowed; a stale orphan self-repairs in place
# (branch iii); a lost predicate on the self-repair re-enters the bounded
# loop rather than falling through (Architect ratification #1); the
# displacer's cleanup delete is predicated (fires / benign no-op). TRUST-MODEL
# HONEST: genuine against an honestly-identified session, NOT hardened
# against a forged identity pair (tracked debt, see module docstring).
plugins/agent_messaging_plugin/tests/session_role_claim_store_smoke.py

# Fleet session-management Phase B, D1 — the cardinality gate wired into
# claim_role_for_session END-TO-END (real RealShapeState + a real
# PeerRegistry over an in-memory Store). Per the Architect ratification, the
# two displacement legs are driven SEPARATELY with an assertion of WHICH
# guard fired each time: (a) the displacer's cleanup delete disabled ->
# displacement still succeeds, the loser's row persists, and its OWN next
# claim self-repairs via branch (iii); (b) a clean displacement -> the
# loser's row is gone immediately (the delete fired, branch iii never ran).
# Plus Dawn's two landing conditions: (c) REL-07(1) fill-when-empty measured
# through the new gate; (d) the §3.1 reserved-mint guard (refuses a fresh
# <solet>-Main mint, passes an already-legislated one). TRUST-MODEL
# HONEST throughout — see module docstring.
plugins/agent_messaging_plugin/tests/role_claim_cardinality_gate_smoke.py

# Fleet session-management Phase B, D1 §3.2/§3.3 (AMEND 2a/2b) — the
# managed_session ledger + session_transition audit trail state layer.
# TWO GUARDS, TWO TOKENS, each proven on its OWN failure mode: an illegal
# edge (not in LIFECYCLE_TRANSITIONS) is refused by a Python check BEFORE
# any write (IllegalLifecycleTransitionError, ledger verifiably untouched);
# a legal edge that loses its race is refused by the predicated update_state
# itself (StaleLifecycleStateError) — exercised by actually racing it, not
# by re-asserting the illegal-edge path under a different name. The audit
# row lands ONLY after a successful ledger write (ordering measured, not
# asserted) — never document a transition that didn't happen.
plugins/agent_messaging_plugin/tests/session_lifecycle_store_smoke.py

# T1 usage-capture lane (2026-08-05) — session_claude_mapping state layer
# (schema.py + session_claude_mapping_store.py): the agent_instance_id <->
# claude_session_id mapping, ONE-TO-MANY over a worker's lifetime (a /clear
# or /resume rotates the Claude session UUID without touching the
# managed_session row). Proves the idempotent-upsert contract the ruling
# requires: re-ingesting the SAME (agent_instance_id, claude_session_id,
# captured_at) triple (the crash-before-spool-delete case) UPDATES the
# existing row rather than duplicating it; a genuinely different firing
# (rotated session_id or later captured_at) inserts a SECOND row alongside
# it — the rotation history survives, never a single mutable slot.
plugins/agent_messaging_plugin/tests/session_claude_mapping_store_smoke.py

# T1 usage-capture lane (2026-08-05) — session_claude_mapping_ingest.py, the
# new plugin::agent_messaging_plugin::drain_session_claude_mapping_spool
# verb (full create-process discipline: @platform_process + EdgeProcessDefinition
# + KB process JSON, cosine-checked against every sibling in this plugin's
# process JSONs, highest similarity 0.79 -- well under the 0.90 warn/0.97
# fail thresholds). Drains the SessionStart hook's file-per-firing spool
# into session_claude_mapping: a fresh file is ingested and deleted; a
# malformed file is skipped and LEFT IN PLACE, never silently deleted; a
# missing/nonexistent spool dir returns zero-processed, not an error; a
# file that survives a crash before its post-write delete is re-ingested
# next drain as an idempotent upsert on the same row, never a duplicate.
# The final case is this plugin's own "wired consumer" proof convention
# (mirrors host_adapter_liveness_smoke.py): drives the REAL
# AgentMessagingPlugin._run_session_lifecycle_sweep against a duck-typed
# self and confirms the sweep tick itself reaches this drain, not merely
# that the function exists unwired.
plugins/agent_messaging_plugin/tests/session_claude_mapping_ingest_smoke.py

# R1 held-authorization queue (2026-08-17, seat GO ruling) --
# held_authorization_store.py: the mechanically-enumerable "what is blocked
# on <role>?" backstop for a peer commit request Git-Controller refused for
# lack of a first-party authorization -- the platform-side counterpart to
# the OBLIGATIONS running-log slot. Proves the two properties the design
# leans on: the open-only default (retired_at IS NULL) correctly excludes a
# retired entry via the {"op": "is_null"} filter grammar, not a bare-None
# false-negative that would match zero rows silently forever; and a second
# retire call on an already-retired entry is a no-op (False, zero rows
# affected) that never overwrites the first retirement's reason/provenance
# -- load-bearing for a queue whose whole purpose is accountability.
plugins/agent_messaging_plugin/tests/held_authorization_store_smoke.py

# T1 S3 (2026-08-05, seat's design ruling) — budget_report.py, the new
# plugin::agent_messaging_plugin::budget_report verb (full create-process
# discipline). FIRST cross-plugin state read of its kind in this codebase:
# joins agent_messaging_plugin's managed_session/session_claude_mapping
# against session_ledger_service's session/event tables via
# namespace-parameterized StateManagementInterface calls against
# session_ledger's own exported NAMESPACE/TABLE_SESSION/TABLE_EVENT
# constants + the SourceVendor enum (no raw SQL, no cross-service singleton
# reach). Proves: a covered row sums its usage_json fields generically and
# sets the as_of staleness marker; all three DISTINCT uncovered shapes (no
# mapping row, mapping resolves to no ledger session, ledger session with
# no usage-bearing events) count as sessions_uncovered per the seat's
# no-silent-caps coverage-disclosure rail; usage sums across every
# claude_session_id a worker has ever rotated through; lane_id/budget_line
# filters; by_model breakdown including the empty-string bucket; a
# boolean/string field in usage_json is never summed (bool is an int
# subclass in Python); and the real AgentMessagingPlugin.budget_report verb
# handler genuinely extracts params and calls through.
plugins/agent_messaging_plugin/tests/budget_report_smoke.py

# T2 (fleet-token-efficiency lane, 2026-08-05, seat's approved text arm-b79d2549…)
# — authority_contract.py, the fleet delegation contract rendered into every
# spawn's --append-system-prompt so a worker's authority to act is anchored in
# a TRUSTED spawn surface (the adapter's own process launch) rather than
# peer-channel prose. Proves: every named placeholder resolves; the template's
# own literal {"op": "is_null"} JSON example survives rendering byte-for-byte
# (confirms plain str.replace is used, never str.format, which would misparse
# it); the seat's two approved text edits hold (opening sentence reads "a
# worker of class", never "claiming role"; FIRST ACTIONS defers the role NAME
# to dispatch prose, never claims the role_class string itself); a spec
# missing role_class/brief_ref/spawned_by_role renders blank slots rather than
# raising (authority-at-first-contact stays ON by default even for a caller
# that predates T2); and the RED-FIRST render-time guard the seat's approval
# explicitly required — an unresolved {placeholder} must raise
# UnresolvedPlaceholderError, never ship into a live system prompt.
plugins/agent_messaging_plugin/tests/authority_contract_smoke.py

# Fleet session-management Phase B, D1 §3.2/§5 — the registration-hook fix
# (Dawn ruling arm-11511b07: managed_session declared agent_session_id/
# agent_id/host_ref columns but nothing wrote them, and no hook owned the
# spawning->live edge). Drives the REAL /peer/register route end-to-end
# (FastAPI TestClient + in-memory state), RED before the fix: a spawned
# session's first registration left its ledger row stuck in 'spawning'
# forever with the identity columns permanently null. GREEN after: first
# registration backfills agent_session_id/agent_id and fires spawning->live;
# a reconnect (or a late register arriving after something else moved the
# row to a terminal state) re-writes identity but never re-fires the edge or
# pulls a terminal row back to 'live'; a session with no managed_session row
# at all (the overwhelming common case — ordinary operator launches) is
# completely unaffected, no exception, no phantom row created.
plugins/agent_messaging_plugin/tests/registration_hook_managed_session_smoke.py

# Fleet session-management Phase B, D1 §5 — the HostAdapter Protocol +
# resolution/selection semantics across the 'operator' and 'headless'
# adapters ('tmux' lands in a later separately-land-able D-step). Selection
# is declared, never probed (C3): an explicit blank host override refuses
# (HostNotDeclaredError, distinct from an unregistered host name); an
# env-declared 'tmux' fails loud with HostMechanismMissingError (never a
# silent substitute — that token now means ONLY "no adapter registered at
# all"); the operator adapter refuses to spawn/terminate (HostCannotSpawnError,
# degenerate — observes via registration only); the headless adapter is NOW
# REGISTERED but still config-gated fail-closed (host_cannot_spawn with
# remedies in an unconfigured environment — never a silent bypass-permissions
# default). No real subprocess spawn here — that's headless_adapter_smoke.py.
plugins/agent_messaging_plugin/tests/session_hosts_smoke.py

# Fleet session-management Phase B, D1 §5 — the 'headless' HostAdapter
# itself (headless_adapter.py), the universal-floor adapter hardening WS-6's
# driven_sessions.py prior art into production code. verify_config()'s 4
# remedies fire independently (missing binary/SOLET_NAME/permission
# mode/.mcp.json); spawn() refuses BEFORE ever touching the OS when
# unconfigured or missing agent_instance_id (the identity backfill_registration
# depends on — verified against mcp_bridge/__main__.py's real
# AGENT_INSTANCE_ID-honoring contract, not assumed); the exact env/argv wiring
# (AGENT_SESSION_ID minted exactly once, label falls back to agent_instance_id
# when no lane_id) via a records-only fake popen_fn; alive()/terminate()/
# shutdown() against REAL short-lived subprocesses (real kill -0 + SIGTERM/
# SIGKILL + reap, never the real claude binary); driver_channel() returns None
# for any host_ref this adapter's in-memory map doesn't recognize (the exact
# post-restart case clear_session/compact_session will check, AMEND 5b) OR
# whose tracked pid has since died on its own without ever being terminate()'d
# — a zombie still answers kill(pid, 0) until reaped, so alive()/
# driver_channel()/_sigterm_then_kill all use Popen.poll() (which reaps) for
# any TRACKED process rather than the raw kill-0 probe (only used for an
# untracked host_ref, e.g. post-restart); send() on a dead process's stdin
# swallows the broken pipe (fire-and-forget contract) instead of raising an
# unmapped exception through the clear_session/compact_session verb layer.
plugins/agent_messaging_plugin/tests/headless_adapter_smoke.py

# Vault-read envelope bug (Dax Part 36 §36.2) — _vault_retrieve_value keys on
# the REAL macos_vault_plugin ActionResult envelope (action_status=="completed"
# for both a hit and a genuine miss, distinguished by data shape), not the
# imagined top-level "status"=="success" the old bearer-HMAC-key read checked
# and never got. The fake vault returns the real envelope shape byte-for-byte
# — a fake returning the imagined shape is exactly what let this ship.
plugins/agent_messaging_plugin/tests/vault_bearer_hmac_key_envelope_smoke.py

# T1 usage-capture lane (2026-08-05) — red-first regression guard for the
# stdout-never-read fix in headless_adapter.py's spawn(): before the fix,
# stdout was piped but never drained (only stderr was), so a chatty child
# (exactly what a real worker's stream-json traffic is) blocks forever on a
# full OS pipe buffer with nothing on the platform side noticing. Monkeypatches
# _drain_pipe to skip its first call (spawn()'s fixed stdout-then-stderr call
# order) to reproduce the pre-fix hang against the REAL spawn(), confirms it,
# restores the real function, and confirms the same chatty child now exits
# cleanly. Also confirms Ruling 3's non-fatal contract: a child killed
# mid-write never raises out of the drain threads.
plugins/agent_messaging_plugin/tests/headless_adapter_stdout_drain_smoke.py

# T1 usage-capture lane (2026-08-05) — slice D, headless init-event
# cross-check (ruling addendum ~17:1xZ). HeadlessHostDriver.spawn()'s
# continuous stdout drain now ALSO parses the child's first stream-json
# line for the {"type":"system","subtype":"init",...,"session_id":...}
# event (MEASURED shape, scratch probe against a real claude
# --input-format stream-json invocation) and writes it as an independent
# init_event spool observation (same file-per-firing mechanism the
# SessionStart hook uses) -- non-fatal at every step: no APP_HOME, no
# matching line, or a write failure all leave spawn() and the drain
# completely unaffected. Proves: a real init event is captured and
# spooled correctly; ordinary non-stream-json stdout writes nothing;
# malformed first-line JSON never raises out of the drain thread; APP_HOME
# unset is a silent no-op, same contract as every other spool env var leg
# in this lane.
plugins/agent_messaging_plugin/tests/headless_adapter_init_event_capture_smoke.py

# Fleet session-management Phase B, §6 L3 rule 1 — the per-work_class
# spawn-default config substrate (plugin.yaml's work_class_defaults block,
# NOT a new state table: "cheapest capable model per work_class" is an
# operator/governance policy call this plugin does not invent, so the
# substrate ships with an empty default and a pure lookup/apply pair).
# _as_work_class_defaults tolerates a malformed entry (skips it, logs,
# keeps well-formed siblings) rather than failing the whole config build;
# _apply_work_class_defaults never overrides an explicit caller model/effort
# and is a true no-op (same object identity) for an unconfigured work_class
# or an empty config block — the shipped-default case is provably inert, not
# just "untested."
plugins/agent_messaging_plugin/tests/work_class_defaults_smoke.py
# restart_session's spawn-leg policy resolution (2026-08-10 fix): the
# choreography's rebuilt spawn request must inherit the SAME
# _apply_spawn_session_policy pipeline spawn_session() itself runs, not a
# private copy that silently drops permission_mode/allowed_tools/transport
# (managed_session has no columns for any of the three). Mutation-proved:
# commenting out _resolve_permission_mode's call site reds exactly this
# smoke's one targeted assertion, nothing else.
plugins/agent_messaging_plugin/tests/restart_spawn_policy_smoke.py
# rotate_session's verify-window false-negative fix (2026-08-10): a real
# production rotation was measurably healthy but reported verify_timeout
# because the new claude_session_id landed ~6.7s after the old 60s
# deadline. Mutation-proved: dropping the post-deadline final re-check
# reds exactly this smoke's one targeted assertion, nothing else.
plugins/agent_messaging_plugin/tests/rotate_verify_window_smoke.py
# M2.2 memory-curation pure functions (slug/slot-tag resolution, the
# activation-ranked curation report). Mutation-proved: disabling the pin
# exclusion in _score_head_line reds exactly this smoke's one targeted
# assertion, nothing else.
plugins/agent_messaging_plugin/tests/memory_curation_verbs_smoke.py

# Fleet session-management Phase B, design §1.1 point 3 — the no-half-alive-
# interfaces liveness smoke for the HostAdapter Protocol: fails when the
# interface has a wired consumer (spawn_session) but zero live implementers
# (the exact BackendRouter gap, ~10 weeks [A1]), and when the module
# docstring's claimed live/deferred adapter sets ('operator'/'headless' live,
# 'tmux' deferred) drift from the real registry. Every check self-proves it
# is load-bearing: it forces the exact failure shape (empty registry / a
# claimed adapter popped out), confirms the assertion reads False, then
# restores real state — red-first proof re-run on every gate pass, not a
# one-time manual demonstration. Also independently confirmed by manually
# removing 'headless' from _build_registry() and observing a real, uncaught
# HostMechanismMissingError from test_claimed_live_adapters_actually_resolve
# before the code was restored (land-report record of the exact mutation).
plugins/agent_messaging_plugin/tests/host_adapter_liveness_smoke.py

# D3 dormant-head retirement — the five retired backend-dispatch bridge
# tools (agent_thread_open/agent_send/agent_messages/agent_status/
# agent_close) fail CLEAN for a stale client still holding the old MCP
# tool descriptors: HTTP 404 (not 500), streamable JsonRpcError
# METHOD_NOT_FOUND (not a crash), stdio ValueError (not a Forwarder
# network attempt against a route that no longer exists). Red-first:
# every case asserts the opposite of pre-removal behavior.
plugins/agent_messaging_plugin/tests/retired_backend_dispatch_stale_client_smoke.py

# Fleet session-management Phase B, D2 §5 — the tmux HostAdapter
# (tmux_adapter.py): records-only fake-run_fn tier (always runs) + a
# real-tmux tier (SKIPs, not fails, on a machine with no tmux binary, per
# C5 universality) exercising spawn/alive/driver_channel/terminate against
# an actual detached tmux session. Also RED-FIRST-proves the shipped
# emit_role_tag.sh reference script's wrapped-vs-raw branches are genuinely
# distinct byte sequences — the exact "raw OSC is swallowed inside tmux"
# trap the R1 spike measured (FINDINGS.md).
plugins/agent_messaging_plugin/tests/tmux_adapter_smoke.py

# R4 Package C (2026-08-10) -- structural drift guard for the worker-hook
# resolution ladder: every filename headless_adapter._WORKER_INJECTED_HOOK_
# FILENAMES declares must ship in BOTH rungs (this checkout's .claude/hooks/
# and the plugin's shipped coordination-hooks/hooks/ fallback), checked
# against the real repo tree, not a synthetic fixture -- a future adapter
# change that injects a new hook filename without vendoring it into rung 2
# must red here.
plugins/agent_messaging_plugin/tests/worker_hook_shipping_smoke.py

# spawn/registration-gaps fix (2026-08-08) -- _TmuxSendKeysDriverChannel's
# paste-stability wait: the submitting Enter fires only after N consecutive
# identical tmux capture-pane reads (mutation-proven -- removing the wait
# turns the ordering assertions red), a mismatch resets the streak, a
# stability timeout still sends Enter (fail-open, matching the channel's own
# fire-and-forget contract), and a failed literal-text send never attempts
# the Enter at all. Offline, stub run_fn -- the actual real-defect fix is
# live-verified separately (findings file), not by this smoke.
plugins/agent_messaging_plugin/tests/tmux_driver_channel_smoke.py

# Fleet session-management Phase B, D1 §4 — the L1 verb bodies
# (spawn_session, list_sessions, session_status, terminate_session,
# retire_session, report_alive, and the D2-window drive_session), pure-
# function level: every declared error token exercised (unknown_role_class,
# unknown_work_class, budget_line_required, role_not_legislated,
# role_class_conflict, reserved_role_name, host_not_declared/
# host_mechanism_missing/host_cannot_spawn, session_not_found,
# illegal_lifecycle_transition, lifecycle_state_conflict, unknown_status,
# empty_text, unsupported_on_host); terminate/retire idempotency
# (already_terminal/already_retired, re-drivable by construction);
# report_alive's overdue-recovery + parked-conflict + unconditional
# report_by re-arm (even on a same-status no-op transition); drive_session's
# text dispatch + parked->live un-park + report_by re-arm (each leg
# mutation-proven: send removed / un-park removed / re-arm removed red
# exactly their own assertions, 2026-08-04).
plugins/agent_messaging_plugin/tests/session_lifecycle_verbs_smoke.py

# Drive-on-delivery lane (2026-08-04), slice 1 — session_lifecycle_verbs.
# drive_on_delivery as exercised through the real dispatch_peer_send seam:
# an eligible (live/idle/overdue) managed recipient's driver channel gets
# the notice ALONGSIDE the existing notify (never instead of it); a
# PARKED or SPAWNING recipient's channel gets NOTHING — proven against its
# OWN explicit lifecycle-state gate, not against _resolve_driver_channel or
# any other guard (mutation-proven 2026-08-04: deleting the gate reds
# exactly those two legs, no others); a raising driver channel never fails
# the outer send (containment mutation-proven the same way); a non-managed
# recipient (no managed_session row) is byte-identical to pre-lane
# behaviour; report_by is never re-armed by a delivery notice (that stays
# drive_session's own edge); state_service=None degrades silently.
# Slice 2 additions (same file, same day): sweep_deadline_dependencies fires
# an edge armed via the REAL arm_session_dependency verb and drives a live
# managed waiter's channel ALONGSIDE the existing session_dependency_wake
# append_event (mutation-proven: removing the reroute reds exactly that leg,
# nothing else); sweep_overdue_sessions' steward notice is byte-unchanged
# for an UNMANAGED steward (the common case, e.g. the seat) and drives a
# MANAGED steward's channel too.
plugins/agent_messaging_plugin/tests/drive_on_delivery_smoke.py

# Drive-on-delivery lane (2026-08-04), slice 2 — arm_session_dependency, the
# FIRST caller of the D1 session_dependency wake-edge machinery: a valid
# session-scoped arm writes exactly the expected row (waiter_lane_id never
# set, fired_at NULL); invalid_waiter/unknown_condition_kind/
# invalid_condition_ref (per-kind shape checks: session_terminal needs an
# 'agi-' id, deadline needs a parseable ISO-8601 timestamp, lane_closed
# needs non-empty) all refuse BEFORE any write; every legal condition_kind
# arms cleanly (proving the shape checks don't reject their own valid
# inputs); lane-scoped arming is unexpressable BY CONSTRUCTION (no
# waiter_lane_id field on the request dataclass at all, asserted
# structurally, not by probing a rejected value).
plugins/agent_messaging_plugin/tests/arm_session_dependency_smoke.py

# Drive-on-delivery lane, fix slice (2026-08-04, acceptance Test C catch) —
# _real_state_fake.RealShapeState's own filter-matching semantics, pinned
# directly: a bare None filter value matches ZERO rows (never a NULL cell —
# mirrors the real postgres provider's `col = NULL`, always false, NOT
# Python's `None == None`); {"op": "is_null"}/{"op": "is_not_null"} are the
# only correct ways to filter on NULL; ordinary scalar equality is
# unaffected; an armed-row insert -> is_null query -> id-keyed predicated
# update -> re-query-finds-nothing round trip (the exact shape
# _fire_armed_dependencies/_fire_session_terminal_dependencies use) proves
# end-to-end. This fixture is shared by 10+ smoke files; the pre-fix bare-
# None-equality-matches-None behavior silently masked a real production bug
# (armed session_dependency rows were never found by the sweep) behind a
# false green for as long as the fixture existed -- this smoke exists so a
# regression here is caught immediately, not silently, the way the
# original bug was.
plugins/agent_messaging_plugin/tests/real_state_fake_filter_semantics_smoke.py

# Fleet session-management Phase B, D1 §4 — the L1 verbs as reached through
# the actual AgentMessagingPlugin @platform_process methods (transport
# shim), not just the pure functions above: params/state extraction,
# VerbError -> _failure_result mapping, and call_context flowing into
# directed_by on the audit trail. Does NOT exercise the platform process
# registry, process_search, or a live process_call (no unresolved <<FIELD>>
# check) — that layer needs a running solet + restart, out of this
# smoke's reach by design.
plugins/agent_messaging_plugin/tests/session_lifecycle_transport_smoke.py

# Fleet session-management Phase B, D1 — the platform sweep (session_sweep.py,
# an on_tick rider, not a verb): sweep_overdue_sessions marks live/idle rows
# overdue past report_by (a row with no report_by is never swept — absence
# means no contract, not an expired one); sweep_deadline_dependencies fires +
# delivers armed 'deadline' session_dependency edges (predicated fired_at
# guard proven re-run-safe; delivery resolves the waiter's live binding and
# is best-effort — an unresolvable waiter or a lane-scoped edge with no
# waiter_instance_id still fires the edge as state, logged, never a crash);
# sweep_lane_closed_dependencies (Dawn ruling 2026-08-03, 'lane_closed'
# replacing the unbuildable 'lane_landed' spec kind) fires+delivers once
# every managed_session row carrying the lane_id is terminal — proven NOT
# vacuously true on a lane with zero spawned sessions, and NOT closed while
# even one row remains non-terminal. SessionRoleClaimPruner (Architect
# ratification #3) proves all three
# predicate branches — a terminal managed_session prunes immediately, a
# live managed_session OR a live registry binding is NEVER pruned regardless
# of elapsed time, and genuine absence only prunes past a grace window
# (never on one missed lookup — the 156-phantom-orphans one-sided-guard
# shape). Also folds in the retire_session crash-mid-retire redrive leg
# (Coordinator-Dawn's ask): a row left 'terminated' with an unfired
# session_terminal dependency (simulating a crash between retire_session's
# steps) is driven all the way to 'retired' by a single re-invocation.
plugins/agent_messaging_plugin/tests/session_sweep_smoke.py

# Role-model v4 slice-C — system slots §6/§6.1/§D.2/§D.6. The reserved 'sys:'
# keyspace + the slot-declaration registry (slot_name, owner_plugin, holder_kind)
# + the sys:autonomic constant (session-filled; INF-01 §D.9 consumes it). The
# §6.1 claim gate on the general peer_claim_role: a normal role passes; a
# session-filled slot (sys:autonomic) is REJECTED (auto-assigned via §D.9, not
# this verb); an undeclared sys: name REJECTED; a plugin-owned slot ALLOWED only
# for the declared owner verified against the SERVER-BUILT CallContext (lifted
# into state, never spoofable params — fixture owned-slot; fail-closed on missing
# context). Plus no-vacant-release (peer_release_role rejects sys:*) + the
# declaration-integrity check (malformed → fail-startup). Offline (pure gate +
# bare-plugin verb calls; no DB).
plugins/agent_messaging_plugin/tests/system_slots_smoke.py

# Role-model v4 slice-D Phase-1 — the §9 migration machinery (store-level; the
# live reader/writer cutover flip is Phase-2, lands last). migrate_agent_role_
# binding_to_v4 (idempotent copy → role entity + role_binding row, holder_kind=
# session; a soft-deleted legacy row is NEVER copied [no-tombstone]; a re-run is
# ON CONFLICT DO NOTHING), verify_migration_parity (legacy ⊆ v4 subset check;
# a missing binding fails the cutover loud; a v4-only system slot doesn't break
# it), release_role_binding_v4 (HARD-delete), and the carry-fwd-c reject predicate
# (a durable session claim requires a non-empty agent_session_id). NO raw SQL.
# Offline (RealShapeState + a faithful write_state).
plugins/agent_messaging_plugin/tests/role_migration_smoke.py

# REL-05 / REL-06 — deaf-wake insurance + honest delivery vocabulary.
#
# REL-06 hard rename (S6): the mint-site constants carry the new vocabulary
# (queued_wake / queued_notification), the old identifiers are gone, a grep gate
# proves ZERO woke_native_channel outside workbench/, and the two role-send
# process JSONs carry the new values. The two role dispatch smokes' value pins
# (role_dispatch_smoke / role_delivery_smoke) become the red-first behavioural
# pins for the rename + the direct-drain generalization.
plugins/agent_messaging_plugin/tests/rel06_relabel_smoke.py
plugins/agent_messaging_plugin/tests/role_dispatch_smoke.py
plugins/agent_messaging_plugin/tests/role_delivery_smoke.py
#
# REL-05 S4 — model-activity route total-classification: iterate the ACTUAL
# FastAPI route table + assert every bridge route is in EXACTLY one of
# {model-initiated, never-stamp}; peer/register is INFRA (F1, from both the
# open-auto-register and reconnect-re-register angle); touch() does not stamp.
plugins/agent_messaging_plugin/tests/route_activity_classification_smoke.py
#
# The INFRA peer/claim_role route, end-to-end: synchronous outcome in the body
# (no queued receipt, so no EDGE_SINK notification per tick); a self-re-claim
# reports 'updated' and wakes NOBODY while a real displacement notifies both
# parties; the non-serializable `prior` never reaches the body; sys: slots are
# refused on this transport; identity comes from the bridge binding, not the
# request body. Plus the assertion that earns the shared claim body — the verb
# and the route publish the SAME payload shape.
plugins/agent_messaging_plugin/tests/peer_claim_role_route_smoke.py
#
# v10 Control #5 S5 — client-side ROLE repair drain: a drain payload settles
# role rows via flip_delivered; concurrent settles for the same external_id
# emit once (single-flight); a role re-emit (emit_count>=1) carries the N3
# [re-emit n/cap ...] marker while a role original (emit_count=0) does not.
# A4 (2026-08-04): pruned from role+direct to role-only — the direct half
# retired with the direct-wake outbox it drained.
plugins/agent_messaging_plugin/tests/direct_wake_delivery_smoke.py
#
# Deaf-wake owed-delivery store-naive vs runtime-aware datetime tz-mismatch fix.
# State-store DATETIME columns are `timestamp without time zone` → every stored
# cell (created_at, last_emitted_at) reads back offset-NAIVE UTC, while _clock()
# and the bridge activity stamp are offset-AWARE UTC. _parse_iso coerces a naive
# stored cell → aware UTC at the single boundary; RED-FIRST seeds NAIVE
# timestamp strings (as the real column returns them) + an AWARE now/activity_at.
# A4 (2026-08-04): pruned to the two surviving sites (_parse_iso itself,
# _within_reemit_window — shared by the surviving role-replay drain); the
# _escalatable / _stamp_consumed_rows legs retired with the apparatus they
# guarded.
plugins/agent_messaging_plugin/tests/deaf_wake_naive_datetime_smoke.py

# Phase 6 "lifecycle hardening" (§4.3) — the authored-joseki lifecycle state
# machine (draft->candidate->proven->superseded->archived) over the
# thinking_authored_joseki row: validation-gated promotion, run-evidence
# earning proven, supersession guards + card banner stamp, the raced
# compare-and-set guard, and knowledge_base_path reconcile (the Phase-3 crumb).
# DONE-WHEN: build_layered_section_stem validates through the candidate gate.
# Offline (filter-honouring in-memory row double; real engine/validator/parser).
# Phase 6 Tier 2 "retire-process" (§4.2) — deprecation-metadata overlay
# (active_retrieval:false DERIVES is_discoverable:false = demote from
# process_search but stay callable; malformed block fails loud) + the
# on-disk reference-scan guard (exact-key match across KB source, symlink
# dedupe, and the machine-readable corpus honesty: scanned_corpora + an
# explicit unscanned note for the live DB plan/WBS instances it can't see).
# Offline (throwaway temp KB tree).
ananta/tests/core/process_registry/retire_process_smoke.py

# Phase 6 Tier §4.5 "plan template vs instance + fork-by-intent" — the pure
# core/plans model: a frozen PlanTemplate distinct from a PlanInstance,
# fork_template_to_instance as a pure transform (frozen => forking cannot
# mutate the template = the §4.5 done-when), derive_discovery_metadata over
# both kinds, the fast-fail bindings contract, and render/parse round-trip of
# the searchable plan-template KB card (so old templates are forkable by
# intent). Template state is card front-matter, NOT a state table (templates
# don't run => no run evidence; the Q14 dual applies to joseki/instances).
# Also covers the SUB-01 pure transition_template state machine: legal-map
# sanity, draft->active->superseded->archived, draft-not-a-target, illegal
# source, stray superseded_by, and superseded->archived pointer preservation.
ananta/tests/core/plans/plan_templates_smoke.py
# Phase 6 Tier §4.5 — the plan-template KB ARTICLE CLASS is searchable: a
# rendered card driven through default_knowledge_plugin's real indexing
# (metadata extraction + chunking + preamble build) surfaces the plan_template
# role, Layer 2, facet tags, and the discovery-by-intent language in the
# embedded surface. Searchable-by-construction; test_retrieval-style ranking
# needs a plan-template KB + example card ingested first = §4.6, not deployable today.
plugins/default_knowledge_plugin/tests/plan_template_retrieval_smoke.py
# 2026-07-26 retrieval-audit instrument repair (B-M5) — discover_test_files now
# traverses symlinks and matches the dot- and underscore-separated glob variants,
# and parse_article_path normalizes the symlink-relative + two real-path dialects
# fixtures declare. Regression-proves 10/45 -> 44/44 addressable fixtures discovered
# with zero parse errors against the real corpus on disk.
plugins/default_knowledge_plugin/tests/retrieval_audit_fixture_discovery_smoke.py
# 2026-07-26 retrieval-audit instrument repair (defect 3 + B-M6) — the new
# EDGE_SINK cron-target sibling audit_retrieval_corpus_cron: dual-layer ABC
# coverage (KnowledgeSearchInterface abstractmethod + both concrete
# implementers), the EDGE_SINK/is_discoverable=False decorator contract, and
# the fast-return + singleton-drainer behavior (submits to a background
# executor, returns immediately, overlapping fires are a no-op).
plugins/default_knowledge_plugin/tests/audit_retrieval_corpus_cron_smoke.py
# SUB-01 (POR §4.5 GOAL) — the verb-backed plan-template curation-lifecycle
# transition machine driven through the REAL plugin delegates
# (transition_plan_template / get_plan_template) over an in-memory plan_templates
# KB double. State is card front-matter, NOT a state table: a transition is a
# read->parse->apply->render->write with NO compare-and-set and NO retirement
# banner (the Template State line + state: tag are the searchable signal).
# Covers draft->active->superseded->archived, idempotent no-op, draft-not-a-target,
# supersession guards (missing/self/unregistered/archived replacement), identity
# mismatch rejection, and the DONE-WHEN front-matter round-trip. Cards seeded via
# render_template_card (no register verb — out of SUB-01 scope). Offline.
plugins/default_thinking_plugin/tests/plan_template_lifecycle_smoke.py

# DEP-01 Phase-2b (2026-07-03) — the extended-plan engine (create_task /
# continue_task) and the planning vertex (process_planning_results) route
# internal reasoning through inference_service.generate_completion via the
# _generate_thinking_completion seam (request-shape pinned with a recording
# fake). Code-truth: generate_completion is a plain delegation to the BOUND
# provider (the vacancy→DEFER flip lives only on the _route_vertex paths), so
# B/C serve on the bound provider — interim, operator-D.1-consistent; INF-02
# tracks an autonomic-routed completion surface. Contract-shape error /
# non-completed / empty envelopes raise typed inference_unusable; live
# provider failures RAISE and propagate loud. AST pins: B/C call the seam; NO
# _invoke_thinking_model / _strip_think_block / chat-completions endpoint /
# qwen model literal remains (TMO-02's subject is deleted — this entry
# replaces the retired timeout_wiring_smoke that pinned the deleted private
# POST's timeout). Offline (recording fakes; static ast).
plugins/default_thinking_plugin/tests/inference_rehome_smoke.py

# System-prompt shipped default (found during the v9/v10 seed-birth
# verification): profile/config/ is excluded from every seed, so every
# newborn silently ran with an empty planning prompt. Ships a plugin-owned
# default at knowledge_base/prompts/; operator override still wins if set.
plugins/default_thinking_plugin/tests/system_prompt_shipped_default_smoke.py

# Phase 6 §4.6 "per-type KB lifecycles" (A) — get_process_schema now projects the
# deprecation tombstone (replacement_key/superseded_date/migration_note/
# active_retrieval) so a migrating agent sees the replacement; None when not
# deprecated; pre-existing projected fields + invalid-key error unchanged.
# Offline (stub state + in-memory processes; the REAL get_process_schema runs).
ananta/src/ananta/services/discovery_service/tests/get_process_schema_deprecation_smoke.py
# Phase 6 §4.6 "per-type KB lifecycles" (B) — the live-refresh path honors process
# deprecation like restart: build_refresh_updates folds apply_deprecation so
# active_retrieval:false DERIVES is_discoverable:false into the refresh updates
# (subsequent discovery rebuild demotes from process_search), default-true stays
# surfaced, no block leaks nothing, malformed fails loud. Offline (pure fn).
plugins/default_knowledge_plugin/tests/refresh_deprecation_honoring_smoke.py

# SoundCloud upload blob-resolution contract (2026-07-02, Where It Rests publish):
# upload_track was impossible to call — resolve_blob_to_path requires the
# blob:// URL form while _resolve_blob_filename's metadata lookup is keyed by
# the bare bmd- id, called with the SAME argument. Fix strips the prefix for
# the metadata lookup only; the smoke guards BOTH directions (red-first
# verified against the pre-fix code). Offline (temp-dir stub with the real
# default_blob_storage_plugin resolution semantics).

# Address-book / vault namespace-collision fix (2026-07-02, Approach B): the
# OAuth client_secret is chain-consumed through the address book, so its vault
# key must live in the RESOLVER's namespace (default_address_book_plugin) — the
# resolver retrieves under its own identity and post-06-07 enforce_namespace
# demands the key's <plugin> segment equal the caller. The owner plugin must
# NOT declare it as an owned required/declared key (the launch-key shape
# validator fatally rejects a foreign-namespace declaration). Both smokes are
# red-first against the pre-fix owner-namespace key + declarations. Offline
# (pure constants + bare-instance declaration methods + rendered error template).

# Singleton drain-until-empty auto-summarizer (2026-07-02): the M6 cron became a
# heartbeat that submits ONE background drainer (BoundedSemaphore(1) singleton
# guard) which loops list_quiescent_sessions → summarize in series until empty.
# Pins: drain-until-empty + next-fire no-op, the attempted-set termination that
# stops a persistent-skip infinite loop, sentinel exclusion, per-session error
# isolation, the singleton guard (second overlapping fire no-ops), and WI-0
# (no summarization on the calling thread — runtime + static). Offline (stub
# repo/writer/inference; real BoundedSummaryExecutor for the threading cases).
ananta/tests/llm/session_ledger/quiescent_drain_smoke.py
# quiescent_read_bounds_smoke (2026-08-15): auto-summarize failed on EVERY
# 10-minute cycle after the read_state row cap deployed — list_quiescent_sessions
# reads the whole eligible session set (27,139 rows live) because the anti-join
# and the 1..50 LIMIT are applied downstream in Python. Pins BOTH bounding
# shapes so neither can be silently dropped: the candidate read carries the
# sanctioned unbounded=True consent, and the summary/source membership reads are
# CHUNKED (bounded by construction, no opt-in), with the caller's other filters
# preserved and the downstream limit still honoured.
ananta/tests/llm/session_ledger/quiescent_read_bounds_smoke.py

# LED-01 event-content embedding producer + search (2026-07-06). Pins: the
# operator scope filter incl. the Codex reasoning-subtype EXCLUSION (red-first:
# neutralizing the subtype check flips it) and worklog INCLUSION; window+overlap
# chunking coverage; deterministic {event_id}:{chunk_index} external_ids with
# chunk-0-first ordering + same-id delete-before-store (idempotent retry,
# red-first proven); the chunk-0 presence discriminator on the newest-first
# paged drain with (event_at, id) row-value cursor; production-shape ANN
# envelope (distance → 1-distance similarity, red-first proven) + __event
# join-back; service-layer clamps + vendor ← session_vendor public envelope +
# fail-closed on missing embedding/vector bindings. Offline (stub state /
# embedding / vector services mirroring the real data.result envelope shapes).
ananta/tests/llm/session_ledger/event_embeddings_smoke.py

# REL-03 swap-window terminated-flow guard (2026-07-02): a failed flow's
# already-queued process_error/process_results siblings, dequeued mid blue-green
# cutover, each re-hit _resolve_io_process_key (empty source_namespace) and
# re-terminate the dead flow — the observed burst. _terminate_flow now tombstones
# the flow_id and _process_action drops any later result/error-PROCESSING sibling
# of a tombstoned flow BEFORE execution (marked failed, NOT re-routed). Pins:
# doomed process_error AND process_results siblings never reach execute_action,
# tombstone-miss executes normally, and a terminal EDGE_SINK delivery of a failed
# flow still executes (scope does not swallow the failure signal) + bounded-FIFO
# cap. Offline (spy action_processor; real _terminate_flow / gate / drop).
ananta/tests/core/substrate_contracts/swap_window_terminated_flow_guard_smoke.py

# SUB-04(b) call-site wiring (2026-07-02): the Phase-0 contract smokes freeze the
# validation LOGIC (deterministic-continuation + pre-dispatch step-contract) by
# calling the validators directly, so a deleted PRODUCTION call site would stay
# green. These AST assertions pin the two seams against call-site removal:
# coordinator._handle_deterministic CALLS validate_deterministic_continuation
# (imported from result_processing.contracts), and inference_transaction._run_pipeline
# CALLS module-level _validate_step_contract. Each seam has a localizing negative
# control. Offline (static ast over the real source via inspect.getfile).
ananta/tests/core/substrate_contracts/call_site_wiring_smoke.py

# DEP-01 Phase-2a authored-by-value conversion (2026-07-03): the Class-A
# artifact-authoring verbs no longer reach the qwen thinking model. Poison
# model/prompt collaborators prove the push path is dead; content-absent
# invocation raises the typed authored_content_required error BEFORE any
# storage side effect (no silent fallback); the downstream section-size
# validation now polices the AUTHORED content. Offline (recording stubs;
# real ArtifactAuthoringService + plugin delegate).
plugins/default_thinking_plugin/tests/authored_by_value_conversion_smoke.py
# DEP-01 Phase-2a fix round (2026-07-03, Rev-C blocker): every projected
# continuation tail — non-joseki authored_register_wbs (phases 2/3), the
# deterministic phase-1 boundary, the joseki tail, and the terminal phase —
# must reference ONLY live thinking_service verbs; a projected step naming a
# de-registered process key (the _append_legacy_wbs_continuation miss) fails
# at dispatch. Blanket assertion over the six DEP-01-retired keys plus
# positive routing pins per tail shape. Offline (pure text projection).
ananta/tests/core/plans/projection_continuation_smoke.py

# ChatGPT-tunnel / streamable-MCP surface (landed 2026-07-04, operator-directed):
# OAuth discovery metadata (static + origin-following dynamic routers,
# oauth_resource_aliases validation), router implicit-session allocation +
# session-header-on-every-response, and the blue-green stable MCP ingress +
# tunnel supervisor (ingress re-resolves router.port per connection; supervisor
# restarts tunnel-client on ingress-port change). All offline; the
# agent_messaging pair needs SOLET_NAME set (vault-scoped constants).
plugins/agent_messaging_plugin/tests/oauth_security_smoke.py
plugins/agent_messaging_plugin/tests/streamable_no_auth_mount_smoke.py
# Task #53 HMAC bearer-token verification (BearerVerifier + _issue_access_token):
# roundtrip, tampered/wrong-key/alg-confusion/expired rejections, constant-time
# compare, forged-sealed-claim rejection, PLUS B1 Q2 (exp scopes the issued_at
# skew — red-first) + client_exists_check + audience-binding rejections. Offline;
# needs SOLET_NAME (vault-scoped constants imported at module load).
plugins/agent_messaging_plugin/tests/hmac_bearer_token_smoke.py
# B1 Finding-B operator-access preservation: a VERIFIED operator_equivalent OAuth
# client keeps operator authority (for_operator_equivalent) after the no-auth
# flip. Pins the propagation chain is_operator_equivalent → trigger-data stamp →
# _build_call_context (red-first on the stamp); default client → for_external;
# stdio bridge unaffected; resolver degrade-safe.
plugins/agent_messaging_plugin/tests/operator_equivalent_propagation_smoke.py
plugins/macos_self_deployment_plugin/tests/blue_green_router/mcp_ingress_smoke.py
plugins/macos_self_deployment_plugin/tests/blue_green_router/tunnel_supervisor_smoke.py

# INF-03 provider-vacancy (2026-07-04): declared-VACANT inference_service is
# a first-class bootable state — constructs without a binding, typed
# 'inference_service_vacant' on provider touch, vertex turns still DEFER
# durably, structural fault edge goes loud-typed (never silent), bound-name
# and env-override behavior unchanged. Offline fakes.
ananta/src/ananta/services/inference_service/tests/provider_vacancy_smoke.py

# INF-03 fix round (Reviewer-A B1, 2026-07-05): boot-INTEGRATION coverage the
# isolation smoke structurally misses — runs the real _create_service_wrappers
# startup step with a vacant inference binding end-to-end (Discovery threshold
# from VACANT_PROVIDER_CONTEXT_CONFIG, ContextService constructed, no inference
# plugin resolution). Offline fakes.
ananta/src/ananta/services/inference_service/tests/vacant_boot_wrappers_smoke.py

# INF-02 autonomic-routed completions (2026-07-05): the durable
# request/response queue behind InferenceService.submit_completion_request —
# session-PRIMARY routing precedence (live sys:autonomic holder → forward;
# vacant → durable DEFER; provider fallback OPT-IN only, never outranking a
# live holder; INF-03 vacancy interplay), predicated-CAS lifecycle (stamp /
# serve-idempotency / requeue / terminal-fail at the attempts cap), real-shape
# state fake schema-enforced for the NEW core__inference_completion_request
# table. Offline fakes.
ananta/src/ananta/services/inference_service/tests/completion_request_smoke.py

# INF-02 agent_messaging half (2026-07-05): the per-type completion drain
# handlers on AutonomicAssignment (claim-time reconcile: forward unassigned +
# holder-death requeue; serve-timeout sweep riding the bridge-lifecycle
# sweeper cadence), the submit_autonomic_completion serve verb (CAS win →
# resume continuation action_def, typed rejections), the forwarder's typed
# inference_completion_request bridge event, and the sweeper on_tick rider
# fault containment. Offline fakes.
plugins/agent_messaging_plugin/tests/autonomic_completion_smoke.py

# INF-02 thinking half (2026-07-05): process_planning_results routes via
# submit_completion_request (awaiting_completion on session/deferred verdicts;
# sync bound-provider path only on the operator-opted provider_fallback) and
# resume_thinking_completion re-enters the loop from the served durable row
# (OUTPUT event + RPK-override actions / finalize; typed rejections for
# unknown/unserved/foreign/incomplete rows; AST pins on the shared
# post-completion path). Offline fakes.
plugins/default_thinking_plugin/tests/planning_completion_resume_smoke.py

# EDGE processor-customizations contract. RELAXED 2026-07-15 (frontier-first
# consolidation): customizations + field_sensitivities are now OPTIONAL on
# EDGE processes — the kb_overlay_loader post-merge both-blocks FATAL and the
# registration-validator customizations/sensitivity FATALs were dropped, so
# edge_customizations_smoke.py (which statically mirrored the deleted boot
# check) retired with them. What remains contract: decorated<->declared
# parity, covered by edge_definition_customizations_smoke.py below, which
# also proves the relax red-first (a no-customizations definition validates).
ananta/tests/core/process_registry/edge_definition_customizations_smoke.py
plugins/default_thinking_plugin/tests/joseki_run_driver_smoke.py

# B3 Half-2 quality_service (GTE-02) — the run_joseki C1 card's gate surface.
# quality_run_gate: run_gate is allowlist-only (unknown / coherence-only name =
# typed rejection — the RED-FIRST security pin), argv is server-fixed (no caller
# flags), output is bounded with an explicit truncation marker (no silent cap),
# and a real fast gate (service_interface_ast) runs end-to-end. quality_list_and_test:
# list_gates maps the REAL gate registry + gate_smokes.txt register, run_test is
# allowlist-only against that register (RED-FIRST), and one registered smoke runs
# through the server-side argv. Offline (real subprocess to the repo venv).
plugins/platform_dev_surface_plugin/tests/quality_run_gate_smoke.py
plugins/platform_dev_surface_plugin/tests/quality_list_and_test_smoke.py

# B3 Half-2 DEPLOY-FIX (2026-07-06 fatality class-pin): repo_root must anchor at
# APP_HOME, not __file__ — the platform runs from a materialized release copy
# (~/.ananta/releases/.../code/) with NO .git but WITH a quality_gates/ snapshot,
# so a __file__ walk fail-louded at green boot (observed) or would silently gate
# the frozen snapshot. Smoke imports the locator from a relocated copy OUTSIDE the
# worktree and asserts it still resolves the WORKTREE via app_home; fail-loud on a
# no-repo app_home (cloud path). Red-first = the __file__-anchored shape.
plugins/platform_dev_surface_plugin/tests/repo_root_release_copy_smoke.py

# B3 Half-1 repo_service (read-only repo primitives) — the 5 §5 security smokes.
# The confinement boundary is the app_home-resolved WORKTREE root which CONTAINS
# profile/ (secrets) + the git dir, so the denylist is LOAD-BEARING (Rev-A ASK-4).
# confinement: traversal/symlink-escape/absolute-outside typed-rejected (root-
# parameterized, release-safe). secret_exclusion: denylist TYPED-reject (not empty)
# + planted-canary read-refuse (Q2) + search-snippet redaction. bounded_output:
# read_file line/byte cap + search cap with explicit truncated marker. readonly_git:
# git_status/git_diff live + STRUCTURAL pin (no mutating git verb reachable; apply
# only with --check). propose_patch_artifact: stores artifact + apply-check flag,
# escaping-path rejected, and NO apply verb exists across interface/public/ops.
plugins/platform_dev_surface_plugin/tests/repo_path_confinement_smoke.py
plugins/platform_dev_surface_plugin/tests/repo_secret_exclusion_smoke.py
plugins/platform_dev_surface_plugin/tests/repo_bounded_output_smoke.py
# repo_service SEARCH FALLBACK (2026-08-08, seed-adoption) — `search` hard-failed
# without ripgrep, which nothing in the adopter install path installs or even
# mentions. It now falls back to the git-native grep, which genesis GUARANTEES
# because it git-inits every born tree, so the fallback cannot itself become the
# undeclared dependency it replaces. Pins: the fallback runs with rg absent; it
# returns the SAME hits as ripgrep for a literal query; it uses PCRE and NOT POSIX
# ERE (measured — ERE agrees with ripgrep on 0% of \d / \w queries, returning WRONG
# hits rather than fewer, so a silent downgrade would trade a loud failure for a
# quiet wrong answer); and a malformed pattern still raises instead of collapsing
# into "no hits".
plugins/platform_dev_surface_plugin/tests/repo_search_fallback_smoke.py
plugins/platform_dev_surface_plugin/tests/repo_readonly_git_smoke.py
plugins/platform_dev_surface_plugin/tests/propose_patch_artifact_smoke.py

# REL-11 get_focused dict-envelope dispatch contract (2026-07-06): the
# service_interface::memory_service::get_focused verb was dead over process_call
# since registration — list[dict] return vs the dispatch contract's dict-only
# rule (action_processor _execute_standard_service_method). Fix = the recall-
# style {"memories": [...], "count": N} envelope across ABC/wrapper/plugin +
# unwraps at every in-process consumer. Pins: registration shape (OBJECT schema,
# memories/count sensitivities), the real plugin envelope wrap, the act-time
# dispatch through the REAL _execute_service_interface_action (red-first: the
# pre-fix list shape raises FrameworkError there), a bare-list negative control,
# and the advancement consumer seam (whose except-guard would silently mask a
# missed unwrap). Offline (stand-in at the plugin's internal backend seam).
ananta/tests/core/substrate_contracts/memory_get_focused_dispatch_contract_smoke.py

# refresh_plugin_process result-contract collision (result-processing lane (a),
# 2026-08-01). The verb's return schema named its top-level field `process_key`
# for the key it had just REFRESHED, while the result contract reserves that
# name for the key of the verb that PRODUCED the result — so
# _check_result_process_key_matches raised RESULT_CONTRACT_VIOLATION on EVERY
# call, after the side-effect had landed and a completed row had been stored.
# Callers saw success; the parent flow token was blocked. Renamed to
# `refreshed_process_key`. Drives the REAL validate_common_success path and
# DERIVES its payload from the code (decorator metadata + ast over the return
# site), so it tracks the verb instead of a hand-typed dict. Mutation table
# M1-M5 in workbench/2026-08-01_refresh_verb_mutation_harness_claude_a.py:
# 5/5 red, incl. M4 which was BLIND until the envelope was bound to
# execute_action's source too.
ananta/tests/core/substrate_contracts/refresh_verb_result_contract_smoke.py

# GTE-06 L2 fresh-source manifest-preflight probe (2026-07-06). S1 harness:
# A5a never-raise + A5b conjunction classification (timeout group-kill, garbage/
# empty-failures/version-mismatch/exit-disagreement envelopes, truncation,
# Popen-failure containment) against REAL subprocesses. S4 orchestrator gate:
# RED probe ⇒ PROBE_FAILED before any spawn; raising seam contained; schema
# preflight short-circuits first; GREEN passes through to spawn. S2 the
# class-ender discriminator: in-process preflight PASSES on a cache-poisoned
# broken edit (the documented limit), the probe subprocess CATCHES it. S5
# release-context fidelity: the verdict follows the PROBED tree, not a
# divergent sibling. S3 core plumbing: probe_failed ⇒ byte-identical manifest
# rollback, A1 CAS-guarded no-stomp on concurrent commit, OSError branch, Q5
# success evidence on the applied envelope. S6 A2 placement: EntryPointMissing
# defers at the L1 call site ONLY (probe + shared function still reject).
plugins/macos_self_deployment_plugin/tests/preflight_probe_harness_smoke.py
plugins/macos_self_deployment_plugin/tests/probe_gate_orchestrator_smoke.py
# §46.1: the F1 root-manifest gate moved out of the outgoing process (where it
# validated against that process's own stale imports) into the L2 probe, under
# the candidate's interpreter. Pins the three-state capability discriminator and
# that the refusal classification (FAILED/root_manifest_drift) is UNCHANGED —
# status is what core routes on, so flattening it would switch manifest rollback
# on for root-manifest refusals as a side effect.
plugins/macos_self_deployment_plugin/tests/root_manifest_probe_gate_smoke.py
ananta/tests/lifecycle_management_service/preflight_probe_cache_poison_smoke.py
ananta/tests/lifecycle_management_service/preflight_probe_release_context_smoke.py
ananta/tests/lifecycle_management_service/apply_manifest_probe_rollback_smoke.py
ananta/tests/lifecycle_management_service/preflight_entry_point_deferral_smoke.py

# JOS-02 session-scoped plan focus (2026-07-07). 13 cases / 37 checks: schema
# column pin vs DEFINITIONS; per-session isolation + cap-20 (F2); the
# LOAD-BEARING resolver keying (completed action's session picks the plan —
# red = `del completed`); advancement + cursor-seam keying; per-card kickoff
# concurrency + same-card typed busy; whole-buffer terminal release (R1);
# vertex-assembly keying; BOTH process_results gate sites threaded (F1) +
# V-5 no-session skip branches; V-2 server-state always-overwrites; migration
# loud-delete idempotence; F3 boot invariant; DCP session preservation (V-8 seam).

# g_suite_plugin Phase 1 reference vertical (OAuth core + Gmail, 2026-07-08).
# smoke_oauth (16 checks): PKCE-S256 consent URL with access_type=offline +
# prompt=consent (the params that force a Google refresh token); refresh stored
# before access; cache hit skips refresh; force-refresh w/o token ->
# gsuite.not_connected; the LOAD-BEARING Google semantic — a plain refresh
# rewrites ONLY the access token and does NOT rotate the durable refresh token
# (red if _refresh_and_persist copies Schwab's rotate-refresh-first); invalid_grant
# -> gsuite.auth_expired. smoke_gmail (15 checks): list row shape + max clamp to
# 100; get_message header/body-decode/attachment-metadata parse; missing id ->
# ValueError; send_message text-only + blob-attachment MIME round-trip through the
# base64url raw. Hermetic (fake vault, patched Google refresh, fake gmail service).
plugins/g_suite_plugin/tests/smoke_oauth.py
plugins/g_suite_plugin/tests/smoke_gmail.py
# smoke_drive (39 checks): the worked BLOB-EXPORT example — list row shape + query
# passthrough + pageSize clamp; download_file drives get-metadata -> get_media bytes
# -> blob writer -> file_blob_key (the *_blob_key whose field_sensitivities entry is
# the FATAL edge_process_mismatch trap the Phase-2 export verbs replicate);
# Google-native docs rejected with a pointer to the export verb (get_media NOT
# called); missing id -> ValueError. Phase 2 additions: upload_file from a blob_key
# (reuses the gmail attachment_loader) or a local absolute path (rejects relative
# paths, rejects neither/both source given); create_folder mime type + parent
# passthrough; share_file permission body shape + invalid-role rejection (no
# notification email sent); the shared export_media_to_blob/resolve_export_mime
# helpers sheets/docs/slides_export all delegate to (non-bytes export rejected,
# case-insensitive format lookup). Hermetic (fake drive service, fake blob writer,
# real temp file for the local-path upload case).
plugins/g_suite_plugin/tests/smoke_drive.py
# smoke_sheets (17 checks, Phase 2, 2026-07-08): create_spreadsheet id + title
# passthrough; get_values grid shape + missing-values-> [] default; update_values
# valueInputOption=USER_ENTERED + range/body passthrough + non-grid values rejected;
# append_values reads updated_cells from the nested `updates` dict Sheets returns for
# append (vs update's top-level updatedCells); export_spreadsheet drives the SAME
# shared drive_actions.export_media_to_blob as docs/slides (Drive's export_media, NOT
# get_media/the sheets service) — default csv mime, xlsx mime override, unsupported
# format + non-bytes content rejected. Hermetic (fake sheets/drive services).
plugins/g_suite_plugin/tests/smoke_sheets.py
# smoke_docs (14 checks, Phase 2, 2026-07-08): create_document title-only vs
# content triggering an insertText batchUpdate at index 1; get_document walks
# body.content[].paragraph.elements[].textRun.content into body_text, SKIPPING
# non-paragraph structural elements (tables) — the regression this pins; missing id
# -> ValueError; batch_update requests passthrough + replies returned + empty
# requests rejected; export_document via the shared export helper — default pdf
# mime, docx mime override, non-bytes content rejected. Hermetic (fake docs/drive
# services).
plugins/g_suite_plugin/tests/smoke_docs.py
# smoke_slides (14 checks, Phase 2, 2026-07-08): create_presentation id + title
# passthrough; get_presentation slide-row shape (object_id + element_count from
# pageElements length, including the empty-pageElements-> 0 case) + count; missing
# id -> ValueError; batch_update requests passthrough + replies returned + empty
# requests rejected; export_presentation via the shared export helper — default pdf
# mime, pptx mime override, non-bytes content rejected. Hermetic (fake
# slides/drive services).
plugins/g_suite_plugin/tests/smoke_slides.py
# smoke_async_jobs (17 checks, sync-verb Phase 1 D0.3 migration, 2026-08-09):
# dispatch (drive_download_file/drive_upload_file/sheets_create_from_files)
# returns {job_id, status:"queued"} WITHOUT ever calling the real
# drive_actions/sheets_actions function; dispatch-time validation fails loud
# BEFORE job creation on missing session_id/flow_id and on no connected
# account; the worker's _process_job runs the real action function against a
# faked Google client and completes via update_job (processing->completed
# with the real result payload, or processing->error with an HttpError
# correctly classified — 404 -> gsuite.not_found); an unregistered verb in a
# job's payload fails loud as gsuite.invalid_params rather than silently
# no-op'ing; start_services/stop_services are idempotent (two starts reuse
# one thread; stop clears the handle). Hermetic (fake AsyncJobManager, fake
# token store, monkeypatched action-module functions — no live Google API,
# no background thread left running past a test).
plugins/g_suite_plugin/tests/smoke_async_jobs.py

# github_midwife_plugin genesis Slice A (2026-07-08) — the macos-free-
# solet profile template + checked-in profile_baseline/ per-plugin
# config templates. Binding-satisfaction (every service_bindings value is
# an allowlisted plugin; inference_service/self_deployment_service
# asserted ABSENT — the declared-vacant/opt-in shape), no orphan
# starting_actions, and public-safe (no /Users/... operator path or
# secret-shaped key anywhere in the profile YAML or any profile_baseline
# JSON; every baseline file is valid JSON). Offline (pure yaml/json parse).
plugins/github_midwife_plugin/tests/macos_free_profile_smoke.py

# github_midwife_plugin genesis Slice B (2026-07-09) — profile-driven
# fail-loud allowlist install. Pins the contract inversion vs
# macos_midwife_plugin.venv_setup.install_target_tree (which swallows
# per-plugin pip failures, warn-and-continue): the FIRST failure raises
# ProfileInstallError immediately and no later package is attempted
# (fail-fast pin: mocked-subprocess call-count stops exactly at the
# failing package). Also covers missing-directory / missing-pyproject.toml
# / missing-venv-python raises, and load_plugin_allowlist parsing
# (incl. loading the real Slice A profile template). Offline
# (subprocess.run mocked throughout; no real pip/network).
plugins/github_midwife_plugin/tests/profile_install_faildloud_smoke.py

# github_midwife_plugin genesis Slice C (2026-07-09, RISKY — flagged for a
# Codex/Rev pass before commit) — first-run scram db_password seed.
# Constructs macos_vault_plugin.keychain.SystemKeychain DIRECTLY (design
# doc §6.5 rider — a post-boot vault_service::store call would deadlock
# against state startup, which is itself waiting on this credential).
# Key-shape verified against the live reader: postgres_state_management_plugin
# resolves vault_service.retrieve(key=f"{solet}.postgres_state_management_plugin.db_password"),
# which macos_vault_plugin's _parse_scoped_key splits into the exact
# (plugin_name, credential) pair this module writes via store_credential —
# no mismatch. Pins: fresh-seed generate+ALTER ROLE+store+round-trip-verify;
# idempotent skip when the role already authenticates with the stored pw;
# rotate-BOTH (never one without the other) when the stored pw is stale;
# inconsistent-Keychain-state and round-trip-mismatch both raise;
# SOLET_NAME required; the generated password is proven absent from
# captured stdout/stderr; both psql-invoking helpers pass the password via
# stdin/PGPASSWORD env, never argv (ps-visible). Needs SOLET_NAME set
# even to import (macos_vault_plugin's package init resolves vault-scoped
# constants eagerly — same class as hmac_bearer_token_smoke.py); the smoke
# self-defaults it to the selected solet name if absent. Offline (FakeKeychain; subprocess.run
# and secrets.token_urlsafe mocked; no real Postgres/Keychain touched).
plugins/github_midwife_plugin/tests/credential_seed_smoke.py

# github_midwife_plugin genesis Slice D (2026-07-09) — no-parent config
# materialization + the 4-step genesis step machine. Coordinator-Dawn
# ratified scope (validate_name -> resolve_target -> materialize_configs
# -> write_manifest_marker, dropping clone_and_copy/knowledge_base_symlinks/
# venv_setup/asset_downloads from the birther.py reference — see steps.py
# module docstring for the full adjudication). Pins: manifest.yaml +
# service_bindings.json + starting_action_definitions.json shape;
# profile_baseline per-plugin configs copied + filtered to the profile's
# plugin allowlist (an out-of-allowlist baseline file is proven excluded);
# plugin_config_overrides merges on top of (not instead of) the baseline;
# missing-profile raises; run_steps executes the 4-step sequence end-to-end
# and stops at the first failure (invalid name / incomplete clone-shape
# each proven to halt before the next step runs, with no side effects
# past the failure point); step_runners is proven genuinely injectable
# (the composability rider) via a custom single-step sequence overriding
# the genesis default. Offline (tmp-dir fixtures; no real Postgres/pip/
# network).

# github_midwife_plugin genesis Slice E (2026-07-09) — bootstrap.py (Layer 0,
# repo-root, STRICTLY stdlib). Pins: an AST scan proves zero non-stdlib
# imports (+ a negative control fabricating a real `import requests` line,
# proving the scan isn't vacuously true); the 7-step sequence
# (homebrew/postgres/pgvector/role_and_db/lm_server/venv_and_seed/handoff)
# dry-runs end-to-end with subprocess/confirm/HTTP all injected fakes — an
# all-healthy fixture reports every step skipped except the terminal
# handoff; a Homebrew-absent fixture stops immediately at step 1
# (needs_user_action, never auto-piping an installer); a forced venv-creation
# failure is caught as a 'failed' step record (not an uncaught exception)
# and halts before handoff. Adaptive host-state per build spec §10.2: each
# probe reports a named enum state (e.g. postgres: absent |
# running_healthy_compatible | running_wrong_version | present_not_running |
# non_homebrew_install), never force-fixes a divergent state. Offline
# (subprocess.run/confirm/http_get all mocked; no real brew/psql/pip/network).
plugins/github_midwife_plugin/tests/bootstrap_stdlib_only_smoke.py

# kb_git managed-branch rename (operator-identity parameterization, 2026-07-11,
# Claude-C) — the local KB-management branch is the operator-identity-free
# 'managed' (renamed from an old fleet-specific default that leaked into a shipped seed). Drives
# git_setup_branch / fetch_and_merge_git against REAL tmp git repos (no-mock, the
# subprocess wrappers ARE the substrate): (1) fresh clone forks 'managed' from the
# default tip, (2) a legacy repo on the old default branch self-migrates — 'managed' forks from
# the old branch TIP (history preserved) and the old branch still EXISTS
# (never-delete-branches invariant), (3) an existing 'managed' is checked out
# idempotently, (4) fetch_and_merge on 'managed' still merges origin/<default>
# cleanly (the rename didn't break the update path). Offline (local tmp repos).
#
# The KB-symlink reconcile ADDITION side. auto_install ran its two
# directions off two sources of truth: removal from the live manifest, addition
# from kb_root.iterdir() (the genesis symlink set), so a plugin added post-birth
# was never a candidate at all — content on disk, no symlink, no index, and
# retrieval then answers CONFIDENTLY from whatever IS indexed. Pins the two
# binding conditions (key on the RESOLVED TARGET not the entry name, so
# default_thinking_plugin's four distinct corpora are not duplicated; and
# manifest=None materialises NOTHING, fail closed) plus an EXPLICIT idempotence
# test — the sweep runs every boot against a tracked git checkout, so a
# non-idempotent pass means unattributable working-tree churn.
plugins/default_knowledge_plugin/tests/kb_symlink_reconcile_smoke.py
#
plugins/default_knowledge_plugin/tests/kb_git_smoke.py

# github_midwife_plugin genesis Slice F (2026-07-09, RISKY — flagged for a
# Codex/Rev pass before commit) — the simple LaunchAgent plist renderer.
# Own-copy fork of macos_self_deployment_plugin/autostart_manager.py's
# simple form (drops the supervisor/current-release/PathState machinery —
# genesis has no blue-green rotation). Pins every build-spec §6.2
# load-bearing plist field: Label=local.solet.<name>;
# EnvironmentVariables sets SOLET_NAME (boot fast-fails without it);
# WorkingDirectory is home_dir/.ananta/runtime/<name> — deliberately NOT
# ananta.core.runtime.get_runtime_dir() (verified 2026-07-09: that helper
# returns one SHARED ~/.ananta/runtime dir with no per-name segment despite
# accepting a solet_name param); ProgramArguments launches ananta.cli
# directly (no supervisor) via the clone's own absolute .venv/bin/python3 +
# absolute --app-home profile dir (never a bare "profile" string, which
# would resolve against the runtime dir instead); RunAtLoad=true;
# KeepAlive is the SIMPLE crash-restart form ({SuccessfulExit: false}), not
# an unconditional <true/>. Also pins install/uninstall/status idempotency
# (unload-before-reload on a stale plist, absent-uninstall is a no-op
# success, a launchctl failure raises AutostartError). Offline (tmpfs
# plist_dir + home_dir; launchctl fully mocked via an injected run callable
# — no real /bin/launchctl, filesystem writes outside tmp, or
# ~/Library/LaunchAgents touched).
#
# Codex F must-fix (2026-07-09, folded post-review): the launchctl-list
# PROBE no longer coerces "execution failed" into "not loaded" — only the
# documented not-found signal (stderr contains "Could not find service")
# maps to False; an execution failure (OSError/timeout) or any other
# unrecognized nonzero-exit shape raises LaunchctlObservationError instead
# of guessing. Pins: (a) status() raises/surfaces the failure rather than
# reporting a fabricated not_installed; (b) uninstall() REFUSES to act on
# an unobservable state — no unload attempt, plist left in place, so a
# transient list failure can never orphan a live launchd job with no
# backing plist; (c) the discrimination boundary itself — an unrecognized
# nonzero exit is not silently re-widened back into "not loaded".
plugins/github_midwife_plugin/tests/autostart_render_smoke.py

# github_midwife_plugin genesis Slice G (2026-07-09) — plugin.py (dual-use
# birth_solet EDGE verb) + genesis.py (Layer 1 entrypoint composing
# B/C/D/F). edge_parity_smoke: entry-point resolution, EDGE-parity (the
# create-process skill's FATAL trap — every EDGE @platform_process needs a
# matching EdgeProcessDefinition with field_sensitivities), return_value_schema
# matches the ACTUAL _birth_result_to_dict keys exactly (no drift), genesis.py
# entrypoints importable, KB process JSON exists + process_key matches the
# runtime registration. genesis_integration_smoke (build spec §8): drives
# run_genesis() end-to-end against a tmp clone + FakeKeychain + tmpfs
# plist_dir/home_dir + fake launchctl — asserts the materialized profile
# config, the attempt marker, the seeded credential, and the rendered plist
# are all coherent, AND that the generated password never appears anywhere
# in captured stdout/stderr across the whole run; plus a resolve_target
# failure surfaces as a named GenesisError. Offline (subprocess.run globally
# mocked for pip; every credential/autostart dependency explicitly injected;
# no real Postgres/Keychain/LaunchAgents/network).
plugins/github_midwife_plugin/tests/edge_parity_smoke.py
plugins/github_midwife_plugin/tests/genesis_integration_smoke.py

# github_midwife_plugin Workstream A (2026-07-20, Claude-B) — git-init of the
# born solet worktree at genesis. A seed never ships `.git` (the "no
# contaminated history travels" invariant), so a seed-born tree is a plain source
# dir and `platform_dev_surface_plugin` readiness (`git rev-parse
# --is-inside-work-tree`) fail-louds. The new git_init post-spine phase gives the
# born tree a FRESH EMPTY history (its own, never the source tree's) + a `.gitignore` + a
# LOCAL identity + one initial commit. Drives git_init_worktree with REAL git on
# a throwaway tmp tree: the readiness probe returns true, exactly one commit on
# `main`, gitignored runtime/secret/venv paths stay OUT while source is tracked,
# and idempotent re-run skips (never re-commits, never clobbers the .gitignore).
# Offline (tmp tree outside the fleet worktree; real git subprocesses).
plugins/github_midwife_plugin/tests/git_init_smoke.py

# kb_symlinks materialize step (2026-07-12, Claude-C) — the genesis step that
# DERIVES + idempotently repairs knowledge_bases/ discovery symlinks for a
# seed-born clone (the MINT assemble bans symlinks, so the seed ships none →
# dead KB). Derive-don't-declare: each plugins/<x>/knowledge_base*/ +
# ananta/knowledge_bases/* + ananta/knowledge_base with a manifest.yaml →
# knowledge_bases/<its-own-manifest-name> as a RELATIVE symlink. The FIRST check
# is the data-loss safety wall (a real content dir at a target name survives
# untouched — the MINT path never triggers this branch, so it MUST be tested on
# its own fixture); plus the 4 repair branches (absent/skip/rewrite/protect),
# dangling-symlink repair, the manifest-name≠dir-name discriminator (thinking
# trio: thinking_plans/authored_joseki/plan_templates), idempotent no-op, and the
# pruned-MINT-tree E2E. Offline (tmpfs fixtures only).
plugins/github_midwife_plugin/tests/kb_symlinks_smoke.py

# root_manifest seed step (2026-07-12, Coordinator-Dawn) — the genesis spine
# step that rewrites a seed-born clone's root_manifest.yaml solet_name:
# line from the MINTING solet's name to the newborn's (the seed ships the
# minting tree's file verbatim; without this step every seed-born newborn
# carries the minter's identity — the exact pollution the seed factory exists
# to prevent). Pins: byte-exact single-line rewrite (regex must not inject a
# blank line — the macOS original's \s*+appended-\n shape did, fixed in both
# own-copies), idempotency, fail-loud missing-file / missing-line refusals
# with the refused file left untouched, and first-line-only semantics.
# Offline (tmpfs fixtures only).
plugins/github_midwife_plugin/tests/root_manifest_seed_smoke.py

# github_midwife_plugin genesis venv-provision + newborn self-seed
# (venv_provision.py; acquisition mode retired 2026-07-18). Pins: birth
# requires an EXISTING clone (_require_existing_clone — absent/empty is
# refused, acquisition retired; a fully-formed clone passes; an
# occupied-but-invalid dir raises rather than guessing) and the verb-level
# retire (birth against an absent target fails loud); the §7 provision_venv
# birth variant (True → create_venv_and_install_seed EXPLICIT + UNCONDITIONAL
# before genesis; False → never called, standard mode skips venv). Pins the
# per-solet credential self-seed: the newborn subprocess env sets
# SOLET_NAME to the NEWBORN's name, nothing is piped on stdin (no
# cross-namespace credential copy), a self-seed subprocess failure surfaces
# its (secret-free) diagnostic, and the pre-seed scram VERIFY runs before the
# self-seed against the parent sibling db (derived from the running
# SOLET_NAME, not hardcoded). Offline (subprocess.run fully mocked; no
# real git/pip/network/Keychain).
plugins/github_midwife_plugin/tests/venv_provision_smoke.py

# github_midwife_plugin name-validation regression (2026-07-19) — the shared
# constants.is_valid_solet_name (fullmatch, so a $-before-newline name
# cannot slip through) and the FOUR name-derivation boundaries it now guards:
# steps.validate_name, credential_seed + venv_provision (Layer 1), and
# bootstrap.py (Layer 0, validated at import before any admin psql/REVOKE/
# createuser call). Closes the unvalidated-name -> SQL/libpq-conninfo injection
# class (Codex BLOCKER + Reviewer-A F1/F2/F3). Offline (pure predicates + a fresh
# in-process bootstrap load; no Postgres/Keychain).
plugins/github_midwife_plugin/tests/name_validation_smoke.py

# github_midwife_plugin genesis human-intake fixes (2026-07-11, Claude-C) —
# closes the three pre-start provisioning gaps verb-mode genesis structurally
# skipped (overnight runbook §11): F8 (a stock py3.13 venv lacks setuptools →
# --no-build-isolation editable installs fail BackendUnavailable; both venv
# seams now pre-install pip/setuptools/wheel — covered by venv_provision_smoke +
# profile_install_faildloud_smoke), F9 (the newborn's mandatory openai_
# embeddings address-book entry is now materialized to entries.json from the
# profile template so the address-book plugin auto-seeds it at boot — covered
# by config_materialize_smoke incl. a seed_loader parity assertion), and F10
# (the vault passphrase file is seeded write-if-absent, mode 0600, never
# regenerated/leaked). vault_passphrase_seed_smoke pins the module in
# isolation (write-if-absent, never-clobber operator/prior files, 0600,
# no-leak); the genesis_integration_smoke additions pin the wiring (the
# passphrase file is materialized during genesis and its value never leaks to
# stdout/stderr or the return). Offline (no vault/Keychain/network).
plugins/github_midwife_plugin/tests/vault_passphrase_seed_smoke.py

# github_midwife_plugin genesis SEED-06 router-at-birth (2026-07-18,
# Coordinator-Dawn) — genesis installs the per-solet blue-green router
# right after the main autostart LaunchAgent, so a seed-born solet comes
# up blue-green-capable with zero operator action (design §4 D1, Q1 RULED).
# router_install_smoke pins the helper's branches in isolation: a free-tier
# newborn (no macos_self_deployment_plugin in the profile allowlist) SKIPS and
# never invokes the installer; a blue-green-capable newborn invokes the shipped
# installer via its OWN venv python with argv [venv python, install_router.py,
# name]; and every fail-loud path (missing installer, missing venv, non-zero
# exit, unrunnable) raises RouterInstallError so a birth never proceeds
# believing in a router that is not there. The end-to-end wiring (skip branch +
# install branch, both faked) is pinned by genesis_integration_smoke, and the
# real router *.template assets surviving the real seed_manifest prune is pinned
# by assemble_smoke (§6#2). Offline (fake Runner; no launchctl/port/subprocess).
plugins/github_midwife_plugin/tests/router_install_smoke.py

# github_midwife_plugin no-MCP-first command launcher (2026-07-21,
# Coordinator-Dawn) — genesis installs `<bin_dir>/<name>` on the operator's
# PATH as a bare symlink to the newborn venv's generic `solet` console
# script (agent_messaging_plugin local_cli); identity resolves by install
# location, so the command pins its own solet and reaches no sibling.
# command_launcher_smoke pins the helper contract in isolation (install /
# idempotent re-run / stale-symlink repoint / non-symlink fail-loud refusal /
# missing-console-script refusal / invalid-name refusal); the end-to-end wiring
# (phase record + marker + sandboxed bin_dir symlink) is pinned by
# genesis_integration_smoke. Offline (tmpfs dirs only).
plugins/github_midwife_plugin/tests/command_launcher_smoke.py

# github_midwife_plugin coordination-hooks SECURITY-CLAIM suite (2026-07-29,
# Coordinator-Day; git_controller_gate_smoke.py inverted in 2026-07-30,
# Git-Controller) — the plugin goes to corporate security review as a
# standalone directory, and SECURITY.md is the document that IS that review. It
# makes ~10 falsifiable claims and, before this suite, nothing verified any of
# them: the word "test" did not appear in it. The drift is not hypothetical —
# adding a fifth hook falsified its "fixed string literal" claim and its hook
# counts (fixed by hand in 92f41b774), and plugin.json's description had
# silently omitted wake_waiter.js before that.
#
# UNLIKE every other entry here, these four live INSIDE the plugin
# (claude_plugin/coordination-hooks/tests/) rather than in the plugin's tests/
# dir, because the reviewed artifact must carry its own evidence: `cd
# coordination-hooks && python3 tests/run_all.py` is verified green on macOS's
# own python3 3.9.6, under `env -i`, outside any git repo, creating no file
# inside the artifact. Nothing in them resolves a path outside the plugin, and
# manifest_consistency_smoke.py asserts that so it cannot regress. `**/tests/`
# is a categorical seed prune, so seeds still ship the plugin without them —
# reviewed is a superset of shipped, which is the safe direction.
#
# No pytest and no npm: a test framework inside the artifact would contradict
# the stdlib-only supply-chain claim these very tests defend. The harness is
# stdlib Python driving the Node hooks as subprocesses — the same contract
# Claude Code uses, and the only contract the Node hooks have (they export
# nothing). Child environments are built explicitly, never inherited, so a
# "disarmed" case cannot silently run against a hook the launcher already armed.
#
# Every check was mutation-verified: 17 deliberate defects (relayed child
# output, propagated nonzero exit, raw-label concatenation, inline shell in the
# manifest, added network import, second subprocess, third-party require,
# count drift, undocumented sixth hook, ...) each turn the relevant smoke red.
# That pass found a real hole — capturing the child's streams via `stdio: pipe`
# falsifies "dropped unread" while leaking nothing observable, so the "unread"
# claim needed a source-level check, not a black-box one. Offline (tmpfs only).
plugins/github_midwife_plugin/claude_plugin/coordination-hooks/tests/manifest_consistency_smoke.py
plugins/github_midwife_plugin/claude_plugin/coordination-hooks/tests/reminder_hooks_smoke.py
plugins/github_midwife_plugin/claude_plugin/coordination-hooks/tests/wake_waiter_smoke.py
# WS-3c — the operator-guidance channel, the plugin's ONLY verbatim-text
# surface. Registered separately from reminder_hooks_smoke.py because its
# arming contract differs (a label alone does not arm it; the event's own
# guidance variable must also be set), so folding it into the reminder suite
# would have made that suite's "armed" fixtures wrong. Asserts default-off,
# byte-exact round trip behind the provenance prefix, refuse-whole over the
# 4,000-char cap without echoing the refused value, and no cross-event leak
# between the two variables.

# github_midwife_plugin coordination-hooks git-mutation guard — the INVERSION
# (2026-07-30, Git-Controller) — this is the sole hook with authority to block
# a tool call; the other four only emit advisory text. Before this entry the
# artifact under corporate review shipped 296 checks over the harmless hooks
# and zero over the one that can refuse: the 143-case suite existed but lived
# in `.claude/hooks/tests/`, which is absent from every seed and from a
# reviewer handed just this plugin directory. This is the FLIP of the prior
# `coordination_hooks_gate_smoke.py` driver (deleted): the 143 cases are now
# single-sourced HERE, inside the plugin, tested against the shipped copy by
# default; `.claude/hooks/tests/git_controller_gate_smoke.py` is now the
# driver, re-invoking this file with GATE_SMOKE_HOOK_DIR pointed at the local
# `.claude/hooks/` twin so the two copies can never drift in what is asserted
# about them. Same two per-copy bindings as before, direction reversed
# (`GIT_CONTROLLER_NAME`/"controller" default here now; `SOLET_GIT_CONTROLLER_NAME`/
# "Coordinator" is the driver's override) — both gate env names are still
# scrubbed from the child env in the driver so an inherited value cannot let a
# gate-OFF case run against a live gate and still report PASS. Both copies
# re-verified 143/143 after the flip; negative controls (wrong env literal,
# wrong authority noun, missing driver target) each fail as designed. Cited in
# SECURITY.md's Verification table and in tests/README.md. Offline (tmpfs
# dirs only, no repository, no network).
plugins/github_midwife_plugin/claude_plugin/coordination-hooks/tests/git_controller_gate_smoke.py

# Fleet session-management Phase B, §6 permission-mode design (2026-08-03) —
# .claude/hooks/headless_tool_allowlist_gate.py, a PreToolUse mechanism for a
# headless worker's spawn-time tool allowlist (this hook, not the CLI's
# --allowedTools flag, is the actual enforcement point when armed —
# --allowedTools was empirically verified additive-only, never restricting,
# even with --setting-sources project excluding both this operator's
# user-scope bypassPermissions default and this checkout's local-scope broad
# Bash allow list). UNLIKE git_controller_gate.py's disclosed mistake-
# prevention/allow-on-error scope: were this armed, it would be the safety
# boundary for an unattended worker with no human to catch a bug, so it is
# FAIL-CLOSED — a malformed stdin payload also BLOCKS (exit 2), never
# silently allows. Opt-in via FLEET_HEADLESS_TOOL_ALLOWLIST being SET (even
# empty — C3 explicit-blank-vs-unset). Operator ruling, same day, effective
# now ("we don't have any restrictions now"): headless_adapter.py's
# _spawn_env only sets that env var when an allowlist is actually configured
# non-empty, so every headless spawn today runs UNARMED/ungated by default —
# this smoke still covers the mechanism itself (which stays landed as shelf
# capability, armed per-work_class whenever usage data argues for it), not a
# currently-enforced restriction.
# Not yet part of the dual shipped/local-copy mirror git_controller_gate.py
# uses (tracked follow-up, not blocking tonight's e2e scope) — single copy
# only, this repo's own headless-spawning mechanism.

# T1 usage-capture lane (2026-08-05) — .claude/hooks/capture_session_mapping.py,
# the SessionStart hook that self-declares the agent_instance_id <->
# claude_session_id mapping to a file-per-firing spool
# (ANANTA_SESSION_MAPPING_SPOOL_DIR, declared by both host adapters, never
# derived here). Non-fatal by design (ruling Q1(e)): missing env vars, no
# session_id in the payload, malformed stdin JSON, and an unwritable spool
# path all exit 0 with a stderr warning, never disrupting the worker's
# session start. Covers the happy path (one correctly-shaped, correctly-
# named spool file per firing), the capture_source composite token
# (hook:<source>, falling back to hook:unknown when source is absent), and
# every non-fatal failure leg.

# T2 (fleet-token-efficiency lane, 2026-08-05) — .claude/hooks/heartbeat_report_alive.py,
# the PostToolUse heartbeat that replaced a REJECTED detached-shell-loop design
# (seat's redesign ruling, after pid-32482 was measured live surviving a fleet
# /clear and stamping an effectively-dead session's row for ~10 hours). This
# hook has NO persistent process of its own -- it only runs synchronously as
# part of an actual tool-call lifecycle, so it dies with the context it
# vouches for BY CONSTRUCTION. Throttled to at most once per _THROTTLE_SECONDS
# via a per-worker local marker file's mtime. Covers: missing env vars are
# non-fatal; a fresh marker throttles the very next firing; a stale marker
# allows another call; a FAILED report_alive call never touches the marker
# (so the next firing retries, never silently suppressed); many rapid firings
# under one identity still throttle correctly throughout (e.g. across a
# /clear -- the worker is genuinely still alive, continued heartbeating is
# correct there); and the RED-FIRST structural false-liveness guard --
# inspects the hook's own source for ANY backgrounding primitive (Popen,
# os.fork, nohup, disown, start_new_session) and asserts none exist.

# WS-4b.1 — stock-Codex Git-Controller adapter. The runner-neutral lexer,
# walker, and decision policy have one canonical source and byte-identical
# materializations in the independently cached Claude and Codex plugins. Codex
# routes only the live-measured Bash payload in this unit and authorizes only
# from launcher-owned AGENT_ROLE: a controller-looking display label, session
# UUID, or hook thread id cannot grant the role. Hermetic subprocess fixtures
# pin read-only allow, mutation block, controller allow, missing-role block,
# wrapped mutation detection, malformed-input behavior, and the explicit
# non-routing boundary for unmeasured edit/delegation tools.
plugins/github_midwife_plugin/codex_plugin/coordination-hooks/tests/git_controller_gate_smoke.py

# A5 / D-5a.3 — four-copy consistency of the git gate's policy text and its
# single-active-session exemption clause. Symbols:
# case_clause_section_byte_equal_across_four_copies,
# case_clause_is_actually_carried_by_the_shipped_message,
# case_clause_names_no_mechanism_the_gate_lacks,
# case_shipped_policy_copies_are_byte_identical,
# case_env_var_divergence_is_preserved_not_unified,
# case_policy_module_adds_no_platform_call_or_network.
# Lives at plugin-repo scope, not inside either plugin's tests/, because it is
# the only leg reaching the origin-repo-only .claude/hooks/ copy and the
# shipped suites must stay standalone (their manifest_consistency_smoke.py
# enforces that). Proves CONSISTENCY only; the matching EFFICACY leg — that a
# blocked session actually receives the text — is asserted on the real block
# path by the two gate smokes registered directly above. Mutation table run
# 2026-08-02: clause drift in a shipped copy, clause drift in the origin copy
# alone, clause defined but not concatenated, a forbidden "the gate detects"
# claim, and env-var divergence unified by deletion each red; restore green.
# Offline (no repository, no network, no subprocess).
plugins/github_midwife_plugin/tests/git_policy_clause_consistency_smoke.py

# WS-4b.2 — stock-Codex fixed coordination reminders and repo marketplace.
# Hermetic checks pin the conservative 0.141 manifest inventory, fixed generic
# context with no prompt/message/label relay, session-label opt-in, reviewed
# entry-point/documentation consistency, and resolution of the repo marketplace
# to these exact plugin bytes. Live Codex trust remains a separate acceptance
# leg because an offline smoke cannot prove host trust state.
plugins/github_midwife_plugin/codex_plugin/coordination-hooks/tests/reminder_hooks_smoke.py
plugins/github_midwife_plugin/codex_plugin/coordination-hooks/tests/manifest_consistency_smoke.py
plugins/github_midwife_plugin/codex_plugin/coordination-hooks/tests/marketplace_consistency_smoke.py
# codex coordination-hooks PREFLIGHT SKIP CONTRACT (2026-08-08, seed-adoption) —
# the Codex hooks are an opt-in capability whose Node runtime nothing in the
# adopter install path provides, so preflight now reports a DISCLOSED SKIP (77)
# when node is absent instead of hard-failing every adopter's gate run. This
# pins the contract across all three combinations, and the third is the point:
# node absent + a BROKEN harness must still fail LOUDLY, never be masked by the
# skip. Deliberately does not call preflight() itself, so it runs on exactly the
# node-less machines it exists to protect.
plugins/github_midwife_plugin/codex_plugin/coordination-hooks/tests/preflight_skip_contract_smoke.py

# jira_plugin (Lane C of the Enterprise Connector Suite, 2026-07-09, Claude-C) —
# hermetic connector smokes (faked address_book_service, faked JIRA client +
# JIRAError, in-memory ProcessExportPolicy; no network/credentials). Each smoke
# carries paired positive+negative red-first controls.
#
# smoke_config.py (JIR-A): config resolution (jira_site entries -> JiraAppConfig;
# missing/incomplete/malformed-expires_at fail loud AT LOAD; naive expires_at
# coerced to aware UTC), client construction (basic_auth (email,token) + pinned
# rest_api_version=2), token-expiry awareness (check_token_expiry: within-N-days
# -> typed jira.token_expiring warning / well-future -> None / expired ->
# negative days; message carries NO site host), and process-export
# deny-effectiveness (live-mirrored allow=('plugin::*::*',) + the plugin's deny
# constant -> is_allowed False for all 12 verbs; drop the deny -> True red
# baseline; deny-only is vacuous, proving the allow is load-bearing).
plugins/jira_plugin/tests/smoke_config.py
# smoke_issues.py (JIR-B): jql_search trimmed rows + 100-cap + assignee-None
# guard + over-byte-cap export; get_issue .raw parse; create/update/delete
# (issue().delete()); JIRAError classification (401/403/429 generic, 404/400
# detail from exc.text); TOPOLOGY-LEAK red-first (site host in JIRAError.url is
# absent from the classified message on BOTH generic AND detail paths, and from
# the _run broad-except path); _run error routing.
plugins/jira_plugin/tests/smoke_issues.py
# smoke_comments.py (JIR-C): add_comment id; list_comments .raw parse + cap;
# list_transitions list-of-dict -> {id,name,to_status}; transition_issue invokes
# the transition + re-fetches new_status; None-safe author flattening.
plugins/jira_plugin/tests/smoke_comments.py
# smoke_attachments.py (JIR-D): download_attachment attachment(id).get() ->
# blob writer -> attachment_blob_key + meta (non-bytes rejected); add_attachment
# blob-only (loader bytes -> BytesIO upload, filename override); BLOB-ONLY
# red-first (the verb schema exposes NO path/file param, only blob_key; a
# path-only call is refused and the loader — the only byte source — never fires,
# so no local file is read).
plugins/jira_plugin/tests/smoke_attachments.py

# jira_plugin — D0.3 deferred-completion migration (sync-verb Phase 1 wave 2,
# lane W2a, 2026-08-09/10): all 10 blocking-suspected/resolved-cannot-determine
# verbs (jql_search/get_issue/create_issue/update_issue/delete_issue/add_comment/
# list_comments/list_transitions/transition_issue/test_connection) moved off the
# dispatch path onto the AsyncJobManager job-ledger shape traced in
# comfyui_image_generation_plugin and mirrored from external_postgres_plugin's
# PluginBase lazy-worker shape
# (workbench/2026-08-09_sync_verb_d03_deferred_completion_doctrine_syncverb-doctrine.md;
# D0.2 appendix rows workbench/2026-08-08_sync_verb_d02_blocking_inventory_syncverb-inventory.md:623-634).
# NOT migrated: add_attachment/download_attachment (D0.2-classified 'clean',
# out of this lane's assigned scope). HERMETIC, 69 assertions: a fake
# AsyncJobManager + plugin._require_client monkeypatched to a fake jira.JIRA
# client prove, across all 10 verbs, (1) dispatch returns {job_id, status:
# queued} without ever building a client, (2) dispatch-time session_id/flow_id
# validation fails loud BEFORE a job is created, (3) the background worker's
# _process_job runs the real issue_actions/comment_actions function (and
# async_jobs.py's own _test_connection, moved there from plugin.py as its only
# remaining caller) and completes the job with the correct result/
# topology-safe-error shape on representative read (jql_search), write
# (create_issue), and two-call (transition_issue) verbs, decoupled from the
# dispatch call, (4) ACTION_HANDLERS carries exactly the 10 migrated verb
# names -- a typo would silently strand that verb's queued jobs forever, (5)
# ensure_worker_started is idempotent (ONE worker thread, never more —
# FlowManager._sequence_cache's unlocked check-then-increment is unsafe for
# concurrent completions into the same flow). Limits-only posture (2026-08-02/
# 08-03 rulings) is unchanged: jql_search/list_comments still never export to
# a file — the completed job's own result payload carries the trimmed rows
# inline, just delivered via the completion continuation instead of the same
# call's own return value.
plugins/jira_plugin/tests/smoke_async_jobs.py

# external_postgres_plugin — Wave-1 Lane A, the "super Datagrip" over FOREIGN
# Postgres DBs (EPG-A..E, 2026-07-09; write surface added 2026-08-09/10, operator
# ruling + Amendment 1). FIVE HERMETIC smokes cover the full logic without a
# database: smoke_config (per-connection resolve + JDBC-URL parse +
# password-never-logged repr/scrub/error), smoke_containment (§8.4 platform-instance
# guard, role-independent host+port+dbname, red-first incl. a fixture superuser case
# + /-socket + blank-port), smoke_guard (read-leader +
# sqlparse single-statement belt — READ verbs only; run_statement never calls
# assert_read_statement), smoke_query (read verbs + run_statement's no-RETURNING/
# RETURNING/rollback/single-statement-only-guard shape + bound-param introspection
# + TOPOLOGY-LEAK red-first at both classify + plugin level + EDGE-parity via the real
# validate_edge_process_provider, now 8 verbs), smoke_data_export (run_query fail-loud overflow
# red-firsts — over-cap raises result_too_large naming export_query, A4 no-export —
# + export_query workspace-TSV write via the REAL containment gate: outside-root/
# empty-roots/misconfigured-root/relative-path/suffix red-firsts + truncated flag).
# (smoke_export_deny retired 2026-07-15 — process_export_deny_patterns emptied by
# operator ruling: the deny layer was friction, not security, on a single-user
# substrate. See workbench/2026-07-15_result_error_processing_architecture_deep_dive.md.)
plugins/external_postgres_plugin/tests/smoke_config.py
plugins/external_postgres_plugin/tests/smoke_containment.py
plugins/external_postgres_plugin/tests/smoke_guard.py
plugins/external_postgres_plugin/tests/smoke_query.py
plugins/external_postgres_plugin/tests/smoke_data_export.py

# external_postgres_plugin — D0.3 deferred-completion migration (sync-verb Phase 1
# wave 1, lane W1, 2026-08-09): list_schemas/list_tables/describe_table/test_connection/
# run_query/export_query moved off the dispatch path onto the AsyncJobManager
# job-ledger shape traced in comfyui_image_generation_plugin
# (workbench/2026-08-09_sync_verb_d03_deferred_completion_doctrine_syncverb-doctrine.md).
# HERMETIC: a fake AsyncJobManager + fake psycopg connection prove (1) dispatch
# returns {job_id, status: queued} without ever calling connection.connect,
# (2) dispatch-time session_id/flow_id + connection_name validation fails loud
# BEFORE a job is created, (3) the background worker's _process_job runs the real
# query_actions function (both no-path-gate and export-path-gate verbs — run_query's
# worker test proves the TSV actually gets written via plugin._export_path_gate) and
# completes the job with the correct result/topology-safe error shape, decoupled from
# the dispatch call — this superseded smoke_query.py's old plugin-level dispatch/
# driver-fault test, which asserted a scenario the async shape makes impossible,
# (4) ensure_worker_started is idempotent (ONE worker thread, never more —
# FlowManager._sequence_cache's unlocked check-then-increment is unsafe for
# concurrent completions into the same flow).
plugins/external_postgres_plugin/tests/smoke_async_jobs.py

# external_postgres_plugin LIVE read-only proof (the load-bearing 25006 proof a
# MagicMock cannot give). GATE-SAFE: both self-skip (exit 0) when the local scratch
# Postgres fixture (epg_smoke_scratch on the Homebrew cluster — NEVER a production DB) is
# unreachable, and run for real (createdb+seed via psql, connect as the local
# trust-auth superuser — the OS user running the smoke) where it is. smoke_readonly proves conn.read_only + SHOW
# transaction_read_only=on in the SAME txn as the user query + 25006 on every write
# incl. first-statement/no-window; smoke_multistatement proves the parser-bypass
# defense-in-depth (a 2-stmt write still 25006). See the completion report's flag on
# whether these live-DB smokes stay registered (GTE-09 lesson).
plugins/external_postgres_plugin/tests/smoke_readonly.py
plugins/external_postgres_plugin/tests/smoke_multistatement.py

# external_postgres_plugin LIVE write-capability proof (operator ruling 2026-08-09
# + Amendment 1, "vendor RBAC is the control plane" — the write reversal). Same
# GATE-SAFE skip/run convention and scratch fixture as smoke_readonly.py above.
# Proves conn.read_only is False on a read_only=False connection; run_statement
# actually commits (visible from a SEPARATE fresh connection, not just an open
# transaction this process still holds) both for a no-RETURNING write (rowcount
# inline) and a RETURNING write (real driver result set -> real TSV); the DEFAULT
# read_only=True connection on the SAME scratch DB in the SAME run still refuses a
# write (the reversal is verb-scoped, not a global posture flip); the single-
# statement SHAPE guard still refuses a two-statement string, but a DELETE is NOT
# refused (the read-leader ACCESS-CONTROL guard genuinely never runs on this path,
# proven live, not just asserted against a fake connection).
plugins/external_postgres_plugin/tests/smoke_write.py

# snowflake_plugin — Wave-2 Lane A, the read-only Snowflake warehouse connector
# (SNW-A..E, 2026-07-13, built from the ratified-but-never-built umbrella design's
# wave-2 slot; D0.3 async containment added 2026-08-10, lane W2c). FIVE hermetic
# smokes: smoke_config (snowflake_account resolve incl. a REAL RSA-2048 PEM
# round-trip through the resolver + eager fail-loud parse of a flattened key +
# repr redaction + statement/login timeout bounds), smoke_guard (the read-leader
# belt — FAST-FAIL ONLY, no session-level read-only flag exists for Snowflake
# unlike Postgres, so the guard cannot itself stop a write; the true boundary is
# the operator-granted read-only role, documented not exercised here), smoke_query
# (read verbs + TOPOLOGY-LEAK red-first at classify level + EDGE-parity; the
# plugin-level dispatch->driver-fault integration now runs entirely inside the
# D0.3 background worker, covered by smoke_async_jobs.py below, not duplicated
# here), smoke_data_export (run_query fail-loud overflow red-first + export_query
# workspace-TSV write via the REAL containment gate, same red-first family as the
# external_postgres note above + truncated flag). (smoke_export_deny retired
# 2026-07-15 — see external_postgres note above.)
plugins/snowflake_plugin/tests/smoke_config.py
plugins/snowflake_plugin/tests/smoke_guard.py
plugins/snowflake_plugin/tests/smoke_query.py
plugins/snowflake_plugin/tests/smoke_data_export.py

# snowflake_plugin D0.3 deferred-completion smoke (2026-08-10, lane W2c). Hermetic
# — a fake AsyncJobManager + a fake Snowflake connection, no live warehouse, no
# real background thread left running past a test. Proves dispatch returns
# {job_id, status: queued} in milliseconds WITHOUT ever opening a connection;
# dispatch-time session_id/flow_id validation fails loud before any job is
# created (no connection_name check exists here — unlike external_postgres_plugin
# this plugin is single-account v1); the worker's _process_job runs the real
# query_actions function against a fake connection and completes the job via
# update_job with the correct result shape on success and a topology-safe
# (code, message) on a driver fault, covering both the no-path-gate verbs
# (list_databases/test_connection) and the path-gate verb (run_query — proves
# the worker actually writes the TSV file and wires plugin._export_path_gate);
# ensure_worker_started is idempotent (two calls, one thread).
plugins/snowflake_plugin/tests/smoke_async_jobs.py

# salesforce_plugin — Wave-2 Lane C, the full-CRUD-including-delete Salesforce
# connector (SFC-A..D, 2026-07-13, same provenance as snowflake_plugin above),
# full CLI delegation since 2026-07-14 (see client.py's module docstring — the
# session-borrow client factory these smokes originally targeted is gone).
# THREE hermetic smokes exercising the pure action functions (unaffected by the
# D0.3 migration below — only the dispatch layer above them changed): smoke_config
# (salesforce_org resolve, sf CLI org-binding verification incl. the pinned-host
# guard, repr redaction, EDGE-parity), smoke_soql (SOQL query-file lifecycle,
# SF_ORG_MAX_QUERY_LIMIT env passthrough, the acknowledge_default_limit_override
# 4-case friction set for both soql_query and export_soql, export_soql
# workspace-TSV write via the REAL containment gate with the full refusal
# red-first family, attributes-key stripping), smoke_records (get/describe/
# list/create/update/delete CRUD via the sf CLI's stable + `api request rest`
# surfaces, TOPOLOGY-LEAK red-first on auth/session-expired/permission classes).
# (smoke_export_deny retired 2026-07-15 — see external_postgres note above.)
plugins/salesforce_plugin/tests/smoke_config.py
plugins/salesforce_plugin/tests/smoke_soql.py
plugins/salesforce_plugin/tests/smoke_records.py

# salesforce_plugin D0.3 deferred-completion migration (sync-verb Phase 1 wave
# 2, lane W2b, 2026-08-10 — mirrors external_postgres_plugin's landed batch 1
# shape above, the closest exemplar: single-round-trip-per-verb I/O via a
# driver/CLI executor object, not paginated). All 9 real-I/O verbs
# (soql_query/export_soql/get_record/describe_sobject/list_sobjects/
# create_record/update_record/delete_record/test_connection) now dispatch
# ms-scale (job_id + status: queued, no SalesforceCliExecutor call on the
# dispatch path — proven directly) and complete on the ONE lazily-started
# background worker thread. smoke_async_jobs proves: (1) dispatch for all 9
# verbs returns queued without ever touching run_json/run_rest — the
# containment requirement this lane's brief named explicitly, (2) missing
# session_id/flow_id in state fails loud at dispatch, before any job is
# created, (3) the worker's _process_job runs the real record_actions/
# soql_actions function against a fake executor for a plain read
# (get_record), a write (create_record), the export-path-gated TSV verb
# (soql_query — the worker test proves the TSV actually gets written via
# plugin._export_path_gate, and the always-TSV contract survives the move
# off the dispatch path unchanged), and the special test_connection wrapper,
# completing the job with the correct result shape, (4) a REST-level fault
# classifies topology-safe through the SAME _run path smoke_records.py
# already covers, now reached from the worker instead of the dispatch
# handler, (5) ACTION_HANDLERS carries exactly the 9 migrated verb names — a
# typo here would silently strand that verb's queued jobs forever, (6)
# ensure_worker_started is idempotent (ONE worker thread, never more —
# FlowManager._sequence_cache's unlocked check-then-increment is unsafe for
# concurrent completions into the same flow).
plugins/salesforce_plugin/tests/smoke_async_jobs.py

# zuora_plugin — subscription-billing REST connector (2026-07-13). Predates the
# 2026-07-09 umbrella hardening pass (explicitly out of scope there — "not in the
# operator's four") but built applying that pass's security posture on top of the
# older 2026-06-20 design: topology-safe generic error messages, chain-consumed
# client_secret, sync verb shape (the export deny was part of that posture until
# its 2026-07-15 retirement — see the external_postgres note above) (not the design
# appendix's async sketch). No delete verb exists (billing records are
# voided/cancelled through Zuora's own workflow). Pure REST — no SQL-shaped
# strings anywhere, so the SQL-lockdown gate is silent for src/ (only the test
# fixtures' ZOQL-shaped strings need the standard false-positive allowlisting).
# FOUR hermetic smokes (httpx.MockTransport / MagicMock, no network): smoke_auth
# (tenant config resolve, repr redaction, bearer-token caching-until-near-expiry,
# a 401 triggering exactly ONE re-fetch-and-retry, a failed token mint raising
# ZuoraAuthError), smoke_data_query (business-data limits + data-export migration,
# 2026-08-02: data_query/bulk_export ALWAYS write to output_tsv_path, the queryMore
# continuation loop over POST /v1/action/query + /v1/action/queryMore fixing the
# previously vacuous BULK_EXPORT_ROW_CAP, the §5 override-friction 4-case set per
# verb, export_containment red-first cases, non-2xx raising ZuoraResponseError with
# the correct is_query flag), smoke_lists (same migration: list_subscriptions'
# internal page/pageSize pagination — a regression guard against the prior
# no-params call silently truncating at Zuora's own 20-record default — and
# list_invoices' single-call write-side cap, both with the §5 override-friction set),
# smoke_objects (get/create/update_object CRUD, get_invoice — single-record §1.2
# exemption, unaffected by the migration — TOPOLOGY-LEAK red-first on auth/
# rate-limit classes vs. response-body detail on not-found/validation-failed).
# (smoke_export_deny retired 2026-07-15 — see external_postgres note above.)
plugins/zuora_plugin/tests/smoke_auth.py
plugins/zuora_plugin/tests/smoke_data_query.py
plugins/zuora_plugin/tests/smoke_lists.py
plugins/zuora_plugin/tests/smoke_objects.py

# get_blob_metadata_resolved_path_smoke.py (2026-08-02,
# blob-export retrieval): the data-export fix across marketo/jira/zuora/g_suite left
# a result_blob_key with no registered way to reach the bytes. Fix adds
# resolved_path to get_blob_metadata (reusing resolve_blob_path) + fails loud
# when metadata exists but the file is gone, rather than a silent null. Also
# pins the RED-first refutation of the Lane C design doc's item 3.1 ("unique
# export filenames" to prevent last-write-wins): storage is keyed by the
# state-generated blob_id, never by the metadata filename field, so two
# exports sharing a filename land at two distinct paths with no collision —
# see workbench/2026-07-31_architect_four_item_dispatch_designs.md and
# Coordinator-Dawn's 2026-08-02 ruling dropping 3.1. Real FilesystemProvider
# against a faithful in-memory state-service stand-in (same pattern as
# s3_blob_storage_smoke.py's InMemoryMetadataState), real temp-dir bytes.
plugins/default_blob_storage_plugin/tests/get_blob_metadata_resolved_path_smoke.py

# marketo_plugin — marketing-automation REST connector (2026-07-28), same
# auth-simplicity class as zuora_plugin (OAuth 2.0 client-credentials,
# chain-consumed client_secret) but with a materially different fault model:
# Marketo returns HTTP 200 with a JSON envelope (success: false + errors[])
# for almost every API-level fault, so MarketoClient decodes and classifies
# on the envelope, not the HTTP status — a 601/602 (invalid/expired token)
# envelope triggers exactly ONE re-mint-and-retry instead of a real 401.
# Unlike zuora_plugin, this connector DOES expose a delete verb
# (delete_leads) — Marketo natively supports lead deletion. Partial-batch
# results (create_or_update_leads/delete_leads/add_leads_to_list/
# remove_leads_from_list) stay success:true with per-record skipped/failed
# entries — not classified as an error. No async Bulk Extract job flow in v1
# (get_leads covers ad-hoc reads via the caller-driven Pattern-B loop, §7.2).
# Pure REST — no SQL-shaped strings anywhere, so the SQL-lockdown gate is
# silent for this plugin (verified: zero findings).
# Business-data limits + data-export migration (2026-08-02, §7.1): all six of
# describe_lead_fields/get_leads/list_activity_types/get_activities/
# list_campaigns/list_static_lists now ALWAYS write to a caller-supplied
# output_tsv_path — blob storage retired from this plugin entirely.
# Internal pagination is hidden by design (2026-08-03 hide-paging change,
# operator ruling "the paging is an implementation detail that should be
# hidden", design doc §5.4/§7.2 as amended): get_leads/get_activities/list_campaigns/list_static_lists now
# page INTERNALLY across Marketo's 300-per-call vendor ceiling up to a §5
# acknowledge_default_limit_override/row_limit-governed effective limit
# (default 500, hard cap 5,000) — no next_page_token/more_result field
# survives on any of the four; a truncated boolean replaces them. Beyond the
# hard cap: no resumption, re-invoke with a narrower filter/date-range.
# describe_lead_fields/list_activity_types are unchanged (single call, no
# pagination surface ever existed).
# FIVE hermetic smokes (httpx.MockTransport / faked client, no network):
# smoke_auth (instance config resolve, repr redaction, GET-based bearer-token
# caching-until-near-expiry, a 601 envelope triggering exactly ONE
# re-mint-and-retry, a failed token mint raising MarketoAuthError, EDGE
# parity), smoke_leads (describe_lead_fields/get_leads/get_activities/
# list_activity_types TSV-handle shape via a throwaway tempfile + passthrough
# gate, get_leads/get_activities internal-loop 2-page accumulation +
# hidden-field regression guard, get_activities' empty-mid-stream-page and
# tokenless-moreResult=true contract-violation loop behavior,
# create_or_update_leads/delete_leads batch validation + tallies proving
# partial-batch success is not an error, classify_marketo_envelope across the
# sourced 601/606/702/1005/unmapped code spread), smoke_campaigns_lists
# (list_campaigns/trigger_campaign/list_static_lists/add_leads_to_list
# validation, remove_leads_from_list proving the DELETE-with-JSON-body path
# uses delete_json, not post_json or httpx.Client.delete()),
# smoke_internal_paging (the named request-500-gets-2-internal-
# pages-one-complete-file acceptance scenario plus the shared §5
# override-friction 4-case set, per verb, for all four internally-paginated
# verbs), smoke_data_export (the REAL export_containment gate:
# outside-root/empty-roots/misconfigured-root red-first cases, an admitted
# path proceeding to the vendor call, and the regression guard that
# check_setup's permission probes need NO export_allowed_roots configured at
# all — a throwaway tempfile + passthrough gate, never the operator's real
# workspace).
plugins/marketo_plugin/tests/smoke_auth.py
plugins/marketo_plugin/tests/smoke_leads.py
plugins/marketo_plugin/tests/smoke_campaigns_lists.py
plugins/marketo_plugin/tests/smoke_internal_paging.py
plugins/marketo_plugin/tests/smoke_data_export.py

# smoke_async_dispatch (D0.3 sync-verb migration, 2026-08-09, Lane C2): the
# get_leads/get_activities/list_campaigns/list_static_lists dispatch handlers
# create an AsyncJobManager job and return {job_id, status} in milliseconds
# rather than running vendor I/O inline — dispatch-time session_id/flow_id
# validation (D0.3 doctrine §1 required step) and invalid-params refusal
# never reach create_job; the background worker (_process_pending_jobs)
# picks up only marketo_plugin's own queued jobs, calls the matching real
# execute_<verb>, and routes vendor success/failure to the right terminal
# update_job call; check_marketo_job_status never omits a schema-declared
# key across queued/processing/completed/error (the exact ExecutionContext
# .store_result PlaceholderResolutionError defect class flagged live during
# this migration). A lightweight fake stands in for AsyncJobManager itself
# (its own internals are a separate, platform-level component, out of scope
# here) but matches its documented method signatures/return shapes exactly.
plugins/marketo_plugin/tests/smoke_async_dispatch.py

# trigger_poll dispatch-offload (2026-07-10 fleet-wide dispatch-wedge fix): the cron
# heartbeat trigger_poll now SUBMITS the whole importer pass to a single-slot
# BoundedSummaryExecutor (_poll_executor) and returns {"poller": started|already_running}
# in milliseconds — the 3rd instance of the reviewed heartbeat/drainer pattern — instead
# of running poll_once() INLINE on the action-queue thread (an inline ~1.2M-event first
# pass parked fleet-wide process_call dispatch: the serial _poll_once loop awaits each
# dispatch to completion). Pins (red-first): AST guard (trigger_poll delegates via
# start_importer_poll_drain with NO inline poll_once; the poll_once lives in
# run_importer_poll_drain), fast-return-while-blocked + off-thread execution, the
# singleton guard (second fire → already_running), and slot-release-on-exception (a
# raising pass still frees the slot). Offline (blocking-stub importer; real
# BoundedSummaryExecutor for the threading cases).
ananta/tests/llm/session_ledger/trigger_poll_offloads_drain_smoke.py

# Lane-2 smoke-remediation Packet L2-P2 (2026-07-11) — two previously-UNREGISTERED
# enforcement smokes that had silently gone red (the LED-01 "enforcement existed
# but ungated" class); fixed to full-green and registered here so they gate.
#
# vault_service_proxy_smoke (6 subsmokes): VaultServiceProxy parity + ActionProcessor
# call_context spoofing-negative + proxy call-context binding + oauth-registry
# passthrough + operator-only-method-list parity (MIRRORS the live
# OPERATOR_ONLY_METHODS constant — W-VAULT-CALLER-ENFORCE sub-2 promotions, all
# more-restrictive) + the no-direct-get_service AST gate (Phase D-2: no plugin CALLS
# get_service("vault_service"); AST-precise so documentation prose is ignored, with a
# planted-violation negative control proving the tightened scan is non-vacuous).
ananta/tests/services/vault_service/vault_service_proxy_smoke.py
# w_vault_local_keychain_smoke (13 SC-* subsmokes): Keychain-native vault surface incl.
# operator-ingest namespace disjointness (fixture-pinned SOLET_NAME, not runner-env
# floated) and the SC-16 surviving-`default_vault_plugin`-refs sweep (allowlist covers the
# sanctioned retained SQL namespace — audit + OAuth tables — per KB 11_vault_and_address_book).
plugins/macos_vault_plugin/tests/w_vault_local_keychain_smoke.py

# Lane-2 smoke-remediation Packet L2-P5 (2026-07-11) — the 3 escalation smokes
# from §6.4/§9.7, fixed to full-green + registered so they gate.
#
# policy_lockstep_smoke (4 scenarios): the D35' aws_midwife IAM renderer<->JSON
# lockstep gate (newborn_self_deployment_role.json re-rendered via the checker's
# own path — the D32' SelfDeploymentConfigBucket grant), plugin-load + interface
# declaration, a hermetic env_config VALIDATION-REJECTION test (empty env ->
# ValidationError; AWS-call-free: validate_environment_config precedes any
# AssumeRole/boto3), and the @platform_process missing-arg error envelope.
# profile_lockstep_smoke: D37' byte-equality between the operator profile
# initialization/profiles/local.yaml and the macos_midwife plugin mirror copy
# (the mirror was re-synced whole-file to close the drift).
# birth_solet_smoke (3 cases): macos_midwife 8-step birth (postgres_schema
# retired 2026-06-09) — dry_run planned-steps, live success + per-step status, and
# idempotency via the BURIED marker (constants.MANIFEST_MARKER_PATH, imported not
# re-hardcoded; rewritten atomic tmp+rename each run — non-empty + differs across
# runs while the idempotency_key matches).

# Lane-2 smoke-remediation Packet L2-P1 (2026-07-11, Claude-B) — 5 §6.4 unregistered
# reds fixed to full-green + registered so they gate (red-first; ruff/pyright 0/0 each).
#
# bridge_session_allowlist_smoke: _build_process_call_trigger_data signature drift
# (now requires inference_vertex_role + operator_equivalent) + a Boy-Scout assertion
# that operator_equivalent propagates into authenticated_principal.
plugins/agent_messaging_plugin/tests/bridge_session_allowlist_smoke.py
# current_identity_smoke: Forwarder.__init__ provides_inference (INF-01) + a masked
# v3->v4 role-seed drift (reseeded via claim_role_binding_v4/HolderClaim; the route
# reads roles_held via the v4 list_roles_for_agent_instance, not the v3 claim).
plugins/agent_messaging_plugin/tests/current_identity_smoke.py
# delete_all_in_namespace_smoke: pgvector module reorg (config/provider/constants moved
# under postgres_backend/vector/) + PGVectorProvider ctor signature drift (plugin_namespace
# + kw-only pool_builder/state_service) + qualified_table now double-quotes (assertion
# updated; _no_pool guard proves the delete path never opens the pool).
plugins/pgvector_service_plugin/tests/delete_all_in_namespace_smoke.py
# find_missing_external_ids_chunking_smoke (2026-08-15, D9): the membership read
# is bounded by the CALLER's candidate-list length, and the orphan reconcile
# passes every active memory id (42,500 live) — one read of that shape exceeds
# the read_state row cap and killed a green boot at reindex_orphaned_memories.
# Pins that an over-cap list issues MULTIPLE reads each within the cap, that the
# missing set is correct across chunk boundaries, and that a refused read still
# raises instead of reading as "all present".
plugins/pgvector_service_plugin/tests/find_missing_external_ids_chunking_smoke.py
# s3_blob_storage_smoke: stale FakeStateService (no write_state) -> InMemoryMetadataState
# faithful full-mirror (§9.1 fallback; write->data.result.generated_id, read->data.records)
# + S3ByteIO byte-path injection + hex content assertion + search_blobs test-5.
# task19_action_factory_lazy_orchestrator_smoke: APP_HOME env-fragility -> hermetic
# os.environ.setdefault (guard order verified: only scenarios 1&2 reach the release-mgr path).
plugins/macos_self_deployment_plugin/tests/task19_action_factory_lazy_orchestrator_smoke.py

# Lane-2 smoke-remediation Packet L2-P3 (2026-07-11, Claude-A) — 5 §6.4 unregistered
# reds fixed to full-green + registered so they gate (red-first; ruff/pyright 0/0 each).
#
# claude_code_vendor_recency_smoke: vendor-recency parser 2-case unknown-type contract —
# an event-shape line with an unrecognized 'type' RAISES "unrecognized event-shape line
# 'type'"; a non-event-shape line is TOLERATED (→ 0 events), not raised.
plugins/claude_code_filesystem_session_source_plugin/tests/claude_code_vendor_recency_smoke.py
# claude_code_filesystem_session_source_smoke: removed a stale SQL-substring-priming-stub
# e2e block (dead post-cutover; §9.14 build-recipe) + its driver call + 7 unused imports.
plugins/claude_code_filesystem_session_source_plugin/tests/claude_code_filesystem_session_source_smoke.py
# core_slice4_schema_manager_unit_smoke: REQUIRED_SERVICES grew EMBEDDING_SERVICE (POR
# §1.3 ◆R2) — the fixture binds it so plugin_schema_service is the SOLE missing service
# the first validate_required_services names.
ananta/tests/llm/core_slice4/core_slice4_schema_manager_unit_smoke.py
# role_inbox_smoke: §9 v4 role_binding cutover — seed TABLE_ROLE_BINDING + holder_kind
# ='session' (not the legacy agent_role_binding) + REL-05 owed-delivery columns
# (consumed / escalated / emit_count / last_emitted_at).
ananta/tests/llm/agent_messaging/role_inbox_smoke.py
# smoke_auth (schwab): W-PLUGIN-LAUNCH-KEYS scoped vault key — assert the store key ==
# VAULT_KEY_REFRESH_TOKEN (was the flat pre-migration "schwab_refresh_token" literal).

# schwab_market_data_plugin D0.3 deferred-completion migration (sync-verb
# Phase 1 wave 2, lane W2b's final small batch, 2026-08-10 — same shape as
# salesforce_plugin/external_postgres_plugin above: single-round-trip-per-verb
# I/O, ONE lazily-started background worker thread). get_options_chain now
# dispatches ms-scale (job_id + status: queued; the fast local bootstrap-flag
# check still runs at dispatch, only the real Schwab REST call moved to the
# worker) and completes via async_jobs.py's _process_job, which reuses
# plugin._safe_market_call (the SAME classification path the pre-migration
# dispatch handler used inline). Hermetic — a fake AsyncJobManager + a
# MagicMock standing in for SchwabHTTPClient, no live Schwab call. Proves:
# (1) dispatch returns queued without ever building the HTTP client, (2)
# missing session_id/flow_id in state fails loud at dispatch, before any job
# is created, (3) the worker runs the real market_data_actions.get_options_chain
# against a fake client and completes the job with the correct result shape
# on success and the topology-safe (code, message) via SchwabHTTPError on
# failure, (4) ensure_worker_started is idempotent (ONE worker thread, never
# more — FlowManager._sequence_cache's unlocked check-then-increment is
# unsafe for concurrent completions into the same flow).

# Lane-2 smoke-remediation Packet L2-P6 (2026-07-11, Claude-B) — 23 registrations:
# 1 P6-greened fix (vpc_endpoints) + 5 §6.2b hermetic-green register-candidates +
# the 17-file §6.3 lead tranche (the 22 non-vpc files are already-on-master, green,
# UNCHANGED; measured +~3s on baseline). Per §9.3/§9.15; hermeticity-screened, rc=0 each.
#
# --- P6-greened (fixed this packet, red-first) ---
# vpc_endpoints_smoke: AwsClients codebuild+ecr fixture + 5->7 endpoint-count rot-proofed.
# --- §6.2b hermetic-green register-candidates (stub-driven; boundary-mock only) ---
ananta/src/ananta/core/orchestration/tests/postgres_authorization_guard_smoke.py
# --- §6.3 lead tranche (platform-invariant / cross-plugin-contract guards; interfaces_split = LED-01) ---
ananta/tests/session_ledger_service/interfaces_split_no_god_class_smoke.py
ananta/tests/session_ledger_service/parse_iso_timezone_smoke.py
# list_canonical_contributors_serialization (2026-08-16, lane-fix-small-defects):
# the service-layer wrapper serializes each contributor's first/last_event_at
# to an explicit-offset ISO string at the EDGE seam, while the repository
# layer underneath keeps its own locked-in naive-datetime return contract
# (list_canonical_contributors_migration_smoke.py) untouched. Record:
# workbench/2026-08-16_dispatch_brief_lane_fix_small_defects.md.
ananta/tests/session_ledger_service/list_canonical_contributors_serialization_smoke.py
ananta/tests/platform/process_registry_builder_wiring_smoke.py
ananta/tests/platform/process_registry_merge_validator_smoke.py
ananta/tests/platform/state_service_write_state_registration_smoke.py
ananta/tests/platform/solet_name_smoke.py
ananta/tests/platform/root_manifest_smoke.py

# Root agent-instruction bootstrap (2026-07-27): AGENTS.md and CLAUDE.md are
# thin companion entrypoints with the same no-MCP local KB/session-ledger
# commands, no stale MCP-first routing, and the pre-retrieval safety rules.
ananta/tests/lifecycle_management_service/apply_manifest_binding_rebind_smoke.py
ananta/tests/lifecycle_management_service/manifest_preflight_smoke.py
ananta/tests/core/plugins/set_active_smoke.py
ananta/tests/services/state_service/query_ordered_facade_smoke.py
ananta/tests/services/inference_service/create_inference_error_response_smoke.py
quality_gates/tests/service_interface_ast_check_smoke.py
quality_gates/tests/sql_access_gate_smoke.py
quality_gates/tests/wint2_vault_key_launch_keys_smoke.py

# Lane-2 smoke-remediation Packet L2-P8 (2026-07-11, Claude-A) — 1 rotted §9.14-item-1
# smoke migrated to the v4 role_binding surface + registered (red-first was a file-level
# crash: it seeded the legacy table but resolved v4; the dead claim_role_binding v3 is
# removed from src in the same commit).
plugins/agent_messaging_plugin/tests/role_binding_store_smoke.py

# ── seed_factory_plugin (the origin-only seed factory; 2026-07-20 split of
#    github_midwife_plugin — the birth spine STAYS shipped, the factory moved
#    here). EDGE parity for the 3 factory verbs + the import-direction pin
#    (factory MAY import the birth spine; the birth spine NEVER imports the
#    factory — a seed-born clone has no factory, so the shipping half cannot
#    depend on the non-shipping half).

# seed_factory_plugin SF-A (2026-07-11, Claude-B) — the pure capability resolver
# (capabilities -> plugin set + template + baseline subset, O-3 named-bundle OR
# explicit-list) + its two data files (seed_manifest.yaml, capability_bundles.yaml).
# Offline, no git/network: named-bundle==template-allowlist lockstep, explicit
# passthrough+dedupe, the BORNABLE force-include of the birth spine, the NO-FACTORY
# fail-loud when seed_factory_plugin is named, unknown/empty raises, data-file
# structure. Red-first proven by dropping the ship-invariant (explicit-list
# assertions + the NO-FACTORY negatives go red).

# github_midwife_plugin seed factory SF-B (2026-07-11, Claude-B) — the assemble
# engine (git archive @ committed ref -> extract -> post-archive prune ->
# STRUCTURAL validation). Driven against REAL fixture git repos (no mocks;
# hermetic tempdirs; never live HEAD, never ~/.ananta). Covers: happy-path
# extracted-set equality + envelope, the manifest-vs-archive DIFF teeth (a
# committed export-ignore drop fails closed), path-in-ref preflight, boot-
# coherence refusal, never-clobber. Red-first proven by skipping the prune
# (the DIFF then flags the 6 excluded files as extra).

# github_midwife_plugin seed factory SF-C (2026-07-11, Claude-B, RISKY —
# security-critical, Rev-C CONCUR-WITH-RIDERS) — the clean-by-construction
# CONTENT validator (§4.2), the credential-free LOCAL SEALING verb (rev-D B3),
# and the public-safe seed-README generator. All hermetic (fixture bundle trees,
# real git in tempdirs, injected identity — never the box's runtime identity,
# never the shared worktree). seed_content_validator: the discriminating pair
# (under one injected fleet/operator identity, sanctioned placeholders
# /Users/alice + SOLET_NAME=<name> PASS while the injected operator home
# path + fleet-name + psql-as-operator markers
# REFUSE) proves bounded-context matching + placeholder-passes; plus secret
# filename denylist (incl. UPPERCASE, R4) + PEM/AWS/OpenAI/GitHub/Slack/Bearer
# content (R2), symlink/.git/profile-config, from_runtime fail-closed, and a
# self-ship guard over the REAL shipped SF-C source. seal: neutral author+
# committer with a red-first ambient-leak control, ls-tree==validated
# (.gitignore-drop teeth), enclosing-worktree + existing-.git + missing-target
# guards, planted-secret refuses with NO .git, point-in-time tree_hash.
# seed_readme: the generated README passes §4.2 by construction + hydration
# pointer (GAP-5 read-as-file) + token substitution.
# cited_path_gate (workbench brief 2026-08-14, lane C): walks shipped markdown
# for backtick-quoted repo-path citations, verdicts DEAD/UNPARSEABLE against
# the bundle's own census, tolerance-file-aware. Wired into
# validate_and_seal_seed_bundle's checks tuple, seal-time, before any git op.
# shipped_doc_lint / shipped_doc_gate (GTE-10, 2026-08-18): the SAME two
# shipped-content checks, run bundle-free over a checkout so they reach the
# PRE-COMMIT battery instead of only seal time. Fixture-driven git checkouts,
# never absence-driven. Red-first on the two citations that actually stopped
# the 2026-08-18 publish, plus the tolerated-debt ratchet in all three of its
# directions and the born-clone declared-skip path (a checkout with no seed
# manifest exits 0 and SAYS it measured nothing).
#
# Mutation-calibrated, 9 named mutations each verified RED: per-profile census
# collapsed to a union; --others dropped from the file enumerator;
# --exclude-standard dropped; reserved-identity reverted to first-match;
# ratchet stops blocking on shrink; undeclared profile treated as fine;
# blocking findings stop blocking; the skip banner loses its text; the
# tolerance partition ignores the allowlist.
#
# PURE UNIT, offline: temp `git init` checkouts with a fixture manifest and
# bundles file. No live platform, database, network, bundle assemble, or seal.
# seed_factory_plugin CONTRIBUTOR-POLICY coherence gate (R5, 2026-08-15): the
# published seed shipped five issue forms alongside a CONTRIBUTING.md refusing
# issue reports, contradicted by its own public issue history. Seal-time,
# fail-closed, in validate_and_seal_seed_bundle's checks tuple. Anchored on the
# VERBATIM shipped refusal so a regex tidy-up that stops matching what actually
# shipped reddens here, and on the pull-request-refusal false-positive
# direction, which is correct policy and must never be flagged.
# seed_factory_plugin PRE-BIRTH agent instructions (2026-08-15): the generated
# root CLAUDE.md / AGENTS.md a downloaded-but-uninstalled seed now ships. Pins
# the pre-birth contract (installed-or-not probe, no hydration marker, Step Zero
# neutralized, own-project-directories service model, {{SOLET_NAME}} intact),
# fail-loud on a partial template set, and the two shipping-surface gates the
# pair must pass at the seed ROOT — §4.2 content validation and zero cited-path
# findings.
# github_midwife_plugin seed factory SF-D (2026-07-18, Claude-B, RISKY —
# publish procedure). The mint_seed / mint_and_birth_local authored joseki
# cards encode the §5 rev-D ladder: seal (VERB) before any gh, gh repo create
# WITHOUT --push then a separate git push, private-default; and the local card
# chains assemble_seed -> birth_solet(provision_venv=True) — the explicit
# §7 venv seam (acquisition-mode clone-of-pinned-upstream retired same slice).
# github_midwife_plugin seed factory §12 LICENSING (2026-07-18, Claude-B, RISKY
# — seal-gate on the SF-C security module). Fail-closed: a seed missing its root
# LICENSE / NOTICE / README license section, or a shipped pyproject declaring a
# non-Apache (stale MIT) license, is REFUSED before any git op. Also pins the
# TIGHT generated-README exception in assemble (only README.md is exempt from
# the provenance + manifest-vs-archive-diff checks; any other stray still fails
# closed) and the single-source COPYRIGHT_HOLDER (holder-only — no product mark)
# enforcement.
# github_midwife_plugin seed factory SF-PUBLISH (2026-07-19, Claude-B) — the
# network publish_seed verb, driven through the PUBLIC API against a FAKE gh CLI
# backed by REAL local bare repos, so git ls-remote/fetch/push (incl. real
# fast-forward rejection) run for real with zero network and zero credential.
# HERMETIC (only the gh subprocess seam is faked). Pins the create->update->
# unchanged outcome ladder (append-only re-mint grafted onto the prior remote
# HEAD, both commits neutral), empty-repo recovery, never-clobber
# (existing_repo_not_a_seed / visibility_mismatch), seal re-verification (wrong
# sha / dirty tree / non-neutral HEAD all refuse not_sealed), target safety
# (non-git dir + enclosing-worktree refuse publish_target_unsafe), input+auth
# preflight (repo_name/visibility invalid, gh unavailable/unauthenticated/
# network), and the direct-helper edges (remote_moved non-ff, verification_failed).
# seed_factory_plugin BORN-CLONE PUBLICATION GATE (2026-08-08, seed-adoption) — the
# general form: a bundle is verified by BIRTHING a throwaway and running its own
# register under the DECLARED-MINIMUM environment, so a prerequisite the bundle
# does not guarantee (measured: node, ripgrep) fails instead of resolving off the
# minting host. Red-first throughout: the SAME fixture register reds when an entry
# needs an ambient tool and greens when it does not; exit-77 is a skip not a
# failure with fail_on_skip as caller policy; a registerless bundle is refused;
# and the verdict binding refuses on ABSENCE, on a wrong sealed sha, and on a
# failing verdict before it ever accepts one.
# seed_factory_plugin PROVENANCE stamp (2026-07-20, Claude-C — design §8.8) — the
# deterministic PII-free lineage stamp end-to-end, hermetic (real fixture git +
# real local bare remotes; only the gh seam faked). Pins ENGINE + ASSEMBLE
# determinism (byte-identical stamp ⇒ publish `unchanged` survives), ancestry
# (§7a: generation-zero empty, two-generation chain, malformed-birth fails
# closed), lineage (§7: predecessor chaining + errors), the TRIPLE seal-identity
# recognition (target/interim/legacy — append onto a legacy- AND interim-sealed
# remote verifies), publish lineage_mismatch (red-first) + the pre-provenance
# compat rung, the content-validator passing a real stamp, and the origin_id
# singleton (§8.7: generate-once, read-same, fail-loud on a non-persisting write).
# hydration_templates: every *.template file (incl. the new Step 4a
# fleet_functions.zsh.template) renders with no undocumented {{TOKEN}}
# surviving, zsh-shaped templates parse under `zsh -n`, the settings.json
# template parses as JSON, and TEMPLATE_VARS.md's file map matches disk.
plugins/github_midwife_plugin/tests/hydration_templates_render_smoke.py

# WS-4b.4 stock-Codex hydration receive surface: generated launcher identity
# single-sourcing, parent-bound watch arm, runtime log, loud arm refusal, MCP
# disarm, stock binary exec, and the repo-local Codex marketplace contract.
plugins/github_midwife_plugin/tests/codex_hydration_launcher_smoke.py

# hydration_guidance.md convention (Step 4b generalized, 2026-07-13): any plugin
# needing hydration-time operator setup ships its own knowledge_base/hydration_guidance.md
# (Pitch + Setup sections, proper headers). This smoke enforces the shape so a future
# plugin author's file is actually processable by the runbook's generic discovery step.
plugins/github_midwife_plugin/tests/hydration_guidance_convention_smoke.py

# export_root_validation (2026-08-02, Tier-0 of the limits/data-export migration,
# 07-29 ruling §3): the hydration-time mutual-containment check between a proposed
# export root and app_home, and its idempotent, additive persistence into every
# installed business-connector's export_allowed_roots. Red-first coverage for
# root-contains-app_home / root-inside-app_home / valid-root-accepted-and-persisted;
# the fourth named case (empty-allow-list refusal) is the connectors' own
# pre-existing invariant, covered where that gate lives (external_postgres_plugin's
# smoke_data_export.py and siblings) — not reimplemented here.
plugins/github_midwife_plugin/tests/export_root_validation_smoke.py

# agent_messaging role-inbox session-visibility smoke, landing with the
# Fork-1a packet (Rev-B CLEAR). A4 (2026-08-04): this comment block's sibling,
# direct_wake_rehome_smoke.py (direct-wake recipient-session re-home), retired
# with the direct-wake outbox it exercised.
plugins/agent_messaging_plugin/tests/peer_inbox_session_visibility_smoke.py

# D11 (2026-07-13) — router-less bridge port discovery (Architect ruling,
# workbench/2026-07-13_d11_bridge_port_discovery_routerless_ruling.md):
# manifest-declared router-presence predicate + write_routerless_bridge_port_file
# conditional write in start_interface, the unchanged generic bridge-write
# guard, R3 self-heal on restart, and the R1 fail-loud contract.
plugins/agent_messaging_plugin/tests/d11_routerless_bridge_port_discovery_smoke.py

# Workbench KB registration, Option B (2026-07-16) — the title_block chunking
# strategy + fast-fail dispatch (W1/W2/W3), the per-KB write_posture /
# require_metadata_block / archive_subdir manifest fields (W4/W6), the
# default-scope exclusion + explicit-name bypass (W5), the archive_file lifecycle
# verb (W11), and hash-preconditioned edit + prior-version snapshot (W12).
# Plan: workbench/2026-07-16_workbench_kb_option_b_implementation_plan.md.
plugins/default_knowledge_plugin/tests/file_ops_posture_smoke.py
plugins/default_knowledge_plugin/tests/search_exclusion_bypass_smoke.py
# Phase-2 fix (2026-07-16): search `name` scoping published through the
# decorated service-interface surface + process JSON (it existed only on the
# plugin layer, so MCP silently dropped it and the W5 name= bypass was
# unreachable). Asserts decorated params declare name, wrapper forwards it.
plugins/default_knowledge_plugin/tests/search_name_scope_smoke.py

# Unified-memory-passthrough Slice 1 (2026-07-16) — agent_memory projection
# records get the same consolidation+purge protection as knowledge:official
# (1a); export_memories tags ALL-filter + origin isolation (1b); tag-preserving
# upsert_memory_by_tag + duplicate-slot reconciliation (1c); export/import
# workspace-root containment, refuse-all default (1d); and the tags-param live
# schema parity that Slice 2's process_call drain/hydrate depends on (1e).
# Plan: workbench/2026-07-16_unified_memory_passthrough_design_v2.md.
plugins/actr_memory_plugin/tests/actr_memory_agent_memory_protection_smoke.py
plugins/actr_memory_plugin/tests/actr_memory_export_tag_filter_smoke.py
plugins/actr_memory_plugin/tests/actr_memory_upsert_tag_union_smoke.py
plugins/actr_memory_plugin/tests/actr_memory_export_import_root_gate_smoke.py
plugins/actr_memory_plugin/tests/actr_memory_tags_param_schema_parity_smoke.py
plugins/actr_memory_plugin/tests/config_binding_order_smoke.py

# Memory-store read discipline (2026-08-15) — regression pin for the green-boot
# death on the read_state row cap: whole-table readers (get_all_memories /
# get_all_memorizations) must carry the unbounded=True opt-in, id-filtered
# readers must NOT, and an error-shaped read result must RAISE from every
# helper instead of being swallowed into an empty list (an error rendered
# empty is indistinguishable from data loss — identity verification read
# "no memories" where the truth was "the read was refused").
plugins/actr_memory_plugin/tests/actr_memory_read_discipline_smoke.py

# Unified-memory-passthrough Slice 2 (2026-07-16) — the Claude-only loop-prover's
# LOCAL mechanics (hermetic, no live solet): PostToolUse capture + echo-break, the
# drain upsert-payload (slot + provenance tags), the hydrate renderer (verbatim
# byte-identical round-trip + MEMORY.md + hash stamping), fail-loud on invalid
# frontmatter, the drain watermark, and the local-delete-vs-forget skip.

# Frontier-first B5 (2026-07-17) — the SINGLE ActionFactory result/error-processor
# merge that assembles a local-inference (process_results / process_error) prompt
# from the inference base template ⊕ a process's customizations survived the
# Track-B deletions (B1 ProcessorPolicyService, B2 exposure filter, B4
# field_sensitivities). HERMETIC (no live solet / LM Studio, per this register's
# offline contract): the real merge preserves the base output_schema (decode
# contract), inherits base arguments, injects the customization guidance,
# preserves the <<RESULT>> observation plumbing, and the inference kind routes
# through it while bridge_delivery/None are unaffected. The live LM-Studio
# end-to-end run is the SEPARATE (non-gate-registered) acceptance evidence.
# Plan: workbench/2026-07-15_connector_and_frontier_first_execution_plan.md B5.
ananta/tests/core/actions/action_factory_result_processor_merge_smoke.py

# Postgres unique-flip DDL capability (2026-07-18) — toggling a column's UNIQUE
# constraint (True<->False) is a supported active-update migration: the
# single-axis _is_unique_only_change branch emits DROP/ADD CONSTRAINT and
# ddl_renderer builds the Postgres-default constraint name (<full_table>_<col>_key).
# HERMETIC (schema-diff level, no live DB): asserts the e2e diff produces exactly
# the DROP CONSTRAINT op for a name-unique removal and single-axis discipline.
# Enables the address-book surrogate-key redesign to drop its name-unique
# constraint via the sanctioned metadata-DDL path (no hand-DDL, no execute_sql).
plugins/postgres_state_management_plugin/tests/schema_diff_unique_flip_smoke.py

# Address-book memory_id back-link capture (2026-07-18) — register auto-ingests
# the address to the memory service and the memory IS created, but the returned
# memory_id must be read from the actr backend's TOP-LEVEL shape
# (result["memory_id"]) not result["data"]["memory_id"], else the address->memory
# back-link is never stored (register returned memory_id=null; systemic; orphaned
# memory on delete). HERMETIC (faked memory service): 4 cases — real top-level id
# returns id, auto_ingest disabled -> None + no remember() call, None service,
# missing/legacy-data/None result -> None.
plugins/default_address_book_plugin/tests/test_memory_integration.py

# Address-book surrogate-key redesign: idempotent register + HARD delete
# (2026-07-18) — name is a lookup index only (not a DB unique key; the surrogate
# id is the identity), so register is an idempotent upsert (replace in place, same
# id) and delete is HARD (soft_delete=False on both the address and its
# address_entry rows), designing out the tombstone-holds-unique-name deadlock and
# fixing the entry-orphan leak. HERMETIC (in-memory backend): 17 cases across the
# upsert-in-place and hard-delete-both-tables invariants.
plugins/default_address_book_plugin/tests/test_upsert_and_hard_delete.py

# INF-06 reliability carve — forwarded-vertex holder-forward recoverability
# (2026-07-18). A died/timed-out sys:autonomic holder that was forwarded a
# Surface-1 action-decode vertex now re-drives instead of silent-stalling. The
# durable core__inference_deferred_vertex row gains a lifecycle STATE
# (deferred|forwarded|failed) + forwarded_at + a monotone attempts counter; the
# forward MINTS a 'forwarded' serve-anchor durability-FIRST (record_forwarded_vertex
# before provider.process_*; a flow_id-absent forward RAISES, a store failure
# propagates loud — never an anchorless fire-and-forget). The core half: the mint
# fail-loud contract, the sweep/drain read+write helpers (forwarded_before /
# live_rows_in_state / increment_attempts monotone / mark_terminal_failed /
# attempts_of), and the ★ Architect §6-bis freshness gate — a forwarded
# process_error turn does NOT advance the plan (should_skip_advancement guard,
# RED-FIRST control on process_results), so the failed step keeps its [>] current
# marker and the re-driven FRESH decode is not blind to the failure.
ananta/src/ananta/services/inference_service/tests/forward_vertex_queue_smoke.py
# The plugin half: the REAL AutonomicAssignment forwarded riders + the REAL
# AgentMessagingPlugin._resubmit_vertex (SUB-05 RESUBMIT) primitive. The
# serve-timeout SWEEP re-drives a past-window forwarded row (attempts++ AT
# sweep-fire) WITHOUT hard-deleting — the re-mint upsert PRESERVES the monotone
# attempts (a delete-then-reset would defeat the cap); at the cap it flips terminal
# 'failed'. RESUBMIT is method-agnostic + fresh-decode NOT replay: a process_error
# row re-drives as a FRESH process_results initial vertex (observation stripped,
# instructions emptied — never the recorded decode), returns False + never raises
# on an unknown flow / missing collaborator (per-row sweep/drain isolation). The
# first-claim DRAIN re-drives forwarded rows (dark-lane holder-transition) +
# deferred rows (the pre-existing §D.9 inert-drain gap this closes) and HARD-deletes
# them, SKIPPING terminal 'failed' rows; a GC sweep reaps aged 'failed' rows.
plugins/agent_messaging_plugin/tests/forward_vertex_redrive_smoke.py

# code_vetting_plugin (W3-A, 2026-07-20, Claude-B) — the AI code-vetting suite
# promoted from quality_gates/vetting/ into the shipping plugin. Two hermetic L1
# smokes relocated with the tree (no live solet / LM Studio; verified offline from
# the new plugin path). l1_scanners: pure-Python scanners emit the right F1
# dimension/constraint on crafted fixtures + a tool-absent path records a
# coverage gap rather than silently passing. secrets_downgrade: the gitleaks
# low-confidence path-class downgrade stays honest — a safe-path hit downgrades
# to advisory/not-shown, the same rule on real source stays HIGH, and a
# specific-provider rule stays HIGH even on a fixture path. Requires the
# code_vetting_plugin package installed (pip install -e). Record:
# workbench/2026-07-20_w3a_plugin_birth_record.md.
plugins/code_vetting_plugin/tests/l1_scanners_smoke.py
plugins/code_vetting_plugin/tests/secrets_downgrade_smoke.py

# self-vet defect B (2026-08-16, lane-fix-small-defects) — the secrets
# scanner's git-target snapshot (_tracked_snapshot -> _extract_skipping_
# unsafe_links) must not die on a tracked absolute/escaping symlink member;
# red-first fixture plus a negative control (an ordinary repo with no
# symlinks still extracts everything). Record: this repair's dispatch memo,
# workbench/2026-08-16_dispatch_brief_lane_fix_small_defects.md.
plugins/code_vetting_plugin/tests/secrets_symlink_extract_smoke.py

# code_vetting_plugin verb surface (W3-A A2, 2026-07-20, Claude-B) — the two EDGE
# verbs' (vet_codebase / scan_quality_guidelines) invariants that running the real
# minutes-long scanners cannot hermetically pin: EDGE decorated<->declared parity
# (red-first guard against the process_registry.edge_process_mismatch startup
# FATAL), the mandatory result-envelope shape, deploy-invariant worktree anchoring
# (the APP_HOME walk + fail-loud off the worktree), and the bounded-report cap.
# Pure Python — no live solet / LM Studio / subprocess / mocks.
plugins/code_vetting_plugin/tests/verb_surface_smoke.py

# code_vetting_plugin dispatch security (W3-B B1, 2026-07-20, Claude-B) — the two
# SECURITY-critical invariants of the inference-substrate/dispatch layer, as build-time
# guards: RIDER-1 redaction (sensitive-dimension evidence + the raw code pack withheld
# from an OFF-OPERATOR forward; locus preserved; the LOCAL substrate keeps full evidence),
# and the A0-R3 metered-key ban made STRUCTURAL (no transport/dispatcher/selector field
# and no verb param accepts an api_key/token/secret — a red-first scan). Pure Python — no
# live solet / LM Studio / subprocess / mocks.
plugins/code_vetting_plugin/tests/dispatch_security_smoke.py

# code_vetting_plugin live state adapter (W3-B B2, 2026-07-20, Claude-B) — the
# production StateWriter binding for the vetting_runs metrics trail, driven
# against a FAKE synchronous state_service. Pins the async-over-sync bridge +
# the state-interface arg mapping: upsert{table,record,conflict_columns=[run_id]},
# query_ordered{order_by:[[started,asc],[run_id,asc]],limit}→data.records,
# delete_records{filters,soft_delete:False} HARD-delete prune; ActionResult unwrap
# fail-loud on non-COMPLETED; MetricsWriter runs UNCHANGED + enforces retention
# end-to-end; the `substrate` provenance field round-trips; the declarative
# vetting_runs SchemaDefinition shape; and ONLY the 3 sanctioned verbs are ever
# called (no raw SQL / execute_sql / foreign namespace). Offline (in-memory fake).
plugins/code_vetting_plugin/tests/live_state_smoke.py

# code_vetting_plugin foreign-target vetting (W3-B FT-1, 2026-07-20, Claude-B) —
# the read-only `target_path` de-anchor on vet_codebase (Architect foreign-target
# ruling A + C). Pins: _resolve_target self-vet default + the fail-loud refusals
# (relative / non-existent / non-dir / inside-own-worktree, red-first); the
# read-only structural from_walk fallback (curated junk excludes, symlink skip,
# enumeration='walk' + foreign=True — NOT gitignore emulation); _build_tree
# git-preferred (ref=HEAD) vs walk-fallback (ref=''); and the scanner-applicability
# ledger invariant — on a FOREIGN tree the self_only platform-canon scanners
# (code_quality/sql_access/orphan_kb/prior_pass) do NOT execute but stay in the
# roster as not_applicable records (scanners_total unchanged). Offline (tmp trees;
# real git for the git-mode build_tree; run_all over a tiny foreign tree).
plugins/code_vetting_plugin/tests/foreign_target_smoke.py

# code_vetting_plugin foreign source-role/platform-variant attribution
# (Cultivar proving wedge, 2026-07-27). Pins the foreign-only, doctrine-backed
# prefix override contract; repo-risk non-suppressibility; exhaustive dimension
# policy; product/non-product/unconfirmed surface partition; bounded candidate
# and platform evidence; exact reconciliation; target-JSON persistence; and
# byte-compatible self-vet omission. Offline (tempfile target + state fake).
plugins/code_vetting_plugin/tests/source_roles_smoke.py

# code_vetting_plugin foreign technology fingerprint + capability routing
# (Wedge 2A, 2026-07-26). Pins the additive target.technology_fingerprint:
# fixed/reconciled enumerated-file probes with bounded strict JSON reads and
# visible malformed/duplicate/oversize/symlink/JSONC gaps; per-package and Deno
# scope isolation; direct vs co-scoped resolved vs lock-only component evidence
# (including scoped npm names); confirmed non-product visibility without product
# routing; bounded Expo Router paths grouped only by confirmed overrides;
# separate Deno/Supabase topology; deterministic capability keys/statuses whose
# execution_status is always not_run_by_fingerprint; markdown ordering/escaping;
# no CoverageRecord mutation; self omission; inline + persisted target-JSON
# round-trip. Hermetic (temporary target + state fake; no network/subprocess/tool
# execution).
plugins/code_vetting_plugin/tests/technology_fingerprint_smoke.py

# code_vetting_plugin test-coverage integration (coverage micro-slice, 2026-07-20,
# Claude-A spec / Claude-B applied; Architect foreign-target ruling §D1-D3) — the
# test_reach dimension + the ## Test Coverage report section. Pins: Dimension.TEST_REACH
# registered; TestCoverageReport.from_harness_artifact round-trips + renders the owner
# rollup with NO percentage (evidence, not a bar); the ## Coverage -> ## Scanner Coverage
# rename + the ## Test Coverage placeholder both present on an unpopulated render;
# build_test_reach_findings emits ONE advisory L1 finding for a verb-owner with 0 executed
# src (and none for a covered owner); zero_reach_targets exposes the critic aiming list.
# Hermetic (synthetic harness artifact; no coverage tool needed).
plugins/code_vetting_plugin/tests/test_coverage_integration_smoke.py

# code_vetting_plugin foreign-scanner scoping (W3-B FT-1.1, 2026-07-20, Claude-B) —
# defects 1+2 from the first LIVE foreign vet. Pins: pip-audit NEVER runs its
# environment-audit mode on a foreign target (no python manifest -> not_applicable;
# manifest present -> a deferred Phase-2 gap) so this engine's venv CVEs are never
# mis-attributed to the target; bandit's no-targets branch + semgrep on a no-python
# quality-surface record an honest coverage GAP (ran=False) instead of reading as
# 'ran clean' (ran=True/0 files). Gated on an empty python surface so a self-vet is
# byte-unaffected (positive python-tree cases prove the gate stays off). Hermetic
# (tmp walk-trees; tool-independent gap logic + tool-guarded reason assertions).
plugins/code_vetting_plugin/tests/foreign_scanner_scope_smoke.py

# code_vetting_plugin report accounting (W3-B FT-1.1 defect 3, 2026-07-20, Claude-B) —
# the ## What was filtered footer now partitions every emitted finding into exactly one
# mutually-exclusive bucket (confirmed / zero-FP-promoted / refuted / downgraded-secret /
# unverified-candidate) so rendered + filtered == emitted, and reports the collapsed-
# duplicate delta so rendered-unique reconciles with rendered-raw. Pins the reconciliation
# invariant + the exact footer text on a crafted one-per-bucket scenario incl. a duplicate
# finding-id. Hermetic (synthetic findings; no tools).
plugins/code_vetting_plugin/tests/report_accounting_smoke.py

# code_vetting_plugin inference-dispatch SEAM (W3-B B3c, 2026-07-20, Claude-B) — the
# staged wiring of the B1 substrate (selector + inference dispatcher + off-operator
# redaction) into the VettingDriver, driven END-TO-END so the seam is tested-not-dead:
# a driver whose L3 is the substrate-selected inference verifier (RecordedTransport)
# stamps a routed candidate + records the substrate on the metrics row; an OFF_MACHINE
# transport => off_operator=True => the forwarded prompt withholds a SENSITIVE finding's
# raw evidence but keeps its locus (RIDER-1); PRIVACY hard-refuses an off-machine transport;
# assemble_inference_driver records the substrate + wires the L3 adapter; the plugin seam
# factories build the transports + driver over a fake inference_service (vacant => fail-loud).
# Hermetic (RecordedTransport / fake infer_fn / capturing test-double; no live inference/subprocess).
plugins/code_vetting_plugin/tests/inference_wiring_smoke.py

# code_vetting_plugin verb import-closure guard (W3-B B3c, 2026-07-20, Claude-B) — pins the
# A0 R6 invariant Reviewer-C verified by grep into a PERMANENT gate: importing the verb
# surface (code_vetting_plugin.plugin) must NOT transitively import the inference subsystem
# (verify/* transports, inference_wiring, the driver/l3_adapter L3 path). Holds because the
# B3c seam imports those function-locally + under TYPE_CHECKING. Runs in its own process so
# the observed sys.modules IS the verb surface's first-import closure. Non-vacuous (asserts
# the plugin module itself loaded). Hermetic.
plugins/code_vetting_plugin/tests/verb_import_closure_smoke.py

# code_vetting_plugin policy-tier rendering (W3-B FT-2, 2026-07-20, Claude-B; Architect
# foreign-target ruling §36-40) — makes the DO-NOT-FLAG screen + L3 POLICY refute directive
# target-class-aware so a foreign multi-tenant target's real RLS/session-isolation finding is
# not dispositively refuted as a single-user category error. Pins: active_tiers derives
# the stack from the class (self=both, foreign=universal-only); the self-vet POLICY directive
# is BYTE-IDENTICAL to the pre-FT-2 text while the foreign render DROPS the project-local clauses
# (multi-tenant category-error / absence-of-raw-SQL / RB-SCOPE); the assembled skeptic prompt
# omits them on a foreign stack; every keyword DNF rule is tagged project-local so the whole
# keyword screen disables on foreign — end-to-end, the heuristic POLICY pre-screen UPHOLDs
# (not dispositive-refutes) a real multi-tenant finding on a foreign target. Hermetic.
plugins/code_vetting_plugin/tests/policy_tier_smoke.py

# code_vetting_plugin stack detection + applicability spine (R7-1, 2026-07-21, Claude-B;
# design workbench/2026-07-20_ts_native_deterministic_adapter_design.md §3/§4/§9). Pins the
# multi-ecosystem spine: detect_stacks (enumeration-driven; .d.ts + root tsconfig count as TS;
# pure-py/TS/mixed/pure-JS fixtures map to the expected Stack sets); the R2 __post_init__
# (stacks non-empty IFF applicability is STACK); the R2 INTERSECTION gate (eslint-like {TS,JS}
# applicable on pure-JS while tsc-like {TS} is not — a single-stack field would false-skip
# eslint); run_all's STACK branch end-to-end (not_applicable record on a no-matching-stack tree,
# executes when the stack is present, roster never forks); and the report header Stacks line.
# Hermetic (tmp walk-trees + a no-op probe scanner; no tools). No STACK scanner ships in R7-1's
# roster yet — tsc/eslint land in R7-4; this pins the machinery they plug into.
plugins/code_vetting_plugin/tests/stack_detection_smoke.py

# code_vetting_plugin semgrep multi-stack ruleset (R7-2, 2026-07-21, Claude-B; design §5.4 —
# FT-D2 proper). Pins (HERMETIC, no live registry hit): pack-per-detected-stack selection
# (PYTHON→p/python, TS→p/typescript, JS→p/javascript; additive sorted union; no mapped stack
# → ()); scan-target/exclude construction (self-vet → quality-surface top-level dirs; foreign
# → '.' + a --exclude per curated WALK_EXCLUDE_DIRS dir, closing the materialized-junk residual);
# files_examined from semgrep's own paths.scanned (ground truth, hardens the FT-D2 'ran clean over
# 0 files' failure); the no-pack GAP (unmapped-stack tree → honest not_applicable, returns before
# any subprocess); and the findings parse. The live TS positive-control is build-verify only
# (registry flake stays a runtime gap, never a gate red). Supersedes FT-1.1's semgrep no-python
# gap (a TS tree now selects p/typescript) — that assertion was removed from foreign_scanner_scope.
plugins/code_vetting_plugin/tests/semgrep_multistack_smoke.py

# code_vetting_plugin osv per-lockfile SCA (R7-3, 2026-07-21, Claude-B; design §5.3). Pins
# (HERMETIC, no live OSV.dev call): _osv_lockfiles (npm + pinned-python lockfiles poetry/pipfile/
# uv/requirements*.txt; a bare pyproject/setup is NOT a lockfile; no lockfile → ()); the CLI-version
# shape-match (_osv_major + _osv_argv — v2 'scan source --lockfile', v1 bare '--lockfile', unparseable
# → None = a loud gap not a guess); per-lockfile ATTRIBUTION (findings anchor to the source.path
# lockfile, NOT _declaring_pyproject which would mis-attribute an npm CVE to a python-env marker);
# and the no-lockfile not_applicable gap (returns before any subprocess). Enumeration-driven per-
# lockfile invocation replaces `-r <root>` (never traverses a materialized node_modules); files_examined
# = lockfiles scanned (was wrongly len(pyproject_files())). Live OSV.dev SCA = build-verify only.
plugins/code_vetting_plugin/tests/osv_lockfile_smoke.py

# ts_toolchain tsc + eslint target-toolchain scanners (R7-4, 2026-07-21) — the
# materialized-deps tier. HERMETIC/absence-tolerant: roster wiring (17 scanners;
# tsc STACK/{TS}, eslint STACK/{TS,JS}, both executes_target_code); the R1 opt-in
# gate BOTH new branches (node_modules present + flag off → opt-in gap [returns
# before any tool touch]; flag on + no node_modules → materialization/node gap,
# absence-tolerant on host node); canned tsc/eslint parse (TYPE_COVERAGE/
# CODE_QUALITY, sev map, null-ruleId → eslint:fatal, node_modules-anchored
# diagnostics DROPPED with count); flood cap (>200 → first 200) + disclosure;
# R3 ran-with-disclosure (a ran=True cap-disclosure record is NOT a coverage_gap
# and renders ran=yes); read-only invariant (target tree byte-identical before/
# after a full run_all, flag on and off). Live tsc/eslint run = build-verify only
# (needs a materialized node_modules). Design:
# workbench/2026-07-20_ts_native_deterministic_adapter_design.md §5.1/§5.2/§6/§8.
plugins/code_vetting_plugin/tests/ts_toolchain_smoke.py

# python_type_check foreign-Python type-checker (R9-E, 2026-07-21) — the R7-4
# mirror: ONE UNIVERSAL... STACK/{PYTHON} scanner, executes_target_code=True,
# behind the existing execute_target_toolchain opt-in (roster now 19). HERMETIC +
# absence-tolerant: THREE distinct gap classes red-first (no config → no-contract
# not_applicable; config + flag unset → opt-in gap; config + flag set + no
# {.venv,venv} → materialization gap — all return before any tool touch); canned
# pyright --outputjson parse (0-indexed line +1, error→MED/warning→LOW,
# constraint pyright:<rule>, message newline-flattened) + canned mypy text parse
# (mypy:<code>, error→MED/note→LOW); env/site-packages-anchored diagnostics
# DROPPED with count; checker version rides provenance; per-tool flood cap;
# TYPE_COVERAGE dim (zero-FP-promoted); SELF finding rows SUPPRESSED (the pyright
# --strict gate is sole self authority, R8 §C) vs FOREIGN emit (live end-to-end
# absence-tolerant on host pyright/mypy). Never installs / never materializes a
# venv. Live positive-control (both checkers on a configured target) = build-verify.
# Ruling: §R9-E workbench/2026-07-21_architect_r9_foreign_parity_ruling.md.
plugins/code_vetting_plugin/tests/python_type_check_smoke.py

# structural_metrics multi-language complexity scanner (R8-1, 2026-07-21) — ONE
# UNIVERSAL parse-only scanner (roster now 18) backed by lizard==1.23.0 (pinned
# in-process lib). HERMETIC + EXACT: pins per-function CCN/NLOC/params/nesting-depth
# on a TS+Python fixture to deterministic probed values (nesting via the `nd`
# extension + a fresh analyzer per file — the `ns` extension over-counts and
# accumulates across a shared analyzer); pinned-version + supported-language-set
# (ts/tsx/js/python present, dart absent — a lizard upgrade that changes coverage
# turns it red); universal-tier band findings on FOREIGN (CCN>30 MEDIUM /
# params>8, nesting>6 LOW; ceiling MEDIUM; COMPLEXITY dimension, zero-FP-promoted)
# vs project-local-tier SUPPRESSION of finding rows on self (same payload, no rows);
# the `## Structural Quality Metrics` section (distribution + worst-offenders +
# the fixed radon-vs-lizard provenance note, NO A-F rank); payload round-trip onto
# the run record + vetting_runs schema column; >1MB size-cap skip disclosed on a
# ran=True row. Design: workbench/2026-07-21_architect_r8_structural_metrics_ruling.md.
plugins/code_vetting_plugin/tests/structural_metrics_smoke.py

# dead-code scanner rebuild — in-process pinned vulture 2.16 (R9-A, 2026-07-21).
# Replaces the bare-PATH LOW-everything stub with a TWO-LAYER scanner. HERMETIC:
# pins the EXACT probed vulture confidences per class (import=90, unreachable=100,
# function/class/variable/method=60 — an upgrade shifting semantics fails loud);
# findings ONLY for the provable classes (unreachable→MEDIUM, unused-import→LOW,
# emitted verdict=CONFIRMED so they render without dimension-promotion), __init__.py
# imports NEVER a finding; the 60% family → capped candidate-dead-symbols payload
# (section + L2 targeting evidence, NEVER findings); per-class self emission
# (imports SUPPRESSED under F401, unreachable EMITTED, @platform_process dropped
# from the self candidate table via project-local-tier ignore-decorators but present
# on foreign); dim=DEAD_CODE (out of the zero-FP registry); candidate-table cap
# disclosure; payload round-trip onto the run record + vetting_runs schema column.
# Ruling: §A workbench/2026-07-21_architect_r9_foreign_parity_ruling.md.
plugins/code_vetting_plugin/tests/dead_code_smoke.py

# duplication disjoint-by-language dispatch (R8-2, 2026-07-21) — ONE UNIVERSAL
# registry slot, internal per-language dispatch per the §A.4 REVISED ruling
# (Option A + riders). HERMETIC: Python exact-block lane (dup:exact_block,
# source=gate:duplication) BYTE-IDENTICAL zero-regression with and without
# non-Python files present; non-Python token-clone lane (dup:token_clone,
# source=lizard, -Eduplicate cross_file_process → get_duplicates at
# min_duplicate_tokens=70) finds a TS clone fixture; DISJOINT proof — a
# python-only tree yields ZERO token_clone rows (lanes never co-report on the
# same file); distinct finding ids + provenance per lane; the coverage row
# ALWAYS discloses the per-language split (mechanism + file counts + threshold
# + per-mechanism dropped — rider 3). Named honest gaps (Python renamed-var
# token-clones = L2; foreign-Python exact-dup out of exact-block's
# self-quality-surface scope) are ruling-recorded, not smoke-tested.
# Ruling: §A.4 REVISED in workbench/2026-07-21_architect_r8_structural_metrics_ruling.md.
plugins/code_vetting_plugin/tests/duplication_disjoint_smoke.py

# assembled hash-pinned rulebook (W3-C C1a, 2026-07-21, Claude-B; Architect Q1/Q2 in
# workbench/2026-07-21_architect_w3c_turnkey_ruling.md). Pins the committed-artifact +
# three-level manifest (per-source/per-tier/whole-artifact hashes) + fail-loud loader:
# the assembler is a pure function of its sources (deterministic; the COMMITTED
# assembled_rulebook.json matches a fresh re-assembly — never stale), a tampered/corrupt
# artifact raises at verify_artifact (the runtime never seeds skeptics against a broken
# moat), the self-vet POLICY directive renders BYTE-IDENTICAL through the assembled stack
# (the FT-2 regression bar — clauses stored verbatim), and §40 tier-filtering drops the
# project-local preamble sections + directive clauses on a foreign target (a real foreign
# finding is never pre-refuted). Design:
# workbench/2026-07-21_w3c_turnkey_ai_critic_design.md.

# rulebook-integrity scanner (W3-C C1b, 2026-07-21, Claude-B; Architect Q2 in
# workbench/2026-07-21_architect_w3c_turnkey_ruling.md). Pins the self-policing lane that keeps
# the moat honest — the 20th roster scanner (self_only, parse-only). Two failure modes collapse to
# ONE stale_rulebook HIGH code_quality finding: a CORRUPT committed artifact (content no longer
# matches its own pinned whole-artifact hash — caught by the shared verify_artifact fail-loud) and a
# STALE one (a source edited without rebuilding drifts ahead — caught by re-assembling and comparing;
# the per-source hashes localize the drifted anchor). HIGH is deliberately above the R8 metric ceiling
# — a stale/corrupt rulebook breaks the run's whole trust chain. The report renders a RULEBOOK
# INTEGRITY WARNING banner in the title block iff the run carries the finding (byte-absent on a clean
# run). Red-first: a freshly-assembled tree is CLEAN; a drifted guidance source turns it red; a
# hand-edited artifact turns it red via the corrupt path. Design:
# workbench/2026-07-21_w3c_turnkey_ai_critic_design.md.
plugins/code_vetting_plugin/tests/rulebook_sync_smoke.py

# get_vetting_run read-verb (W3-C C3a, 2026-07-21, Claude-B; Architect Q3 + Dusk fork-ruling Option A
# in workbench/2026-07-21_w3c_c3a_get_vetting_run_design.md). The run_id READ-VERB the joseki card's
# payload routing is gated on (the ruling routes L2/L3 payloads by run_id through read-verbs ONLY —
# deterministic steps cannot pipe a prior step's runtime result). Pins: read_vetting_run reads a
# persisted vetting_runs row by run_id via the SYNC single-namespace query_state equality filter (own
# namespace, no join, no raw SQL — asserted on the recorded call shape) and returns None for an unknown
# id; a run persisted with report + dead_symbols + structural_metrics round-trips at the joseki's field
# paths (report, dead_symbols.candidates[], structural_metrics.literals[]); the get_vetting_run verb
# returns a COMPLETED envelope with the row for a known id, a typed run_not_found ERROR envelope for an
# unknown id (fail-loud, never an empty success), and invalid_run_id for an empty id. HERMETIC (a fake
# StateServiceProtocol; no live solet/DB). The report column is added to the vetting_runs schema +
# RunMetrics + persisted in the driver (R8-1 column pattern). Design:
# workbench/2026-07-21_w3c_c3a_get_vetting_run_design.md.
plugins/code_vetting_plugin/tests/get_vetting_run_smoke.py

# vet_codebase persist opt-in (W3-C persist micro-slice, 2026-07-21, Claude-B; Dusk GO on the ACKed
# design). The joseki's step-1 pin needs a PERSISTED run to read by run_id; the bare vet_codebase verb
# returned inline and did not persist. Adds an opt-in persist:true param (default false, return payload
# byte-identical — persist is a pure side-effect) that writes an L1-only run (substrate='heuristic',
# layers_run=[L1_deterministic]) via a SYNC persist_run_sync beside MetricsWriter (state_service is sync
# from the executor thread; shares the retention decision _prune_targets with the async writer so the
# bound cannot drift). Pins: sync write + bounded-retention prune (round-trips report + structural_metrics.
# literals[] + dead_symbols.candidates[] via read_vetting_run; retention=2 over 3 rows drops the oldest);
# the _run persist GATE red-first (persist=False writes NOTHING + payload key set unchanged; persist=True
# writes exactly one row); vet_codebase param WIRING red-first (reads persist, threads it to _run). A
# foreign target's allowlist_delta is empty (allowlist_totals is a self-vet concept — no fail-loud on the
# absent quality_gates/ files). HERMETIC (a fake StateServiceProtocol + a stub scan_fn; no real scanners).
plugins/code_vetting_plugin/tests/vet_persist_smoke.py

# scheduled self-vet heartbeat (W3-C W3C-3, 2026-07-21, Claude-B; Architect ruling Q4 + Dusk GO). The
# program's LAST slice: a NEW EDGE_SINK verb trigger_scheduled_self_vet fires the DAILY L1-only self-vet
# on a single-slot background executor (the slot IS the lease) and returns a fast started/already_running
# receipt — a full L1 self-vet is MINUTES so it must NOT run inline (action-queue fast-return contract).
# The background run goes through the PERSISTING L1 path (a vetting_runs row per run = the R9-3 trend
# baseline; substrate=heuristic) and queues a REGRESSION-ONLY memory note (tag
# code_vetting:self_vet_regression) when its blocker/high counts worsen vs the prior run (else silent —
# trend-not-nag). Pins: single-slot lease (in-flight → already_running; frees after the run); the verb
# FAST-RETURNS touching NO state/memory (the R6/boundary is smoke-visible via a raising service + a
# non-running executor); EDGE_SINK registration (processor_policy_category=edge_sink + NOT in
# get_edge_process_definitions, else edge_process_mismatch FATAL); the is_regression predicate (blocker
# OR high worsens → True; medium/low churn silent); the background body persists a row + queues the note
# only on a worse run. HERMETIC (fake create-before-upsert StateServiceProtocol + fake memory + a stub
# scan; no real scanners / live solet). Cron REGISTRATION is a post-deploy create_cron_schedule step (out
# of this slice). Design: workbench/2026-07-21_w3c3_trigger_scheduled_self_vet_design.md.
plugins/code_vetting_plugin/tests/scheduled_self_vet_smoke.py

# local-invocation CLI (2026-07-18, v1) — the non-MCP local client over
# the localhost bridge surface, for clients whose MCP-server allowlist blocks the
# stdio bridge. HERMETIC (httpx.MockTransport; self-defaults SOLET_NAME):
# health/search/call happy paths + JSON output; `call` polls process/result until
# terminal; one-shot invocation NEVER registers a peer identity (no registry
# pollution — the load-bearing design pin); exit-code mapping for
# not-running/non-completed/bad-args. Design:
# workbench/2026-07-18_mcp_free_local_invocation_cli_design.md.
plugins/agent_messaging_plugin/tests/local_cli_smoke.py

# Watcher lifecycle. Real subprocesses under a
# redirected XDG_RUNTIME_DIR, because flock is a property of a process's open
# file description and an in-process runner cannot exercise it honestly. Covers
# W1 (second arm REFUSES and leaves the incumbent undisturbed; a sequential
# re-arm after a clean exit still succeeds, so the singleton is a live-holder
# check and not a permanent lockout), W2 (SIGTERM exits 143 via the handler
# rather than -15 by default disposition — the discriminator that proves the
# bridge context manager got to unwind and evict the registry row), and the
# --exit-with-parent probe (absent pid means gone; an unsignalable-but-live pid
# does not).
plugins/agent_messaging_plugin/tests/watch_lifecycle_smoke.py

# A4 slice 2 (2026-08-04) — FLEET_TRANSPORT watch-transport parity proof.
# Marker retirement (A4) makes every peer_send/peer_send_by_name call
# append_event unconditionally; this smoke proves end-to-end, against REAL
# collaborators (BridgeSessionManager, PeerRegistry, the real /events FastAPI
# route, dispatch_peer_send, local_cli/cli.py's own _emit_line), that a
# markerless send reaches the watch client's /events poll AND the watch
# spool file — the exact chain _stream_events drives, unchanged by slice 1.
plugins/agent_messaging_plugin/tests/watch_transport_parity_smoke.py

# The dispatch liveness gate. `append_event` succeeds
# against ANY non-closed bridge, and a SIGKILLed watcher leaves its server-side
# session standing, so `queued_watcher` was reported for up to the full 3_600s
# idle sweep while nothing polled the queue. The SAFETY half is the priority
# half: every "stale is refused" case is paired with a "fresh still delivers"
# control, because an over-firing gate would stop delivery to healthy watchers —
# a worse regression than the defect. The knob is exercised at NON-DEFAULT
# values (300s and 30s) so a dropped config wiring cannot hide behind the 90s
# default.
plugins/agent_messaging_plugin/tests/watcher_liveness_gate_smoke.py

# WS-2e §4.3.3a (Day's live route-theft find) — peer_register refuses to bind
# under a session id a DIFFERENT LIVE session already holds. Without it, a
# watcher launched from another session's shell inherits its AGENT_SESSION_ID
# and the register self-refresh silently re-points EVERY role that session
# holds. R7b is the safety half and the reason the gate is LIVENESS-gated
# rather than existence-gated: a restart always brings a new instance id under
# the same session id, so an existence gate would refuse every legitimate
# succession — worse than the theft it prevents, and R7a alone cannot see it.
plugins/agent_messaging_plugin/tests/register_session_conflict_smoke.py

# F-AISLOP timestamp-fragility unification (2026-07-20) — the AI code-vetting
# suite's ai_slop lens found ONE lexical ISO-8601 timestamp idiom copy-pasted
# across 3 durable-queue serve-timeout/GC sites (false-mirror docstrings + a
# diverged missing-stamp default). All 3 now value-compare via
# ananta.core.domain.timestamps.to_naive_utc (no lexical fallback) and fail LOUD
# on an unparseable stamp. HERMETIC (pure predicate calls + one integrated
# gc_terminal_rows drive against the REAL-SHAPE state fake; no live solet/LM
# Studio/DB; self-defaults SOLET_NAME). RED-FIRST: a NAIVE stored stamp
# equal to the tz-aware cutoff instant discriminates value-vs-lexical, so
# reverting any site to the lexical compare turns its boundary + fail-loud cases
# red. Sites: forwarded_vertex_reconcile.terminal_row_aged (GC, never-delete-on-
# unknown-age) + deferred_vertex_queue.forwarded_before + completion_request_queue
# .forwarded_before (both sweeps, surface-on-anomaly). Record:
# workbench/2026-07-20_faislop_timestamp_unification_record.md.
plugins/agent_messaging_plugin/tests/timestamp_value_compare_smoke.py
plugins/agent_messaging_plugin/tests/wake_hook_smoke.py
plugins/macos_self_deployment_plugin/tests/steady_state_reassert_activation_smoke.py

# Bridge peer-identity registration retry (2026-07-25). `_open_with_retry` loops
# unbounded but `_register_identity` had ONE attempt and swallowed failures into a
# log line, leaving a half-alive session: process calls work, the session is
# invisible to peer_list/peer_send and cannot read peer_inbox. Hit three sessions
# in one evening, each presenting as "that agent is ignoring me". Hermetic —
# scripted _post, patched backoff, no bridge or network.
plugins/agent_messaging_plugin/tests/register_identity_retry_smoke.py

# KB retrieval-audit cron install with durable identity (2026-07-31). The
# predecessor trigger `sch-2l18uk4q9et3a` fired daily from 2026-06-01 and executed
# the audit ZERO times, for two independent reasons: it used the memory-tag
# heartbeat shape (terminated at the poller's EDGE_SINK_SKIP branch with no
# dispatch, forever, for headless crons), and it inherited the creating session's
# id, dead since June. `ensure_kb_retrieval_audit_schedule` installs the EDGE_SINK
# target with system-owned session_id AND flow_id -- both, because fixing only the
# session id is a half-fix indistinguishable from a fix until it dies. The smoke
# also pins the two properties that keep the retirement safe: the default tag must
# NOT match the predecessor's (so the idempotent clear-by-tag step can never bare-
# swap it), and the already-present check must compare the action's TARGET and not
# only the cron (a right-cadence wrong-target schedule is the defect itself).
# Hermetic -- shipped factory and shipped validator, no scheduler, no DB.
plugins/default_scheduling_plugin/tests/ensure_kb_retrieval_audit_schedule_smoke.py

# Cron UTC-compliance repair (2026-08-16 lane-utc-scheduler). SchedulerManager's
# BackgroundScheduler is configured `timezone=datetime.UTC`, but `add_cron_job`
# called `CronTrigger.from_crontab(cron_expression)` with no `timezone` kwarg --
# APScheduler's CronTrigger.__init__ resolves an unset timezone to
# get_localzone() (the HOST's local tz) at construction time, never consulting
# the scheduler's own UTC config, despite from_crontab's docstring claiming it
# defaults to the scheduler timezone. Measured: on the deploy host,
# CronTrigger.from_crontab('30 7 * * *').timezone resolved to America/Vancouver,
# a -25200s offset -- the same magnitude independently measured in the
# peer_list.updated_at UTC-naive bug. The plugin's own KB doc claims "All cron
# expressions use UTC timezone", making the doc part of the defect. Pins a
# fixed non-UTC get_localzone() (not the host's real zone) so the assertion
# stays meaningful on a UTC-local CI host. Covers both the live add_cron_job
# call site and the restore_schedules path (same call site, delegated).
# Hermetic -- real SchedulerManager/APScheduler, get_localzone patched, no DB.
plugins/default_scheduling_plugin/tests/cron_utc_timezone_smoke.py

# ActionExecutor-not-initialized deferred-fire repair (2026-08-16
# lane-utc-scheduler, defect A). Reproduced from
# profile/data/logs/2026-08-15_profile.log: sch-2n0h8hh6jp9te and three other
# schedules fired during two separate blue-green swap startup windows, before
# set_action_factory had run in the candidate process. The old code's
# _mark_schedule_failed on this path persisted schedules.status=error, which
# SchedulerManager.restore_schedules reads to SKIP the schedule on the very
# next restart -- a one-shot write, not an ongoing firing loop, is what killed
# sch-2n0h8hh6jp9te permanently. Pins BOTH halves of the fix: the fire-time
# write must leave schedules.status untouched (universal job tracker gets
# 'pending', an allowed asynchronous_jobs.status CHECK value, not 'failed'),
# AND -- per explicit rider, since a shallower smoke could miss a regression
# that only re-breaks restore -- the resulting schedule must actually SURVIVE
# SchedulerManager.restore_schedules and get re-armed in APScheduler, not just
# avoid the status write in isolation.
# Hermetic -- real SchedulingPlugin/SchedulerManager, fake repository/tracker
# doubles, no DB.
plugins/default_scheduling_plugin/tests/action_executor_deferred_fire_smoke.py

# The peer_inbox platform process (the PULL receive path for
# sessions with no MCP and no live watcher) plus peer_holds_role's additive
# delivery_route_attached. Gates the layer the process owns: caller-identity
# resolution off the peer_binding row, the two INDEPENDENT cursors, the page
# bound, the loud-failure set (an inactive bridge must ERROR, never read as an
# empty inbox), and the serialized shape the return_value_schema declares. Also
# runs the wake footer's own advertised command through a shell and into the
# process -- a shipped instruction that cannot execute is the defect this lane
# closed. Hermetic: in-memory stores, no bridge, no DB.
plugins/agent_messaging_plugin/tests/peer_inbox_process_smoke.py

# Peer-enumeration asymmetry close (WS-1a follow-on, operator-prompted
# 2026-08-02): peer_inbox let a no-MCP session read its OWN mail; peer_list
# closes the companion gap -- the same session had no way to see who ELSE was
# live. Global unfiltered registry snapshot, zero parameters (nothing to scope
# by). Gates: an inactive bridge/missing registry ERRORs (bridge.not_running),
# never reads as an empty snapshot; a genuinely empty registry still succeeds
# with a real empty snapshot (the two must stay distinguishable); the
# per-agent_id grouping and sorted agent_ids; the field-set decision pinned
# explicitly -- bridge_id and agent_session_id must NEVER appear in the
# serialized row even when present on the underlying binding, so the no-MCP
# path never gets a wider exposure than the two pre-existing MCP surfaces
# (localhost HTTP route, Streamable MCP tool) have ever had; three-surface
# parity -- the process's output is asserted byte-identical to calling
# peer_list_view.serialize_peer_list directly, proving delegation rather than
# a hand-rolled copy that could drift; and the declared return_value_schema
# matches the actual returned keys exactly. Hermetic: in-memory store, no
# bridge, no DB.
plugins/agent_messaging_plugin/tests/peer_list_process_smoke.py

# Result-processing contracts (c) — a plugin's TYPED ErrorDetail must survive to
# the stored action result. `_mark_action_failed` stored a constant
# code="action_failed" on every failure, so every /process/call consumer
# platform-wide saw one code and could not discriminate failure classes. The
# constant was the symptom: the signature took `error_message: str` and all
# three call sites stringified first — one of them threw away a full ErrorDetail
# dict it already held. Drives the REAL _mark_action_failed (only the DB seams
# and the downstream dispatch hop are stubbed) and asserts on what is STORED.
# Carries the untyped control: no typing in => the historical constant out, so
# the fix cannot be satisfied by inventing codes it does not have.
ananta/tests/core/actions/action_failure_error_typing_smoke.py

# peer_mark_role_covered (pull-surface boundary design, R1 identity fencing) --
# the write's security surface. Refuses a call with no registered-route
# identity (state["inference_vertex_session_id"] absent, the one-shot
# `solet call` shape) and proves the CLI's caller_attribution_* family is
# NEVER consulted as a fallback even when it names a valid role holder --
# the exact instance-id-spoofing shape measured against peer_claim_role.
# Covers the live ownership re-check (a displaced prior holder cannot advance
# the mark past mail the new holder never saw), attest-by-message_id (a
# caller can only ever name a pair that corresponds to a row that exists),
# and the monotonic no-op (an older attestation never regresses the stored
# mark). Hermetic: in-memory stores, no bridge, no DB. The floor/byte-ceiling
# logic this mark feeds is covered separately in
# ananta/tests/llm/agent_messaging/role_inbox_smoke.py.
plugins/agent_messaging_plugin/tests/peer_mark_role_covered_smoke.py

# Fleet session-management D4 Part B item 1 (§3.1 Q1) — the legislate_role
# governance-act verb: the ONE sanctioned path that stamps an
# authority-carrying role_class ('primary'/'principal') onto a role row at
# birth. (a) a fresh legislation creates the row; a same-class re-run is an
# idempotent no-op; a DIFFERENT-class re-run is refused loud
# (RoleClassConflictError), never silently reassigned. Verb-layer guards
# (role_class_not_legislatable, reserved_primary_name_required,
# missing_argument) driven directly. (b) the ordinary claim path's
# reserved-mint refusal is exercised against a name THIS SMOKE NEVER
# LEGISLATES (Zzz-Main, not Coordinator-Main) so the guard is genuinely tested, not
# vacuous — and the failure message is asserted to confirm WHICH of the two
# reserved_role_name-emitting guards fired. Plus condition 2: claim-time
# fleet-uniqueness (displaces, never coexists) and class-consistency
# (cardinality_conflict against a session already holding a different named
# role) asserted AGAINST THE ROW THIS ACT ACTUALLY LEGISLATES, reusing the
# landed D1 claim gate rather than rebuilding it. Real RealShapeState, no
# bridge/DB.
plugins/agent_messaging_plugin/tests/legislate_role_smoke.py

# schema-debt-external-id lane, 2a (2026-08-06): session_ledger's schema
# smoke, extended to gate the standardizer's external_id unique=False
# relax (leg 1) and the real event table's composite domain key surviving
# standardization alongside it (leg 3) — reverting either half of the fix
# reds this. Registering now (was previously unregistered) because it is
# the direct regression guard for a defect that shipped a fleet-wide
# skip-storm; offline, no DB.
ananta/tests/llm/session_ledger/schema_smoke.py

# schema-debt-external-id lane, 2a (2026-08-06): behavior-pin for the
# OTHER (raw-dict/bootstrap-dialect) external_id validator in
# schema_management_service.py, deliberately left unrelaxed this slice
# (Path 2 has no caller with the composite-conflict shape yet — see
# workbench/2026-08-06_schema_debt_external_id_findings_schema-debt-impl.md).
# Reds if a future change relaxes Path 2 without reading that follow-on
# record, so the two validators can't silently drift further apart.
# Offline, no DB.
ananta/tests/services/state_service/schema_management_service_external_id_pin_smoke.py

# schema-debt-external-id lane, 2b-S1 (2026-08-06): the retire_duplicate_source
# repair verb (canary-gated duplicate-source dedup — session/import_batch/
# source_cursor/active_lease re-point + soft-delete). Legs: dry-run count
# reporting; refused while the loser is still enabled; refused while an
# active polling lease is shown; an expired lease does NOT block; the happy
# path re-points all four child tables with the exact filter/update shape
# asserted and soft (never hard) deletes the loser; a still-referenced-after-
# repoint race raises and never soft-deletes a non-orphaned row; fail-fast
# refusals (same id, missing row, source_kind mismatch) fire before any
# child-table read. Stub state-service, offline, no DB.
ananta/tests/llm/session_ledger/retire_duplicate_source_smoke.py

# schema-debt-external-id lane, 2b-S1 (2026-08-07): set_source_enabled, the
# missing quiesce-protocol prerequisite retire_duplicate_source's canary
# discovered live (no verb could set enabled=false on a source row). Legs:
# disable->re-enable round trip with exact prior/new + update-shape
# assertions; a same-value call is an idempotent no-op (zero writes); an
# unknown source_id raises before any write; integration leg proving the
# quiesce linkage — a disabled source refuses poll_source. Stub
# state-service, offline, no DB.
ananta/tests/llm/session_ledger/set_source_enabled_smoke.py

# rotation-systematization P2 slice A (ratified 2026-08-07) -- seat_rotation_helper's
# pane-resolution 0/1/N gate (mutation-proven against the sibling plugin's dict-collapse
# defect), poll-until-stable settle detector (consecutive-reset + timeout fail-closed),
# and the full run_rotation send sequence's exact two-call text-then-CR shape + ordering
# (a settle timeout or pane ambiguity NEVER reaches the pickup send). Offline, stub
# iterm2-shaped fixtures, no real API connection.
plugins/agent_messaging_plugin/tests/seat_rotation_helper_smoke.py

# rotation-systematization P2 slice A/B (ruling 3, ratified 2026-08-07) --
# rotation_thresholds' declared ceiling table (ships empty per ruling), conservative
# fallback for any unmatched model, and the 50%-threshold boundary comparison.
plugins/agent_messaging_plugin/tests/rotation_thresholds_smoke.py

# L4c self-notice leg (2026-08-17) -- the rotation surface's third leg, which
# reaches the MEASURED SESSION ITSELF rather than its steward. Pins: the
# 300K-500K blind spot (band-keyed, not `rotation_due`-keyed -- the two
# instruments provably disagree there); band-EDGE latching so an escalation is
# never suppressed by the milder notice that preceded it; the 20-minute floor
# as a repeat bound rather than a period; the coverage gap counted as a number
# rather than swallowed; the capacity band firing early on an unrecognised
# model (fail-safe, never fail-open); tier-invariance of the break-even; the
# overage TTL moving the threshold DOWN; one horizon per notice, not two; and
# -- with drive_on_delivery replaced by a landmine -- that nothing on this
# path can inject a turn into the session it is notifying.
plugins/agent_messaging_plugin/tests/rotation_self_notice_smoke.py

# L4c ROUTING JOIN (2026-08-18) -- the `agent_session_id` column that makes a
# watcher-held worker reachable from its own gauge row. A SEPARATE file from the
# suite above on purpose: that one passes green and UNCHANGED against a build in
# which this join is dead code, because it tests the leg's decisions against a
# fake registry that resolves by instance id alone. The join is a WIRING fact,
# so everything here drives a REAL PeerRegistry over a real peer_binding store
# and a REAL BridgeSessionManager, and asserts on an event that really landed on
# a bridge. Pins: delivery to a session whose gauge row and live binding are
# keyed on DIFFERENT ids; the paired control (same scenario, join NULL ->
# unroutable, nothing delivered) without which every delivery is equally
# explained by "the instance lookup happened to work"; that a session id is
# RESOLVED through the registry and never rebuilt from the ledger id, proven by
# a decoy binding registered under exactly the value a reconstructor would build
# -- so a deriving build MISROUTES rather than merely failing; that the join is
# never mandatory, so a bridge-held seat still resolves on its own key; NULL
# preserved as NOT REPORTED at every boundary; a duplicate session-id binding
# failing loud rather than being counted as a coverage gap; the reporting hook
# actually SENDING the field, in BOTH repo copies, at an AGREEING generation
# (compared, never hardcoded, so an honest future bump does not red); and the
# plugin.py raw.get mapping not silently dropping it -- the joint at which the
# whole landing can be declared, stored, resolved and still write NULL forever.
plugins/agent_messaging_plugin/tests/rotation_self_notice_join_smoke.py

# rotation-systematization P2 slice B (ruling 2, ratified 2026-08-07) -- the
# rotation_due_watch.py PostToolUse hook: threshold fires with identity-in-content /
# below-threshold stays silent / latch (keyed on claude_session_id, independent of
# throttle) prevents re-fire / unknown-model falls back to the conservative ceiling /
# no-steward-role falls back to a local self-notification marker. Note: this hook and
# its test live under .claude/hooks/ -- the gate scope regex misses that directory
# (known trap), so GC must hand-run the static gates on rotation_due_watch.py and
# .claude/hooks/tests/rotation_due_watch_smoke.py directly; this line only registers
# it with run_smokes.py's behavioral gate, which IS scope-independent.

# L4b (2026-08-17, lane-l4b-rotation-surface) -- rotation_due_notice.py, the
# UserPromptSubmit DELIVERY half for a session the sweep cannot reach. The sweep
# notice lands on a STEWARD's bridge and drive_on_delivery no-ops for an
# operator-launched session, so the one session whose rotation decision is the
# expensive one was reached by nothing; this hook surfaces the self-notify marker
# rotation_due_watch.py was already writing and nobody was reading.
#
# Pins: the EMPTY path (fired on every prompt) emits absolutely nothing; a marker
# is surfaced once and then stamped, so it can never repeat; a marker keyed to
# another session generation is not ours; the writer's temp-dir fallback root is
# searched too; several markers still produce exactly ONE json object (two are not
# two notices, they are unparseable stdout); an unstampable marker is still
# DELIVERED; and -- the security leg -- a marker owned by another uid or
# group/world-writable is REFUSED, because this hook's output enters a prompt and
# its input lives where on a shared host anyone can write.
#
# Same .claude/hooks/ scope-regex trap as the sibling above: GC must hand-run the
# static gates on .claude/hooks/rotation_due_notice.py and this test directly;
# this line registers only the behavioural gate, which IS scope-independent.

# rotation-systematization P4(b) (P4(a) ratification 2026-08-07, six bound
# conditions) -- the idle-boundary seat watcher: poke/rotate precedence
# (mutation-proven -- pending mail defers rotate even past its threshold, and
# poke wins over rotate whenever both conditions could apply), poke-cooldown
# boundaries, the idle-file/transcript-mtime cross-check's fail-open vs
# fail-closed split, the ACT-time gate (condition 5, mutation-proven --
# run_rotation is NEVER invoked when the gate refuses), and the hard privacy
# rule (condition 2, mutation-proven -- the injected poke text is EXACTLY the
# fixed constant, never built from inbox content). Offline, stub iterm2-shaped
# fixtures + a stubbed solet call, no real API/fleet connection.
plugins/agent_messaging_plugin/tests/seat_idle_watcher_smoke.py
# Deaf-wake fix (2026-08-08) -- black-box smoke for the four project-
# vendored Python ports of coordination-hooks@<solet>'s JS hooks under
# .claude/hooks/ (wake_waiter.py, check_messages_reminder.py,
# step_zero_reminder.py, role_binding_reminder.py). Each invoked as a real
# subprocess exactly as Claude Code invokes it; covers wake_waiter.py's
# exit-2 wake-signal contract (mutation-proven), the unresolvable-CLI and
# wrong-transport/precondition-miss silent paths, and role_binding_
# reminder.py's JSON.stringify-equivalent label quoting (mutation-proven).

# R4 seed-packaging audit, Package B (2026-08-10) -- parity smoke for the
# memory-passthrough origin-resolution ladder (env SOLET_NAME ->
# root_manifest.yaml solet_name, placeholder-skipped ->
# CLAUDE_PROJECT_DIR-basename), asserting the two independent
# implementations (.claude/hooks/memory_passthrough/_journal.py and
# agent_messaging_plugin.plugin's _resolve_solet_name_for_memory_tags)
# agree on output across an env/file/dirname matrix. Requires the venv
# (imports agent_messaging_plugin.plugin) and SOLET_NAME set at
# import time (an unrelated pre-existing module-level requirement in
# plugin.py); skips loud rather than failing if SOLET_NAME is unset.
# Mutation-proved: breaking the placeholder-skip guard on either side reds
# exactly that scenario's two targeted assertions, nothing else.

# fleet-wake-integrity Task 1 (2026-08-08) -- proves the codex wake_capable
# flag-loss root cause: get_peer_binding_schema()'s persisted columns never
# included wake_capable, PeerRegistry.register()'s insert() dict never wrote
# it, and _binding_from_row() never read it back, so every registration's
# declared value was silently dropped and every resolved binding read back
# the BridgeBinding dataclass default (True) regardless of what the bridge
# declared. Mutation-named: reverting any of the three write/read points
# reds test_wake_capable_false_persists_through_register_and_resolve.
# Offline, in-memory Store backend only.
plugins/agent_messaging_plugin/tests/peer_registry_wake_capable_persistence_smoke.py

# managed Codex runtime parity (2026-08-10) -- orthogonal
# agent_runtime x host registry, declarative ledger/default + restart carry,
# Codex-native app-server/config/identity path, provider-overlay refusal,
# /clear and /compact protocol translation, styled tmux pickup verification,
# and watch registration without an implicit role claim. Offline records-only
# fakes; no model turn, tmux server, bridge, or database.
plugins/agent_messaging_plugin/tests/codex_managed_session_smoke.py
# initialization/tests/profile_loader_smoke.py — profile-loading smoke, brought
# into the per-file static-gate scope by the tail-class widening (625f70886 +
# d89b0dca7). Registered 2026-08-09 after Git-Controller's landing check found
# the cited "325/325" had never exercised it (this register is an allowlist;
# in-scope-for-static-gates is not in-suite). Offline: filesystem/profile
# parsing only, no live platform.

# spawned CLAUDE-worker peer registration (2026-08-14, registration-loss RCA
# lane R) -- the tmux/headless adapters' absolute solet resolution and PATH
# export, the presence sidecar that actually puts a spawned worker in
# peer_list (--no-claim, spool KEPT unlike Codex, none on the mcp transport),
# and the /rename skill + seed-template lockstep that claims against the
# derived agi-watch id rather than the launcher's AGENT_INSTANCE_ID. Offline:
# argv/env shape assertions and file-text reads only; no tmux server, bridge,
# database, or model turn. Every leg verified to RED under a named mutation.
plugins/agent_messaging_plugin/tests/spawned_worker_registration_smoke.py

# retrieval-companion runner (2026-08-14, yaml-runner canonicalization lane Y) --
# the pure logic of plugins/default_knowledge_plugin/tools/run_retrieval_companions.py:
# shape validation, discovery across BOTH companion filename spellings
# (*.retrieval_test.yaml AND *_retrieval_test.yaml), the ratified split scope
# (targets judged within the article's own KB, forbidden judged cross-KB), rank
# scoring at every threshold boundary, and the refusal to build a verdict from an
# unreadable report envelope. The RUNNER itself needs live retrieval and is
# deliberately NOT registered here (same class as tools/retrieval_probe.py, run
# post-deploy); this entry gates the half that can be proven offline -- temp-dir
# fixtures and hand-built payloads, no solet, no database, no model turn. Every
# leg verified RED under a named mutation, including the one that first survived
# green: deleting the retired-key guard left `non_target_queries` still refused by
# the downstream unknown-key guard, so the assertion now pins the MESSAGE, not
# just the exception type.

# retrieval-audit scope split + LEGACY_KEY finding (2026-08-14, retrieval-audit
# revival lane A) -- the audit path's half of the same hoist: ScopePolicy /
# ScopePlan / RATIFIED_SCOPE_PLAN / RETIRED_KEYS now live in retrieval_audit.py
# and run_retrieval_companions.py imports them, so this smoke proves object
# IDENTITY across both consumers, not just equal values. Also guards
# run_case_under_scope_plan's one/two-call merge (process_key_freshness always
# from the call that carried the target queries, never double-counted) and the
# new LEGACY_KEY finding: parse_test_file stays tolerant of the retired
# non_target_queries key (never raises, still coerces to an empty forbidden
# set) but records it, and aggregate_audit fails the article on it even when
# every live query passes -- closing the defect where five companions on the
# retired spelling reported PASSING with their overreach claims unenforced.
# Offline by construction: hand-built payloads and a temp-dir fixture, no
# solet, no database, no model turn.
plugins/default_knowledge_plugin/tests/retrieval_audit_scope_and_legacy_smoke.py
ananta/tests/core/services/job_service_retrieval_smoke.py
ananta/tests/core/services/job_service_lifecycle_smoke.py
plugins/agent_messaging_plugin/tests/cli_job_await_smoke.py

# completion-wake routing (2026-08-14, lane-w-completion-wake) -- the PUSH half
# of the background-job completion contract, whose durable/retrievable half the
# job_service smokes above cover. The core smoke pins the replace decision: a
# bridge-origin flow carrying a completion_route_role stamp submits the delivery
# verb INSTEAD of the inference continuation, an unstamped flow stays pull-only
# (the negative control that keeps coverage honestly partial), errors route like
# results, a failed delivery falls back leaving the unreached marker standing,
# and -- the property most at risk from a careless refactor -- a routed
# completion still resolves its FRG token. The plugin smoke pins the dispatch
# stamp and the delivery verb, including why it is not peer_send_by_name: that
# verb's sender ladder would resolve a bridge-origin completion's sender to the
# job's own originator, stamping every completion as sent by its recipient.
# Offline by construction: fakes and hand-built rows, no solet, no database, no
# model turn.
ananta/tests/core/actions/job_completion_role_routing_smoke.py
plugins/agent_messaging_plugin/tests/job_completion_delivery_smoke.py

# oversized action payloads + action-path liveness (2026-08-15,
# lane-ac-payload-guard) -- the "cannot happen again" half of the 2026-08-15
# action-queue freeze, whose root cause was an 82 MB deliver_result payload
# whose json.loads held the GIL for two hours and starved every other thread.
# The smoke's load-bearing case is [3]: it hands the claim guard input that is
# BOTH over the bound and unparseable, so a guard placed downstream of
# json.loads raises a decode error while a correct pre-parse guard raises the
# size refusal -- the only assertion that can distinguish a guard which fires in
# time from one that has already caused the outage it was meant to prevent. The
# fixtures ADVANCE (a real over-bound string, a real over-cap record set) rather
# than asserting on constants, and every guard here was red-mutated: disabling
# the enqueue check, parsing before measuring, restoring the unbounded read,
# restoring the queue-depth gate on the liveness alarm (GAU-10, 2026-08-18 --
# the gate suppressed the alarm forever on a freeze that began at depth 0,
# since only the poller being killed could ever unstick it), and requeueing an
# oversized orphan each turn exactly the check that names that property red.
# Offline by construction: pure functions and a spy, no solet, no database.
ananta/tests/core/actions/oversized_payload_guard_smoke.py

# migrate_to_solet LaunchAgent OWNERSHIP — both directions, because each alone
# is satisfied by a catastrophic implementation. The P2 rebrand renamed
# --homunculus to --solet; the migration knew the flag and would have rewritten
# it, but skipped candidates by NAME (com.openai.*), which caught our own
# tunnel supervisor and left the ChatGPT connector without public ingress for
# four days (2026-08-15). Positive-only would pass a change that migrates every
# plist on the machine — the exact blast radius the skip exists to prevent;
# negative-only would pass the original defect. Also pins report/apply
# symmetry: --scan-stale never looked at ~/Library/LaunchAgents, so it returned
# a clean report about the one surface apply mode edits. Red-mutated three
# ways: restoring the name-prefix skip, forcing ownership true, and dropping
# the LaunchAgents scan — each flips a different named check. Offline by
# construction: temp tree, no launchctl, real machine never read.
plugins/macos_self_deployment_plugin/tests/migrate_to_solet_ownership_smoke.py

# adopter issue #9 -- the split result envelope (2026-08-15,
# lane-ae-issue9-envelope). An envelope came back carrying a success payload
# AND an unsupported_on_host error. Both halves were real: error_message is one
# mutable column on the action_events row, result is the latest of many
# action_results rows, and one action row that executed twice ends up pairing
# attempt 1's error with attempt 2's success. The completed-status writer never
# cleared the column; its failure counterpart always sets it.
#
# The smoke registers BOTH halves of the fix, because the first alone is a
# regression: a bare clear makes the envelope clean and the double execution
# INVISIBLE, deleting the only surviving evidence that an action row ever runs
# twice. So the clear must announce itself -- a WARNING carrying the action_id
# and both timestamps, plus a counter on get_metrics().
#
# Red-mutated four ways, each flipping a different named check: dropping the
# clear reproduces the adopter's exact envelope; clearing SILENTLY (the trap --
# the one-line patch) leaves the envelope check green and reddens all seven
# instrument checks; announcing unconditionally reddens the clean-completion
# control; and swallowing an unreadable pre-clear read reddens the blind-spot
# check. Nothing under test is stubbed -- the two shipped status writers, the
# shipped result store and the shipped envelope reader all run against an
# envelope-faithful in-memory pair of tables. Offline by construction: no
# solet, no database, no model turn.
ananta/tests/core/actions/completion_clears_stale_error_smoke.py

# query_state_bounds_smoke (2026-08-15, lane B of the unbounded-state-read
# programme): pins the MAX_READ_ROWS row bound at EVERY query_state
# implementation, which had four and they disagreed.
#
# Registered mainly as a REGRESSION PIN ON A FALSE PREMISE. The programme's
# census recorded 70 query_state call sites as structurally unable to bound
# themselves, on the reasoning that its (namespace, filters) signature has no
# limit slot. It has one: the filters dict IS read_state's query dict, so
# limit/unbounded pass straight through and always did. Section 1 measures that
# end-to-end so the belief cannot be re-derived from the signature and acted on
# a second time -- the census instrument itself encoded it and mis-filed a call
# site that was already passing limit=1.
#
# The two REAL holes it closes, neither of which the census could see because it
# classified call sites and never implementations: BootstrapDatabaseStorage
# carried its own copy of the body that skipped the cap its sibling read_state
# applies to the same rows off the same in-memory store; and the transaction
# query_state had no bound at all -- which mattered because StateTransaction
# exposes no read_state, making it the ONLY row-returning read primitive inside
# a transaction, with no bounded alternative to migrate to.
#
# Red-mutated in both directions per hole, each reddening a different named
# check: undoing the bootstrap delegation reddens the over-cap refusal; making
# bootstrap read_state actually filter (the opposite direction, and the change
# deliberately NOT made) reddens the no-result-change checks; dropping the txn
# overflow raise reddens the never-truncate pair; and dropping the fetch_limit
# pass-through reddens the LIMIT-compilation pair. Section 4 is the load-bearing
# one: an over-cap unbounded read must RAISE, never return a prefix.
#
# PURE UNIT, offline by construction: real implementations against recording
# fakes. No solet, no database, no model turn.
ananta/tests/services/state_service/query_state_bounds_smoke.py

# --- Lane A, unbounded state reads (2026-08-15 SCAN_UNBOUNDED remediation) ---
#
# bounded_read_ceiling_smoke pins the CALL-SITE half of the read-bound contract.
# read_bounds owns the provider cap; bounded_read owns what a site must do when
# it passes an explicit limit to read a whole small table. The asymmetry it
# guards: no limit -> loud refusal past the cap, explicit limit ->
# overflow_is_error=False -> SILENT PREFIX past the limit. So "just add a limit"
# converts a site that failed loudly at 10,000 into one that quietly returns 500
# of 600. The boundary is >= (at exactly `ceiling` rows a result cannot be told
# from a truncated one); the M1 calibration mutates precisely that.
#
# unbounded_scan_repair_behaviour_smoke asserts the repaired sites DO THEIR JOB
# rather than merely pass a limit. Each of the three carried two defects — an
# unbounded read AND a result navigated through a data["result"] key that
# read_state has never returned — so a bound-only assertion would have gone green
# against all three at once. Calibrated with the navigation reverted while the
# bound is KEPT (M4): the resolved-value checks redden while every filter/limit
# check stays green.
#
# PURE UNIT, offline by construction: real production classes against recording
# spy state services. No solet, no database, no model turn.
ananta/tests/services/state_service/bounded_read_ceiling_smoke.py
ananta/tests/services/state_service/unbounded_scan_repair_behaviour_smoke.py

# 2026-08-15 read-cap boot repair — the three agent_messaging_plugin startup
# backfills, all of which read a whole table with no limit on the BOOT path.
# _run_startup_backfills runs them before the FastAPI surface comes up, so a
# refused read there does not degrade a feature, it stops the bridge starting:
# that is exactly what happened when the default read bound dropped to 100 and
# role_class_backfill's 133-row role_binding read was refused, taking agent
# messaging and the action queue down with it.
#
# All three reads are paginated now (bounded_read.iter_table_rows). Each smoke
# carries a fixture that STRADDLES THREE PAGES, because the property under test
# is completeness across a page boundary and a fixture below the boundary cannot
# test it — the pre-repair role_class smoke was green on five rows throughout the
# outage. Each also wraps RealShapeState in CapEnforcingState, which refuses an
# over-bound unbounded read the way the provider does; without it these smokes
# stay green against code that cannot boot at all.
#
# role_class_backfill_smoke plants its cardinality violation on page THREE (a
# single-page read reports a clean fleet) and is red-first proven against the
# pre-fix source: reverting the read reproduces the live
# query.unbounded_read_over_cap refusal.
#
# message_important_backfill_smoke EXISTED BUT WAS NEVER REGISTERED HERE, so it
# had never run. role_message_consumed_backfill_smoke is new — that module's
# docstring claimed "S7 exercises it against a real state fake" and no such test
# existed anywhere in the tree.
#
# PURE UNIT, offline: in-memory state over real ActionResult shapes, with pages
# served by the real ordered-query primitives. No solet, no database.
plugins/agent_messaging_plugin/tests/message_important_backfill_smoke.py
plugins/agent_messaging_plugin/tests/role_message_consumed_backfill_smoke.py

# read-cap sibling sweep, 2026-08-15 PDT / 2026-08-16 UTC (lane-ak). Pins
# read.py's per-session event walk after it stopped being an unbounded
# query_state + Python sort over a 2,077,082-row table.
#
# REGISTERED IN THE SAME CHANGE AS THE CODE, deliberately. The sibling
# read_migration_live_smoke.py is NOT in this register and therefore has never
# run in the suite — a 360/365 green says nothing about a file outside the
# allowlist. That gap is exactly what this sweep found when it changed that
# smoke and then could not demonstrate the change was exercised.
#
# The fixture is DELIBERATELY UNLIKE PRODUCTION and must stay that way: live
# data is dense with zero created_at ties, i.e. benign in both dimensions a
# cursor can get wrong, so a production-shaped fixture would test nothing. Its
# created_at-tie group is the only executable evidence for the "wrong tool"
# rule in bounded_read.iter_table_rows' docstring; delete it and "simplifying"
# this site onto the shared helper starts passing review.
#
# Red-first calibrated against 4 mutations: dropped id tie-break (4 red),
# first-page-only (5 red), full-page-means-done (5 red), and ordering by
# created_at instead of sequence (1 red — precisely the ordering assertion).
#
# PURE UNIT, offline: a query_ordered fake that honours row-value `after`
# cursors, refuses a cursor/order_by arity mismatch, and applies the
# include_deleted default. No solet, no database.
ananta/tests/llm/session_ledger/event_sequence_walk_smoke.py

# 2026-08-16 read-cap, second round — the flow-scoped counters in core/.
# Both sites read every matching row and returned len(rows), and both were
# found by the census and classified HOT, then parked in a tier-2 list whose
# predicates were SAMPLED by eye rather than measured. {flow_id_trace, state}
# and {flow_id_trace, status} read like narrow keys; they matched 129 and 2,902.
#
# FlowRuntimeGraph.get_pending_token_count is reached from complete_token on
# EVERY action completion — it failed loudly and broke the completion path.
# ActionQueuePoller._get_flow_error_count is worse because it is QUIET: its
# record extractor graceful-degrades a refused read to [], so it reported 0
# errors for a flow with 2,902 and the max-flow-errors CIRCUIT BREAKER (threshold
# 3) silently stopped firing — on a path only ever taken because a flow is
# already going wrong.
#
# Both repaired with the scalar `count` verb rather than a bound: the callers
# wanted a number, and count runs the aggregate in the owner plugin and ships no
# rows, so it is outside the row cap entirely. get_pending_tokens genuinely needs
# its rows and is paginated instead.
#
# Red-first proven: reverting the FRG count reproduces the live production error
# string verbatim ("Failed to query pending token count for flow ..."); reverting
# the poller count reddens 3 checks; degrading the scalar extractor to 0 on a
# non-completed envelope reddens 2. That last mutation was GREEN until
# calibration found the branch uncovered — restoring 0 there would have
# re-silenced the breaker one layer down without failing anything.
#
# PURE UNIT, offline: pages served by the real ordered-query primitives, and the
# fake REFUSES an over-cap unbounded read the way the provider does. Without that
# refusal the smoke would pass against code that cannot run in production.
ananta/tests/core/state/flow_counter_bound_smoke.py

# read-cap sweep wave 2a, 2026-08-16 (lane-ak).
#
# session_page_walk_smoke — pins read_support.walk_sessions_page, which replaced
# list_sessions' unbounded whole-table read (14,412 rows to return 50; MEASURED
# DEAD on rel-20260816T042114Z-41eed531f with cap_rows=100).
#
# Its fixture is DELIBERATELY UNLIKE PRODUCTION and must stay that way. The
# defining property is POSITION, not size: qualifying rows lie BEYOND the first
# page ("band A" = 90 non-qualifying rows at the head of the sort order), because
# a big fixture whose survivors all sit in page 1 exercises the cursor exactly as
# little as a 5-row one. Band A is also the ONLY reason the inlined M3 check —
# pushing `limit` down without post-filtering, which silently UNDER-RETURNS — is
# detectable at all. If M3 ever passes trivially, band A has been weakened:
# repair the fixture, do not delete the test.
#
# Red-first calibrated: dropped id tie-break (RED: 10 of 20 tie-group rows lost
# at a page boundary), non-advancing cursor (RED: walk ceiling raises), dropped
# pushed-down equality filters (RED: wrong-vendor row appears). The tie-break
# mutation did NOT redden on the first attempt — the fixture contained the tie
# group but no test reached it at the default limit; that gap was found by
# running the mutation and is now closed by test_tie_group_survives_a_page_boundary.
#
# list_sessions_m17_filters_smoke — EXISTED SINCE M17 AND HAD NEVER BEEN
# REGISTERED, so none of its 26 checks had ever run in the suite. Third
# unregistered smoke found during this sweep. It covers list_sessions' windows,
# order variants, limit, canonical-only default, vendor filtering and both
# source_kind routes — i.e. exactly the surface wave 2a changes.
#
# Both PURE UNIT, offline: in-memory state shims over real ActionResult shapes.
# No solet, no database.
ananta/tests/llm/session_ledger/session_page_walk_smoke.py
ananta/tests/llm/session_ledger/list_sessions_m17_filters_smoke.py

# read-cap sweep census follow-on, 2026-08-16 (lane-ak).
#
# census_fold_smoke — EXISTED SINCE THE 2026-06-20 CENSUS MIGRATION AND HAD NEVER
# BEEN REGISTERED, so none of its checks had ever run in the suite. FOURTH
# unregistered smoke found during this sweep (after message_important_backfill,
# core_slice3_migration_live, and list_sessions_m17_filters). It is the ONLY
# behavioural coverage of build_census, including the change registered here.
#
# It now also pins the streaming property: build_census reads __source whole (21
# rows) but STREAMS session / tool_call / import_batch through base.walk_table
# rather than reading them whole (27,208 / 637,496 / 284,787 rows measured;
# `census` was DEAD on rel-20260816T042114Z-41eed531f, refusing on `session`).
#
# Its `walk` fake is a GENERATOR on purpose. Streaming exposed a trap that lists
# had hidden: _CensusAggregator consumed `sessions` TWICE — once to index, once
# to tally — which is merely wasteful for a list and silently WRONG for a
# generator (the second pass sees an exhausted iterator and every session tally
# comes back empty, with no error). A list-returning fake would let that bug pass.
#
# Red-first calibrated: reintroducing the double consumption (RED, 4 checks incl.
# session_count=0), and dropping the id->source index build (RED, 5 checks — the
# tool_call and event joins lose their session lookup).
#
# PURE UNIT, offline: in-memory reader + state shim over real ActionResult shapes.
ananta/tests/llm/session_ledger/census_fold_smoke.py

# read-cap sweep wave 2b, 2026-08-16 (lane-ak).
#
# session_kind_membership_smoke — pins list_sessions(source_kind=...), which was
# over the 100-row cap TWICE: _junction_canonical_ids read __session_source_kind
# whole (4,005 rows for claude_code_local, 8,055 total, MEASURED), and its output
# then became `id = ANY(those 4,005)` on __session. The live refusal on the
# serving release named session_source_kind — the FIRST of the two — which is why
# the junction was repaired first.
#
# THE FAKE ENFORCES THE CAP, and that is the whole mechanism: it raises when an
# `= ANY` filter carries more than MAX_READ_ROWS values, and when an unlimited
# read returns more than MAX_READ_ROWS rows. Against a permissive fake an
# unchunked read succeeds quietly and the smoke certifies the bug it exists to
# catch. The refusal IS the test.
#
# That fake earned itself immediately: it caught a real defect in the first
# version of the repair. base._query_membership_chunked claims "a chunk matches
# at most its own length" — true only for a UNIQUE column. external_session_id is
# non-unique BY DESIGN (a canonical and its siblings share it), so a 100-value
# chunk returned 200 rows and was refused. That read is now chunked AND PAGED.
#
# Fixture size (120) is bounded on BOTH sides: above MAX_READ_ROWS so the walk
# pages and the read chunks, and below list_sessions' own limit clamp of 200 so a
# complete result is not truncated by the clamp. The first attempt used 250 and
# the verb correctly returned 200 — indistinguishable from a lost chunk.
#
# PURE UNIT, offline: cap-enforcing in-memory state over real ActionResult shapes.
ananta/tests/llm/session_ledger/session_kind_membership_smoke.py

# read-cap sweep, managed_session fleet list, 2026-08-16 (lane-ak).
#
# managed_session_list_bound_smoke — pins list_managed_sessions, the ONE fleet
# list behind plugin::agent_messaging_plugin::list_sessions. Its no-filter call
# was MEASURED REFUSING on the serving release: query.unbounded_read_over_cap,
# table managed_session, cap_rows 100, against 106 live rows. The ledger is
# append-mostly and unpruned, so it crossed the bound by accumulating history —
# a bound that fails as the fleet gets MORE use, same shape as the RUNNING
# import-batch set repaired earlier in this sweep.
#
# Fixture is 250 rows at a 100-row page so the walk must page more than once;
# the live table was 106, a near-miss, which is exactly why the refusal was a
# surprise. It also pins the SOFT-DELETE OVERRIDE: the old code seeded
# {is_deleted: 0} and let a caller's filters overwrite it, so is_deleted=1
# returned soft-deleted rows. iter_table_rows expresses that as include_deleted,
# so the request is TRANSLATED, not dropped — no in-repo caller uses it, but
# list_sessions forwards arbitrary filters, so removing it silently would be a
# behaviour change smuggled inside a bound fix.
#
# The fake ENFORCES the cap. Red-first calibrated: reverting to the pre-fix
# unbounded read reproduces the LIVE production error verbatim
# ("'managed_session' returned 250 rows against the 100-row cap"); a page-1-only
# walk returns 100 of 250; dropping the override translation returns 250 instead
# of 30 for is_deleted=1.
#
# PURE UNIT, offline: cap-enforcing in-memory state over real ActionResult shapes.
plugins/agent_messaging_plugin/tests/managed_session_list_bound_smoke.py

# F6/F7 gate repair (2026-08-16, lane-ai) — two defects in the gates themselves.
#
# 1. A gate that CRASHED was rendered as a gate that found VIOLATIONS: the
#    aggregate mapped every unrecognised exit code to "BLOCKING", so radon_cc's
#    RecursionError printed as "Blocking gate violations (radon_cc)" — complexity
#    debt in the reader's own code, asserted about code nothing had measured.
# 2. The scope walks were filesystem-based and reached vendored trees
#    (18,321 .py under plugins/cosyvoice2_tts_plugin/src/.venv_cosyvoice).
#
# Pins the scope predicate from BOTH sides, because the first fix over-corrected
# to tracked-only and produced a FALSE GREEN inside the hour — a brand-new
# never-added smoke with a CC-12 went unmeasured while the aggregate reported
# clean. So: a brand-new untracked file MUST be gated; a gitignored vendored
# tree must NOT be. Fixing either direction alone is how this went wrong twice.
#
# Every assertion is mutation-calibrated: reverting the wrappers to rglob, the
# scope to tracked-only, the classifier to "unknown exit = violation", or the
# exit-code-before-traceback ordering each turns this smoke red, and the
# negative control (a real violation still reads as a violation) stays green.
#
# PURE UNIT, offline: synthetic CompletedProcess objects + a tempdir; no
# subprocess, no gate run, no timing dependence.
quality_gates/tests/gate_crash_rendering_smoke.py

# Context-gauge storage layer (2026-08-16, lane-ai) — the verbs behind the
# rotation policy had NO plugin-side test at all before this. Their entire
# coverage was two hook-side fakes scripting the response envelope
# ({"status":"completed","result":{"data":{}}}), which assert nothing about
# contents and therefore CANNOT go red when the interface widens — they just
# keep passing. Widening safely required creating coverage, not updating it.
#
# Pins the tri-state contract that widening threatens: cache fields are
# true / false / NOT REPORTED, and a reporter that omits them must never read
# back as "cache is warm". Mutation-calibrated — coercing None to False in the
# store, coercing it in the read-back, or dropping the cache view entirely
# (the pre-widening shape) each turn this smoke red.
#
# Also pins that rotation_band is DERIVED at read time from the live policy
# constants rather than stored, so a threshold change never needs a backfill.
#
# PURE UNIT, offline: a recording state double, no database, no bridge.
plugins/agent_messaging_plugin/tests/context_status_verbs_smoke.py

# release-pointer stability (2026-08-17, lane-release-pointer) — the spawn-env
# half of deploy survival. Every spawn surface pinned the solet CLI into a
# VERSIONED release dir (AGENT_WAKE_CLI, the PATH prepend, and the long-lived
# `solet watch` registration sidecar's argv[0]), and a deploy REAPS old
# releases, so one cutover dangled every long-running worker's CLI at once —
# silently, because each consumer is non-fatal by design. Measured twice
# (lane-ai dark both ways across two deploys, 2026-08-16).
#
# Pins that the resolved path is expressed through the atomically-swapped
# `current` pointer, and — the leg that matters — that the pointer lands
# UNRESOLVED: a rewrite that calls Path.resolve() still names a real file
# TODAY, so every ordinary liveness check stays green while the version pin
# survives untouched. The suite asserts the release id is textually ABSENT
# from what reaches the environment, which is the only form that mutation
# cannot pass.
#
# Includes a DEPLOY-SWAP SIMULATION leg (old release dir removed, pointer
# moved) that re-stats AND re-executes the worker's pre-deploy env, plus a
# negative control proving the simulation really reaped something. Also pins
# the two refusals — mid-cutover version skew, and a pointer whose target was
# reaped — and the Repair-4 non-regression that a dangling pointer never
# breaks a session that would otherwise resolve solet from PATH, and never
# raises.
#
# Mutation-calibrated, 5 named mutations each verified RED: no-op rewrite;
# Path.resolve() re-pin; skew check dropped; stat/exec guard dropped;
# stabilization applied at expose_worker_cli only (which leaves the sidecar
# and worker_path pinned, i.e. registration still dies on deploy).
#
# PURE UNIT, offline: a synthetic release layout in a temp dir with real
# symlinks and executable stubs. No tmux server, bridge, database, network,
# deploy, or model turn.
plugins/agent_messaging_plugin/tests/release_pointer_stability_smoke.py
