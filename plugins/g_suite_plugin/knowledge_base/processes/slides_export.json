{
  "process_key": "plugin::g_suite_plugin::slides_export",
  "display_name": "Slides: Export",
  "description": "Export a Google Slides presentation to pdf or pptx into blob storage and return its deck_blob_key. Requires a connected account.",
  "embedding_description": "Export an existing presentation's rendered bytes (pdf or pptx format) via Drive's export_media into platform blob storage, returning a deck_blob_key reference (not the bytes themselves). Google-native presentations cannot be fetched with drive_download_file — this export verb is the correct path, exactly like sheets_export and docs_export for their respective product types. The returned deck_blob_key can be attached to email or handed to other plugins for viewing/printing. Fails gsuite.not_found if the id does not exist.",
  "result_processor_customizations": {
    "action_label": "Presentation exported to blob",
    "result_type": "g_suite_slides_export_result",
    "result_description": "Dict with deck_blob_key (a blob-storage reference to the exported bytes).",
    "presentation_guidance": "Confirm the export and format used. The deck_blob_key references potentially sensitive content — surface it for downstream use but do not treat it as public."
  },
  "error_processor_customizations": {
    "action_context": "Export a Google Slides presentation into blob storage.",
    "error_interpretation": "gsuite.invalid_params: missing id, or format is not pdf/pptx. gsuite.not_found: the id does not exist. gsuite.not_connected: no account linked.",
    "recovery_guidance": "gsuite.invalid_params: pass format as pdf or pptx. gsuite.not_found: the presentation may have been deleted. gsuite.not_connected: run connect_account."
  }
}
