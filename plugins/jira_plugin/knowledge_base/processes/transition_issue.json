{
  "process_key": "plugin::jira_plugin::transition_issue",
  "display_name": "Jira: Transition Issue",
  "description": "Move a Jira issue through a workflow transition (by transition id from list_transitions), optionally adding a comment. Returns the new status. Write action.",
  "embedding_description": "Apply a chosen workflow transition id to an existing Jira issue, moving the ticket from its current status to the transition's target status and optionally posting a comment during the move. Returns the resulting status. This is the workflow-state write for a selected transition.",
  "result_processor_customizations": {
    "action_label": "Jira issue transitioned",
    "result_type": "jira_transition_issue_result",
    "result_description": "Confirmation and the issue's new status after the transition.",
    "presentation_guidance": "Confirm the issue moved and state its new status. This changes a real ticket's workflow state."
  },
  "error_processor_customizations": {
    "action_context": "Move a Jira issue through a workflow transition.",
    "error_interpretation": "jira.bad_request: the transition id is not valid from the issue's current status, or a transition screen requires a field you did not supply. jira.not_found: the issue key does not exist. jira.invalid_params: key or transition_id was missing. jira.permission_denied: the account cannot transition this issue.",
    "recovery_guidance": "jira.bad_request: call list_transitions to get a currently-valid transition id (they depend on the current status). jira.invalid_params: supply both key and transition_id. jira.not_found: verify the issue key."
  }
}
