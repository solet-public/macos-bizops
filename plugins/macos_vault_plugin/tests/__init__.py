"""macos_vault_plugin test package.

W-VAULT-LOCAL-KEYCHAIN (Tier 3, 2026-06-07): root for the per-credential
Keychain dual-write smoke suite and the in-memory ``FakeKeychain`` that
keeps CI off the host's real OS keychain.
"""
