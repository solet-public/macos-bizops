name: openai_embeddings_plugin
description: >
  Openai Embeddings plugin process definitions and reference documentation.

category: plugin-internal
tags:
  - plugin_reference
  - openai_embeddings

content:
  patterns:
    include:
      - "**/*.md"
      - "**/*.txt"
    exclude:
      - "**/.git/**"
      - "**/.archive/**"
      - "processes/**"
  chunking:
    strategy: headers
    max_chars: 3000
    overlap_chars: 200
