{
  "hooks": {
    "PostMessage": [
      {
        "matcher": ".*",
        "command": "${INSTALL_DIR}/shipper.py --push-stdin",
        "description": "Session-shipper post-message tap for deployment ${DEPLOYMENT_ID}",
        "timeout_ms": 5000
      }
    ]
  }
}
