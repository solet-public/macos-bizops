name: postgres_state_management_plugin
description: >
  Postgres State Management plugin process definitions and reference documentation.

category: plugin-internal
tags:
  - plugin_reference
  - postgres_state_management

content:
  patterns:
    include:
      - "**/*.md"
      - "**/*.txt"
    exclude:
      - "**/.git/**"
      - "**/.archive/**"
      - "processes/**"
  chunking:
    strategy: headers
    max_chars: 3000
    overlap_chars: 200
